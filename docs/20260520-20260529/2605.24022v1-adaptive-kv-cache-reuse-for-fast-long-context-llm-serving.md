---
title: Adaptive KV Cache Reuse for Fast Long-Context LLM Serving
title_zh: 自适应KV缓存复用实现快速长上下文LLM服务
authors: "Fei li, Song Liu, Yan Liu, Jinhua Cui, Shiqiang Nie, Jinyu Wang, Weiguo Wu"
date: 2026-05-20
pdf: "https://arxiv.org/pdf/2605.24022v1"
tags: ["query:sparse-attn"]
score: 9.0
evidence: 针对非前缀场景的自适应KV缓存重用
tldr: 长上下文LLM推理中，预填充阶段TTFT成为主要瓶颈。传统KV缓存复用仅适用于严格前缀场景，非前缀复用会破坏全局注意力并引入异构存储I/O开销。本文提出CacheTune，通过离线频域分析识别关键KV对并选择性重计算，结合稀疏传输、异步流水及自适应重计算比例，实现语义恢复与计算I/O协同。实验表明，在主流模型上获得3.72x-4.86x的TTFT加速和3.93x-6.21x的吞吐提升，生成质量接近全重计算，且对SSD/HDD等I/O受限存储仍保持2.34x-2.36x加速。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.24022v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1468, \"height\": 866, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2605.24022v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 882, \"height\": 581, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2605.24022v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 877, \"height\": 395, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2605.24022v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 882, \"height\": 374, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2605.24022v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1587, \"height\": 1018, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2605.24022v1/fig-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 1502, \"height\": 891, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2605.24022v1/fig-007.webp\", \"caption\": \"\", \"page\": 0, \"index\": 7, \"width\": 1780, \"height\": 1151, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2605.24022v1/fig-008.webp\", \"caption\": \"\", \"page\": 0, \"index\": 8, \"width\": 826, \"height\": 724, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2605.24022v1/fig-009.webp\", \"caption\": \"\", \"page\": 0, \"index\": 9, \"width\": 877, \"height\": 315, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2605.24022v1/fig-010.webp\", \"caption\": \"\", \"page\": 0, \"index\": 10, \"width\": 872, \"height\": 362, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2605.24022v1/fig-011.webp\", \"caption\": \"\", \"page\": 0, \"index\": 11, \"width\": 877, \"height\": 367, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2605.24022v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1744, \"height\": 310, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2605.24022v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 819, \"height\": 378, \"label\": \"Table\"}]"
motivation: 非前缀KV缓存复用导致语义退化与异构存储I/O瓶颈，需兼顾语义一致性与硬件效率。
method: 离线频域分析定位关键KV对，在线选择性重计算语义关键token，结合稀疏传输、异步重叠与自适应重计算比例调度。
result: 主流模型上TTFT加速3.72-4.86倍，吞吐提升3.93-6.21倍，质量接近全重计算；SSD/HDD场景仍加速2.34-2.36倍。
conclusion: CacheTune以频率引导与硬件感知的复用策略有效缓解长上下文LLM服务的TTFT瓶颈，同时保障生成质量。
---

## 摘要
在长上下文大语言模型（LLM）推理中，预填充阶段产生的首Token延迟（TTFT）已成为限制交互性能和部署成本的首要瓶颈。KV缓存复用为减少冗余预填充提供了直接途径，但传统前缀缓存仅适用于严格前缀场景；在非前缀设置中直接复用KV缓存会破坏跨块的全局注意力关系，导致生成质量显著下降。当可复用的KV缓存被卸载到GPU外部缓存池时，跨异构硬件层的I/O开销进一步成为新的TTFT瓶颈。因此，高效的非前缀KV缓存复用需要同时实现语义一致性恢复和计算-I/O协同优化。本文提出CacheTune，一种频率引导且硬件感知的长上下文LLM服务KV缓存复用系统。CacheTune首先通过频域分析离线识别对跨注意力恢复最关键的KV对，然后在线仅重新计算这些语义关键token，同时复用其余KV。为了将这种语义选择转化为端到端延迟降低，CacheTune进一步结合稀疏KV传输、多流异步重叠、延迟位置编码恢复以及硬件感知的自适应重计算比例调整，以平衡异构缓存池中计算与数据移动。在主流LLM和长上下文任务上的评估表明，CacheTune实现了3.72倍至4.86倍的TTFT加速和3.93倍至6.21倍的吞吐量提升，同时保持与完全重计算相近的生成质量。即使缓存被卸载到I/O受限的SSD/HDD存储，CacheTune通过自适应重计算仍可保持2.34倍至2.36倍的TTFT加速。

## Abstract
In long-context Large Language Model (LLM) inference, the Time-To-First-Token (TTFT) latency incurred by the prefill stage has become the foremost bottleneck limiting interactive performance and deployment cost. KV Cache reuse offers a direct path to reduce redundant prefill, yet traditional prefix caching applies only to strict-prefix scenarios; directly reusing KV Cache in non-prefix settings breaks the cross-chunk global attention relationships and causes significant degradation in generation quality. When reusable KV Cache is offloaded to GPU-external cache pools, I/O overheads across heterogeneous hardware tiers further emerge as a new TTFT bottleneck. Efficient non-prefix KV Cache reuse therefore requires both semantic-consistency recovery and compute-I/O co-optimization.   This paper presents CacheTune, a frequency-guided and hardware-aware KV Cache reuse system for long-context LLM serving. CacheTune first identifies, offline, the KV pairs most critical to cross-attention recovery through frequency-domain analysis, and then selectively recomputes only these semantic-critical tokens online while reusing the remaining KVs. To turn this semantic selection into end-to-end latency reduction, CacheTune further combines sparse KV transfer, multi-stream asynchronous overlap, deferred positional-encoding recovery, and hardware-aware adaptive recomputation-ratio tuning to balance computation and data movement across heterogeneous cache pools. Evaluations on mainstream LLMs and long-context tasks show that CacheTune achieves 3.72x-4.86x TTFT speedup and 3.93x-6.21x higher throughput while maintaining generation quality close to full recompute. Even when caches are offloaded to I/O-bound SSD/HDD storage, CacheTune sustains 2.34x-2.36x TTFT speedup through adaptive recomputation.

---

## 论文详细总结（自动生成）

## 1. 论文的核心问题与整体含义（研究动机和背景）

- **核心问题**：长上下文大语言模型（LLM）推理中，预填充（Prefill）阶段的首Token延迟（TTFT）成为主要瓶颈。现有KV缓存复用方法仅适用于严格前缀场景，在非前缀（non-prefix）场景下（如RAG、多文档QA），直接复用独立编码的KV块会破坏跨块的全局注意力关系（cross-attention loss），导致生成质量严重下降。此外，当KV缓存被卸载到GPU外部的异构存储（CPU内存、SSD、HDD）时，跨层级的数据传输I/O开销进一步加剧TTFT瓶颈。
- **整体含义**：亟需一种既能恢复非前缀复用中丢失的全局语义、又能协同优化计算与I/O开销的系统，以实现长上下文LLM服务的高效低延迟推理。

## 2. 论文提出的方法论

### 核心思想
- **离线频率引导的选择性重计算**：通过快速傅里叶变换（FFT）将KV缓存从序列域映射到频域，发现能量集中于低频分量；低频KV对承载全局语义信息，因此仅需在线重计算低频主导的token，其余KV直接复用。
- **硬件感知的自适应流水线**：将重计算与缓存传输重叠，并利用硬件profile与轻量校准搜索确定最优重计算比例，平衡计算与I/O。

### 关键技术细节
1. **频率域重要度评分**：
   - 对每层KV张量沿序列维度执行实值FFT（rFFT）：`bK^(ℓ)=rfft(K^(ℓ), dim=0)`，`bV^(ℓ)=rfft(V^(ℓ), dim=0)`。
   - 低通滤波保留最低α比例（默认α=0.5），再通过逆rFFT得到重建的`êK^(ℓ)、êV^(ℓ)`。
   - 每个token的低频能量得分：`s_i^(ℓ) = 0.5*(||êK_i||₂ + ||êV_i||₂)`。
   - 按重计算比例r（如15%）对得分进行TopK选择，得到重计算索引集合`I_freq^(ℓ)`，其余KV被直接复用并被稀疏传输。
2. **KV缓存卸载与稀疏复用流水线**：
   - **传输流**：异步预取下一层未被选中的KV对（`(1-r)N`个）到GPU。
   - **重计算流**：并行重计算`I_freq^(ℓ)`中的KV对，同时应用正确的全局位置（延迟RoPE恢复：存储预RoPE的原始Key，在线用全局位置重新应用RoPE）。
   - **融合**：通过scatter融合将两部分组装成完整KV缓存，通过CUDA Event同步层间依赖，隐蔽传输延迟。
3. **硬件感知自适应重计算比例搜索**：
   - 建立TTFT模型：`T_TTFT(r) ≈ ℓ·max(rN·t_c, (1-r)N·t_i) + ℓ·t_o`，其中t_c为单token重计算开销、t_i为单token有效传输开销、t_o为流水线固定开销，均由一次性硬件profile测量。
   - 解析最优比例：`r₀ = t_i/(t_c + t_i)`，并施加质量下限`r_min=15%`。
   - 使用黄金分割搜索（GSS）在`[r_min, r_max]`内对校准集（如5-10个样本）上的平均TTFT进行优化，得到最终`r*`。该搜索约需3-4分钟，一次部署即可复用。

## 3. 实验设计

- **数据集与场景**（表1）：
  - SAMSum（多轮对话摘要，指标ROUGE-L）
  - MuSiQue、WikiMQA、HotpotQA（多跳QA，指标F1）
  - Multi-News（多文档摘要，指标ROUGE-L）
  上下文由多个独立文档块/历史片段拼接而成，模拟RAG等非前缀场景。
- **对比方法**（共7种）：
  - Full Recompute（上界）、Full KV Reuse（下界）
  - vLLM Prefix Caching（仅前缀复用）
  - CacheBlend（首层全重计算+后续按HKVD选择15%重计算）
  - EPIC（仅重计算每块前k个attention-sink位置，k=16）
  - CacheSlide（基于RPDC的加权校正注意力）
  - KVLink（插入可学习link token，需预训练）
- **模型**：Mistral v0.3-7B、Llama 3-8B、Qwen 2.5-32B。

## 4. 资源与算力

- **硬件平台**：
  - A100平台：2×NVIDIA A100-40GB，PCIe Gen3 ×16；HDD缓存池（有效读写约205/201 MB/s）。
  - RTX 4090平台：2×NVIDIA RTX 4090-24GB，PCIe Gen3 ×16；SSD缓存池（有效读写约535/445 MB/s）。
- **算力细节**：
  - 离线频率分析极轻量：对3K token的KV块，生成重计算索引平均仅需0.69 ms（GPU）。
  - 硬件校准搜索需3-4分钟（10个样本）完成一次部署。
- **未明确项**：论文未说明模型训练或预训练所需的算力（如KVLink需额外预训练，但CacheTune本身无需训练）。

## 5. 实验数量与充分性

- **主要实验组**：
  1. **精度-TTFT权衡**（图7）：3模型 × 5数据集 × 8方法 = 约120个数据点，展示散点图。
  2. **吞吐量随请求率变化**（图8）：3模型 × 3数据集 × 多种请求率，展示TTFT曲线。
  3. **重计算比例r影响**（图9）：Mistral模型在不同r（5%-25%）下的精度与TTFT速度比。
  4. **消融研究**：
     - 频率选择机制有效性（图10）：对比随机选择、高频选择、低频选择（r=15%）。
     - 存储层与硬件自适应分析（图11、表2）：GPU/CPU/SSD/HDD下不同策略的TTFT，自适应r vs 固定15%。
- **充分性评价**：
  - 实验覆盖3种模型尺度（7B/8B/32B）、5种任务类型、7种对比方法，消融验证了核心设计。
  - 对比方法包含最先进系统（CacheBlend、EPIC、CacheSlide、KVLink），且在同一硬件条件下比较。
  - 局限：未与更大模型（如70B+）或更多缓存管理方案（如CacheGen）对比，但已具代表性。

## 6. 论文的主要结论与发现

- **性能提升**：
  - 相比Full Recompute，CacheTune实现3.72×–4.86× TTFT加速，3.93×–6.21×吞吐量提升。
  - 平均保留94.8%的生成质量（接近全重计算）。
- **对比最先进方法**：
  - 在相同r=15%下，CacheTune精度和延迟均优于CacheBlend；优于EPIC、KVLink（后者精度损失较大）。
  - 在CPU/SSD/HDD缓存下，CacheTune均保持最低TTFT，且通过自适应r在慢速存储上仍获得2.34×–2.36×加速。
- **机制验证**：
  - 低频选择显著优于随机和高频选择，证明低频KV对全局语义恢复关键。
  - 硬件自适应搜索（GSS）有效找到最优r（如HDD下36.4%，SSD下30.9%），避免I/O瓶颈。

## 7. 优点

- **方法创新**：
  - 首次将频域分析引入非前缀KV缓存复用，通过识别低频能量集中点实现无需训练、模型无关的token重要度建模。
  - 将重计算索引同时用于语义恢复和I/O调度，实现“双重利用”。
- **系统设计全面**：
  - 稀疏传输+多流异步重叠+延迟RoPE恢复，有效隐藏CPU/GPU及跨层传输开销。
  - 硬件感知自适应比例搜索（Roofline+Golden Section），无需手动调参。
- **实验充分**：覆盖多种模型、任务、存储层级，对比方法全面，消融实验理性。

## 8. 不足与局限

- **I/O极端场景**：当外部存储带宽极低或异步重叠窗口不足时，系统仍可能受I/O限制（论文自身提及）。
- **校准代价**：虽仅需5-10个样本和3-4分钟，但每次部署新硬件/存储平台仍需重新执行，可能对快速迭代不友好。
- **模型覆盖**：仅评估7B-32B模型，未验证更大模型（如70B或MoE架构）下的扩展性。
- **对比方法范围**：未与专门针对前缀复用或混合缓存策略的系统（如CacheGen、PromptCache）进行详细比较。
- **辅助假设**：RoPE层需存储预RoPE的原始Key，增加GPU显存占用（虽通过异步流水缓解）。
- **非通用性**：严格前缀场景下可能不如专用前缀缓存高效；高度依赖离线预计算索引（但成本很低）。

（完）
