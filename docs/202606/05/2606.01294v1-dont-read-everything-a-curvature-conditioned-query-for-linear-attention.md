---
title: "Don't Read Everything: A Curvature-Conditioned Query for Linear Attention"
title_zh: 不要读完所有内容：一种用于线性注意力的曲率条件查询
authors: "Dong Le, Thong Nguyen, Cong-Duy Nguyen, Anh Tuan Luu"
date: 2026-05-31
pdf: "https://arxiv.org/pdf/2606.01294v1"
tags: ["query:sparse-attn"]
score: 8.0
evidence: 基于曲率条件的查询改进线性注意力，提升检索和长上下文任务
tldr: 线性注意力读取时所有过去键加性贡献，导致有用目标被稀释。本文提出曲率条件查询(CCQ)，利用softmax二阶泰勒展开的曲率矩阵构造查询收缩算子，沿记忆高密度方向压缩后读取。CCQ仅修改读取步骤，兼容GLA等线性注意力。实验表明，在困惑度、零样本准确率、S-NIAH检索、长度外推至20K及LongBench上均有显著提升，额外成本极小。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.01294v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 752, \"height\": 303, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.01294v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 796, \"height\": 347, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.01294v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 793, \"height\": 487, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.01294v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 792, \"height\": 490, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.01294v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1456, \"height\": 2370, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.01294v1/fig-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 1440, \"height\": 1821, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.01294v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 774, \"height\": 632, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.01294v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1661, \"height\": 722, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.01294v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1662, \"height\": 569, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.01294v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1662, \"height\": 436, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.01294v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 857, \"height\": 304, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.01294v1/table-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 602, \"height\": 468, \"label\": \"Table\"}]"
motivation: 线性注意力读取步骤未优化，有用目标被大量存储向量稀释，导致长上下文和检索任务性能差。
method: 基于softmax log-partition二阶泰勒展开的曲率（键协方差）构建线性算子，沿高密度方向收缩查询后再读取状态。
result: 在GLA和Gated DeltaNet上，困惑度、零样本准确率、S-NIAH检索、长度外推至20K及LongBench准确率均提升。
conclusion: CCQ以极小额外成本显著增强线性注意力长上下文能力，且可组合现有线性注意力模型。
---

## 摘要
线性注意力通过维持一个循环快速权重状态，降低了softmax注意力的二次成本，但在上下文检索和长上下文任务中始终落后。现有的补救措施通过门控、增量更新或核特征映射作用于内存的写入侧，但读取步骤保持不变：每个过去的键都对输出有加性贡献，因此有用的目标被大量存储向量稀释。我们借用softmax几何的一个特定部分来构造一个廉价的读取时查询收缩。在等向性注意力点对softmax对数配分函数进行二阶泰勒展开，得到一个局部二次模型，其曲率与运行键协方差一致，该量可以通过与线性注意力状态相同的循环/分块机制来维护。相关的线性算子沿着内存的高密度方向收缩查询，然后再读取状态。我们将这种机制称为曲率条件查询（CCQ）。CCQ仅修改读取步骤，并且可以与任何线性注意力骨干组合使用。将其附加到GLA和Gated DeltaNet上，它能在训练上下文内及之外提高困惑度、零样本下游精度、S-NIAH检索、从4K到20K的长度外推困惑度以及LongBench精度，且额外成本很小。

## Abstract
Linear attention reduces the quadratic cost of softmax attention by maintaining a recurrent fast-weight state, but it consistently lags on in-context retrieval and long-context tasks. Existing remedies act on the write side of memory through gating, delta updates, or kernel feature maps, but the read step is left unchanged: every past key contributes additively to the output, so useful targets are diluted by the bulk of stored vectors. We borrow one specific piece of softmax's geometry to construct a cheap read-time contraction of the query. A second-order Taylor expansion of the softmax log-partition at the isotropic-attention point gives a local quadratic model whose curvature coincides with the running key covariance, a quantity that can be maintained with the same recurrent/chunkwise mechanism as the linear-attention state. The associated linear operator contracts the query along the high-density directions of memory before it reads the state. We call this mechanism Curvature-Conditioned Query (CCQ). CCQ modifies only the read step and is composable with any linear-attention backbone. Attached to GLA and Gated DeltaNet, it improves perplexity, zero-shot downstream accuracy, S-NIAH retrieval at and beyond the training context, length-extrapolation perplexity from 4K to 20K, and LongBench accuracy, at small extra cost.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义（研究动机和背景）

- **核心问题**：线性注意力（Linear Attention）通过循环快速权重状态将复杂度从二次降为线性，但在上下文检索与长上下文任务上始终落后于标准 softmax 注意力。已有改进集中在“写侧”（如门控、增量更新、核特征映射），而“读侧”保持不变：每个过去键加性贡献输出，有用目标被大量存储向量稀释。
- **整体含义**：本文指出软注意力缺失的竞争机制（对数配分函数的抑制效应）是关键瓶颈，并提出仅修改读侧即可弥补这一差距，在不改变现有写规则的条件下提升线性注意力的检索与长上下文能力。

### 2. 论文提出的方法论：核心思想、关键技术细节

- **核心思想**：借用 softmax 对数配分函数的局部曲率（在等向性注意力点 q=0 处的 Hessian 等于运行键协方差 Σ_t），构建一个廉价的读时线性算子 `I - λ_t Σ_t`，沿内存高密度方向收缩查询，使清理后的查询 `q_clean` 优先匹配孤立目标。
- **技术细节**：
  - 二阶泰勒展开：`At(q) ≈ log t + μ_t^T q + ½ q^T Σ_t q`，其中 `Σ_t = Cov≤t[k]`。
  - 清洁算子：`q_clean = (I - λ_t Σ_t) q_t`，`λ_t = σ(W_λ q_t + b_λ) ∈ (0,1)` 为查询门控。
  - 运行协方差 Σ_t 可通过与骨干状态相同的循环/分块机制维护（额外维护一个 d_k×d_k 矩阵和一个 d_k 向量）。
  - 读操作不变：`o_t = S_t q_clean`；仅修改查询，不修改写规则。
- **算法流程**（文字说明）：
  - 训练阶段：分块扫描，每个块内计算键的二阶矩和均值前缀和，再用它们构造清洁查询，最后送入骨干注意力的分块更新。
  - 推理阶段：每个时间步维护 (S_t, C̄_t, μ_t) 三个状态，用当前键更新协方差和均值，计算清洁查询，再通过骨干更新状态并输出。

### 3. 实验设计：数据集、基准与对比方法

- **数据集**：
  - 预训练语料：FineWeb-Edu 子集。
  - 评测：lm-evaluation-harness（8个零样本任务 + LAMBADA、WikiText）；S-NIAH 检索（pass-key、number、uuid，长度 1K/2K/4K/8K）；6个长上下文语料（GovReport、QMSum、NarrativeQA、Qasper、PG19、CodeParrot）做长度外推 PPL（4K→20K）；LongBench 14个英文任务。
- **基准**：Llama 风格 Transformer、Mamba2、GLA、GLA-Hedgehog、Gated DeltaNet。
- **对比方法**：CCQ-GLA、CCQ-Gated DeltaNet（仅读侧修改，其余相同）。
- **模型规模**：500M 参数（15B tokens 训练）和 1.3B 参数（40B tokens 训练）。

### 4. 资源与算力

- 论文明确说明了硬件配置：单节点 8× NVIDIA H200 GPU，使用 FSDP + bfloat16 混合精度。训练时全局 batch size 500M 模型为 262,144 tokens/步，1.3B 模型为 524,288 tokens/步。未给出具体训练时长（小时数），但提到 CCQ 额外开销约 1–2 ms/层（1.3B 层形状下），端到端吞吐损失较小。

### 5. 实验数量与充分性

- 实验数量：大量实验覆盖了语言建模困惑度、零样本推理、合成检索（4个长度×3种任务）、长度外推（6个数据集×9个长度点）、长上下文理解（14个任务）。此外还有关于键对齐假设的诊断实验（附录 H）和消融式分析（附录 G 的检索边际命题）。
- 充分性：实验设计较为全面，包含不同规模（500M、1.3B）、不同骨干（GLA、Gated DeltaNet）、不同评测维度。但缺乏更大规模（7B+）的实验，也未在 Mamba2 或 RWKV 等选择性状态空间模型上测试；过长上下文（>20K）未覆盖；训练 token 量较小（40B），可能不足以完全体现收益。

### 6. 论文的主要结论与发现

- CCQ 以极小额外成本（每层 1–2 ms）显著改善线性注意力在检索和长上下文任务上的表现。
- 在 500M 和 1.3B 规模下，CCQ-GLA 和 CCQ-Gated DeltaNet 在 wiki text 困惑度、零样本平均准确率、S-NIAH 检索（尤其 4K/8K 外推）、长度外推 PPL 以及 LongBench 平均分上均一致优于各自骨干。
- 诊断实验证实了假设：检索相关键位于高方差方向之外的子空间，而干扰键聚集在高方差方向内（对齐差异为负且稳定）。

### 7. 优点

- **创新性**：首次明确区分“读侧”与“写侧”改进，仅修改读侧即可获显著提升，思路新颖且与现有写侧方法正交。
- **理论依据**：从 softmax 对数配分函数的局部曲率出发，推导出清理算子，具有良好的可解释性。
- **实用性**：计算开销极低（仅需额外运行统计量），易于集成到现有线性注意力骨干中（实验显示直接在 GLA 和 Gated DeltaNet 上加装即可）。
- **稳定性**：设计了 sigmoid 门控和归一化，确保算子谱在 (0,2) 内，训练稳定。
- **实验全面**：覆盖语言建模、零样本、检索、长度外推、长上下文理解等多个维度，且包括诊断性实验验证假设。

### 8. 不足与局限

- **规模限制**：仅评测了 500M 和 1.3B 参数、至多 40B tokens，未在 7B+ 或更大模型上验证，泛化性存疑。
- **骨干覆盖**：仅在 GLA 和 Gated DeltaNet 上测试，未集成到 Mamba2、RWKV 或 xLSTM 等流行线性 RNN 中；未与更复杂的读侧改进（如 TTT、Titans）比较。
- **训练成本**：训练 token 量相对较小（40B），可能不足以使 CCQ 的收益完全收敛；更长时间训练或更大数据规模下的表现未报告。
- **协方差近似**：使用未加权的运行协方差，忽略了 softmax 的查询相关重加权，可能在高选择性场景下不准确。
- **与衰减交互**：对于含遗忘门或衰减的骨干，使用无衰减的协方差可能过度惩罚已遗忘方向，论文未深入探讨这一点。
- **评测偏差**：长上下文评测仅针对英文 LongBench，缺少多语言或更长上下文（>20K）的实验；合成检索仅 S-NIAH 三种，未覆盖更复杂的 RULER 任务或多跳检索。
- **可重复性**：未提供开源代码或预训练模型，仅描述了算法。

（完）
