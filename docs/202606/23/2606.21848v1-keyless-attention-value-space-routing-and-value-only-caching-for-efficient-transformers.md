---
title: "Keyless Attention: Value-Space Routing and Value-Only Caching for Efficient Transformers"
title_zh: 无键注意力：值空间路由与仅值缓存实现高效Transformer
authors: Xin Gao
date: 2026-06-20
pdf: "https://arxiv.org/pdf/2606.21848v1"
tags: ["query:sparse-attn"]
score: 9.0
evidence: "提出无键注意力与仅值缓存，KV缓存内存减少50%"
tldr: "标准Transformer的KV缓存占用了大量内存和带宽。本文提出Keyless Attention，完全消除key投影，仅使用query和value，实现Value-Only缓存，减少50%缓存开销。该方法基于深度-m注意力分解框架，在m=3时通过值空间路由矩阵匹配标准注意力的参数量。实验表明，在GPT-2等5个模型上，Keyless Attention在困惑度和下游任务上匹配或优于标准注意力，同时大幅提升效率。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.21848v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1024, \"height\": 413, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.21848v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1028, \"height\": 622, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.21848v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1706, \"height\": 287, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.21848v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1697, \"height\": 683, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.21848v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1369, \"height\": 267, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.21848v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1448, \"height\": 281, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.21848v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 817, \"height\": 537, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.21848v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1033, \"height\": 419, \"label\": \"Table\"}]"
motivation: 标准注意力中KV缓存占用大量内存，限制了Transformer的推理效率。
method: 移除key投影，使用值空间路由矩阵连接query和value，实现Value-Only缓存及深度-3注意力分解。
result: "KV缓存减少50%，在4/5模型上困惑度持平或更优，零样本常识推理中4/5基准领先。"
conclusion: Keyless Attention是一种高效且性能无损的注意力变体，可替代标准注意力用于部署。
---

## 摘要
我们提出了一种无键注意力机制，完全消除了键投影，仅基于查询和值进行操作。这产生了仅值缓存，与标准注意力相比，KV缓存内存和访问开销减少了50%，同时匹配或超越了标准注意力的解码吞吐量。除了效率提升，我们引入了深度m注意力分解：标准注意力计算注意力双线性形式的深度2分解，而无键注意力实现了该系列的深度m实例。当m=3时，无键注意力通过值空间路由矩阵匹配了标准注意力的投影矩阵数量，该矩阵取代了键投影并引入了路由与检索之间的耦合。在五个模型和四种架构（GPT-2 280M、GPT-2 557M、Pythia 410M、Qwen2 1.5B和Llama 3.2 1B）上的实验表明，无键注意力在5个模型中的4个其困惑度匹配或优于标准QKV注意力。在下游零样本评估（GPT-2 557M）中，无键注意力在5个常识推理基准中的4个上表现更优，同时实现了全程50%的KV缓存减少。

## Abstract
We propose Keyless Attention, an attention mechanism that eliminates the key projection entirely, operating over queries and values only. This yields a Value-Only Cache that reduces KV cache memory and access overhead by exactly 50% over standard attention, while matching or exceeding standard attention's decode throughput. Beyond efficiency, we introduce Depth-$m$ Attention Factorization: standard attention computes a depth-2 factorization of the attention bilinear form, while Keyless Attention realizes a depth-$m$ instance of this family. At m=3, Keyless Attention matches the projection matrix count of standard attention via a value-space routing matrix that replaces the key projection and introduces a coupling between routing and retrieval. Experiments across five models and four architectures (GPT-2 280M, GPT-2 557M, Pythia 410M, Qwen2 1.5B, and Llama 3.2 1B) show that Keyless Attention matches or outperforms standard QKV attention on perplexity in 4 out of 5 models. On downstream zero-shot evaluation (GPT-2 557M), Keyless Attention outperforms on 4 out of 5 commonsense reasoning benchmarks, while achieving 50% KV cache reduction throughout.

---

## 论文详细总结（自动生成）

好的，作为资深学术论文分析助手，我将基于您提供的论文内容，生成以下结构化、深入的总结。

### 论文总结：无键注意力

#### 1. 核心问题与整体含义（研究动机和背景）

-   **核心问题**：标准Transformer架构中的KV（Key-Value）缓存是长上下文推理的主要瓶颈。KV缓存的大小随序列长度、批大小、层数和注意力头数线性增长，消耗了大量内存和带宽，尤其是在大规模模型部署中。
-   **整体含义**：本文挑战了标准注意力机制中“查询-键-值”（QKV）投影的必要性。通过理论分析和实验证明，完全移除关键部分在架构上是可行的。这为设计更高效的Transformer架构提供了新范式，直接解决了KV缓存冗余的根本原因，而非事后压缩或管理。

#### 2. 方法论：核心思想、关键技术细节

-   **核心思想**：消除注意力机制中的Key投影，将查询和值（Value）直接结合，从而在推理时只需缓存Value，即可实现50%的内存节省。
-   **关键技术细节**：
    -   **无键注意力机制 (Keyless Attention)**：
        -   公式：`Attention(Q, V) = softmax(QV^T / sqrt(d_k)) V`
        -   操作：注意力得分直接在查询和值之间计算，不再涉及键的计算和存储。
    -   **仅值缓存 (Value-Only Cache)**：
        -   在自回归推理中，只缓存先前生成的Value表示（`v_i`），不再缓存Key。
        -   内存开销比标准KD缓存减少50%。
    -   **深度m注意力分解 (Depth-m Attention Factorization)**：
        -   提出了一个通用框架，将注意力机制中的参数化形式（`Ω`）视为深度为`m`的矩阵分解过程。
        -   **`m=2`**：对应标准QKV注意力（`Ω = W_Q (W_K)^T`）。
        -   **`m=3`** (QVV(3)）：本文方法，使用三个矩阵：`W_Q1`、`W_Q2`、`W_V`。查询通过两次投影得到（`Q = X W_Q1 W_Q2`），`Ω = W_Q1 W_Q2 (W_V)^T`。参数量与标准QKV相同。
        -   **值空间路由 (Value-Space Routing)**：注意力得分计算中的路由矩阵（`W_Q2`）作用于查询，使其与值的表示空间对齐。这引入了路由与检索之间的强耦合（梯度纠缠），作为隐式正则化器，减少过拟合。
    -   **理论等价性**：证明了在特定条件下（如单头且初始化矩阵满足秩条件），无键注意力可以与标准QKV注意力完全等价。

#### 3. 实验设计：数据集、Benchmark和对比方法

-   **数据集**：
    -   **语言建模**：主要在Wikipedia-103数据集上进行训练和验证（使用30M token的子集）。
    -   **下游任务**：零样本文本推理，包括 HellaSwag、ARC-Challenge、StoryCloze、SciQ、BoolQ。
-   **Benchmark / 对比方法**：
    -   **主要基线**：标准QKV注意力（QKV）。
    -   **对比模型架构**：
        -   GPT-2 (280M, 557M)
        -   Pythia 410M
        -   Qwen2 1.5B
        -   Llama 3.2 1B
    -   **其他方法**：在消融实验中，对比了QVV(2), QVV(4) 和 QKV(3) 等变体。

#### 4. 资源与算力

-   **文中明确说明**：所有实验均在单个NVIDIA A100-SXM4-80GB GPU上完成。
-   **未说明部分**：论文未提及单次训练的具体时长（如小时数或天数），也未提及是否使用了多卡并行训练。

#### 5. 实验数量与充分性

-   **实验数量与广度**：
    -   **模型数量**：5个不同规模、不同架构的模型，覆盖了从280M到1.5B的参数范围，以及不同的位置编码（RoPE、Absolute）和注意力模式（MHA、GQA）。
    -   **任务类型**：1个语言建模任务（困惑度）+ 5个零样本推理任务。
    -   **消融实验**：研究了分解深度 `m` 的不同取值（2, 3, 4）对性能的影响。
    -   **效率分析**：通过基准测试验证了解码吞吐量和KV缓存大小。
-   **充分性与公平性**：
    -   **充分**：实验设计较为系统，覆盖了主流架构、多种规模，并进行了消融分析。对比组的训练条件（数据集、优化器、学习率调度）均保持一致，仅改变注意力机制，确保了比较的公平性。
    -   **客观性**：GPT-2实验报告了3个不同随机种子的均值和标准差，显示了结果的稳定性。其他实验也报告了标准差，增加了结论的可靠性。

#### 6. 主要结论与发现

-   **性能匹配或超越**：无键注意力在5个模型中的4个上，其验证集困惑度匹配或优于标准QKV注意力；在5个零样本基准中的4个上表现更优。
-   **效率显著提升**：在所有模型和实验中，KV缓存内存占用精确减少50%。解码吞吐量也略有提升（如文中Qwen2测试）。
-   **更好的泛化性**：在所有实验中，无键注意力在最佳Epoch后的验证损失退化速度普遍慢于标准注意力，表明其具有更强的抗过拟合能力。作者归因于“值空间路由”带来的梯度纠缠作为隐式正则化器。
-   **鲁棒性强**：在Qwen2和Llama 3.2等更大、更现代的模型上，无键注意力同样展现了优势，表明该方法具有良好的跨架构泛化能力。

#### 7. 优点

-   **方法简洁且理论扎实**：出发点直接、思路清晰（“不存Key”），并且从理论上证明了与标准注意力的等价性，为方法的有效性提供了坚实基础。
-   **立竿见影的效率提升**：直接通过架构设计实现50%的KV缓存减少，无需量化、剪枝或系统级技巧，效果确定且可预测。
-   **性能无损甚至是正面**：经验证据表明，移除Key投影不仅没有显著损害模型质量，反而在一些指标上带来了提升，特别是在泛化性上。
-   **兼容性强**：Value-Only Cache可以与MQA、GQA、MLA等现有的缓存优化方法正交结合，实现复合收益。
-   **实验覆盖全面**：在多个主流模型库（GPT-2, Pythia, Qwen2, Llama）和多种配置（GQA, MHA, RoPE等）上进行了验证，结论具有较强的普适性。

#### 8. 不足与局限

-   **实验规模**：实验主要在1.5B参数以下的模型上进行，未在更大规模（如7B, 13B, 70B）或更长的上下文长度（如128K, 1M）上验证。作者在“Limitations”中承认了这一点。
-   **训练数据**：所有模型仅在一个数据集（Wikipedia-103的子集）上进行预训练，未在多任务或多领域数据上评估。其在大规模、多样化预训练数据上的表现尚不明确。
-   **理论分析不充分**：作者虽提出“梯度纠缠”作为抗过拟合的解释，但“由于缺少直接机制分析，我们将此留待未来工作”，目前仍是一种假设，缺乏因果验证。
-   **优化代价**：无键注意力的训练收敛速度在初期（第一个Epoch）有时慢于标准注意力（如Qwen2实验），虽然最终结果更好，但在计算预算严格的场景下可能需要更长训练时间。
-   **潜在的应用限制**：虽然兼容GQA，但对于某些需要严格数值稳定性的场景（如混合精度训练），移除Key投影可能引入新的数值敏感性。

（完）
