---
title: Recency/Frequency Adaptive KV Caching for Large Language Model Serving
title_zh: 面向大型语言模型服务的近期/频率自适应KV缓存
authors: "Yang Shen, Meghana Madhyastha, Robert Underwood, Bogdan Nicolae, Randal Burns"
date: 2026-06-19
pdf: "https://arxiv.org/pdf/2606.21238v1"
tags: ["query:sparse-attn"]
score: 9.0
evidence: 基于近期和频率的自适应KV缓存用于LLM服务
tldr: "大模型推理中KV缓存加速显著，但现有LRU策略易使不同工作负载相互冲刷缓存，降低命中率。本文提出自适应缓存机制，根据访问的近期性和频繁性动态分配缓存空间。在合成文档QA任务上，缓存命中率提升10.8%，首token延迟降低12.6%；实际对话任务提升2.1%和2.0%。该方法支持批量推理，具有良好可解释性，能有效适应多样化工作负载。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.21238v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1762, \"height\": 873, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.21238v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 859, \"height\": 243, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.21238v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1655, \"height\": 523, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.21238v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1654, \"height\": 524, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.21238v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1657, \"height\": 525, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.21238v1/fig-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 855, \"height\": 502, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.21238v1/fig-007.webp\", \"caption\": \"\", \"page\": 0, \"index\": 7, \"width\": 1771, \"height\": 618, \"label\": \"Figure\"}]"
motivation: 现有LRU缓存策略导致不同推理工作负载相互冲刷，缓存利用率低，影响性能。
method: 提出自适应KV缓存，动态区分近期和频繁出现的块，智能分配缓存空间。
result: "合成文档QA命中率+10.8%，首token延迟-12.6%；对话任务各+2.1%和-2.0%。"
conclusion: 自适应缓存有效提升命中率和响应速度，可解释性强，适应多样化负载。
---

## 摘要
键值（KV）缓存是加速大型语言模型推理和生成的一种强大技术。推理工作负载大且多样化，使得有效缓存变得困难。现有的缓存管理策略采用最近最少使用策略来驱逐缓存块。然而，LRU会导致多个不相关的工作负载相互刷新对方的缓存。为了解决这个问题，我们集成了自适应缓存，在近期和频繁出现的KV块之间动态分配缓存空间。评估表明，在合成文档问答工作负载上，与朴素的vLLM相比，它使KV缓存命中率提高了最多10.8%，首Token生成时间降低了最多12.6%；在真实对话工作负载上，分别提高了2.1%和2.0%。该方法很好地泛化到批量推理，并表现出清晰的可解释性，同时有效适应多样化的工作负载。

## Abstract
Key-value (KV) caching is a powerful technique for accelerating large language model inference and generation. Inference workloads are large and diverse, which makes them difficult to cache effectively. Existing cache management strategies adopt the least-recently-used policy for evicting cache blocks. However, LRU leads to multiple unrelated workloads flushing each other's caches. To address this, we integrate adaptive caching that dynamically allocates cache space between recently and frequently occurring KV blocks. Evaluations show that it improves the KV cache hit rate by up to 10.8% and reduces time to first token by up to 12.6% over naive vLLM on synthetic document question answering workloads, and 2.1% and 2.0% respectively on real-world conversation workloads. The method generalizes well to batch inference and demonstrates clear interpretability while effectively accommodating diverse workloads.

---

## 论文详细总结（自动生成）

# 论文详细中文总结

## 1. 论文的核心问题与整体含义（研究动机和背景）
- **核心问题**：现有LLM推理服务中的KV缓存管理主要采用最近最少使用（LRU）策略，导致不同推理工作负载（如文档问答、多轮对话）之间相互冲刷缓存，缓存命中率低，首Token生成时间（TTFT）增加。
- **研究动机**：实际工作负载存在混合的近期性和频繁性访问模式（如热点文档、活跃用户会话），单纯基于近期性的LRU无法有效捕捉频繁复用模式，尤其在缓存空间有限时问题突出。
- **整体含义**：通过自适应地平衡近期和频繁访问块的缓存分配，可以显著提高缓存命中率和服务性能，且方法易于集成到现有系统中。

## 2. 论文提出的方法论：核心思想、关键技术细节
- **核心思想**：将操作系统的自适应缓存替换算法ARC（Adaptive Replacement Cache）引入LLM服务框架vLLM的KV缓存管理。
- **关键技术细节**：
  - **双队列结构**：缓存分为低频队列L1（T1物理缓存+B1幽灵缓存）和高频队列L2（T2物理缓存+B2幽灵缓存）。请求命中时，块从L1提升到L2。
  - **动态缓存分区**：通过幽灵缓存（仅存元数据）的命中反馈信号动态调整T1和T2的物理空间比例。若幽灵缓存B1命中，增加T1配额；B2命中则增加T2配额。
  - **驱逐决策**：当缓存满时，根据调整后的队列长度驱逐相应队列的LRU块，并将元数据压入对应的幽灵缓存。
  - **参数无关**：不需要手动调整参数，自动适应工作负载变化。
  - **集成方式**：以KV块（固定16 tokens）为粒度，与vLLM的PagedAttention机制无缝结合。

## 3. 实验设计：数据集、基准、对比方法
- **数据集/场景**：
  1. **合成文档QA负载**：使用QuALITY（381篇长文档+761个问题）和WikiQA（3047个问题-段落对），通过滑动窗口Zipf采样生成3072个请求（窗口大小512，指数1.0），模拟热点变化。
  2. **真实多轮对话负载**：使用Qwen-Bailian匿名数据集Trace A，包含两小时43058个请求，平均输入长度约2300 tokens，前20000个请求用于热身。
- **基准**：vLLM默认的LRU缓存替换策略。
- **对比方法**：
  - **LRU**（基线）
  - **DBL**：固定1:1比例的最近/频繁双队列（用于分离自适应和双队列贡献）
  - **ARC**（本文方法）

## 4. 资源与算力
- **模型**：Qwen3 14B参数（代表性大小）。
- **GPU**：单块NVIDIA H100 PCIe GPU，80GB显存（运行在Lambda GPU Cloud）。
- **推理配置**：离线推理模式，批大小固定为1（除批量实验外），每个请求仅生成1个token以消除解码变异性，KV块大小为16 tokens，前缀缓存开启。
- **未明确说明**：训练时长/能耗未提及（因为方法本身无需训练，仅需元数据更新）。

## 5. 实验数量与充分性
- **实验组数**：
  - 文档QA：在QuALITY和WikiQA上比较LRU、DBL、ARC在不同缓存大小（图中多个大小点）下的命中率和TTFT（图3、4）。
  - 对话负载：同样比较三种策略在不同缓存大小下的命中率和TTFT（图5）。
  - 批量推理：在QuALITY上固定缓存大小20k tokens，比较不同批大小（1,2,4,8）下的性能（图6）。
  - 动态分区分析：展示QuALITY和对话负载下T1比例随时间演化（图7），解释自适应效果。
- **充分性评价**：
  - **优点**：覆盖了两种截然不同的工作负载（合成文档QA和真实对话），包含不同缓存大小、不同批大小，以及分离双队列贡献的消融实验（DBL vs ARC），实验设计较全面。
  - **客观性**：使用公开数据集和标准基准（vLLM），控制变量（统一模型、GPU、块大小），结果可复现。
  - **不足**：仅使用一个模型（Qwen3 14B），未验证其他规模或架构的模型；仅测试离线推理模式，未涉及在线推理、分布式或多GPU场景；合成负载使用Zipf分布，可能不完全反映真实分布。

## 6. 论文的主要结论与发现
- **ARC显著提升缓存命中率和TTFT**：
  - 文档QA：命中率提升1.2%~10.8%，TTFT降低2.0%~12.6%（小缓存效果更明显）。
  - 对话负载：命中率提升1.1%~2.1%，TTFT降低1.0%~2.0%（大缓存时DBL退化，ARC仍保持优势）。
- **自适应分区的贡献**：固定双队列（DBL）在某些缓存大小下性能甚至差于LRU，而ARC通过动态调整维持优势（图5大缓存区域）。
- **批量推理友好**：ARC在不同批大小下保持稳定优势（TTFT降低5%~7%）。
- **可解释性**：通过T1比例演化可直观反映工作负载的近期/频繁特性变化（如Zipf窗口切换、对话缓存大小选择）。

## 7. 优点：方法或实验设计上的亮点
- **方法层面**：
  - 将经典操作系统缓存算法ARC成功迁移到LLM推理场景，原理清晰且无需训练参数。
  - 天然抵抗缓存冲刷（工作负载切换时保留频繁块）。
  - 与现有vLLM架构无缝集成（KV块级别）。
- **实验层面**：
  - 分离双队列与自适应分量的贡献（DBL对比实验）。
  - 在真实生产轨迹（Trace A）上验证，增加了结果的可信度。
  - 分析了缓存大小和批大小的影响，覆盖主要性能维度。

## 8. 不足与局限
- **实验覆盖有限**：
  - 仅使用一个模型（Qwen3 14B）和一个GPU，未验证跨模型、跨规模（如7B、70B）的泛化性。
  - 未在在线推理、前解码分离架构、CPU/磁盘分层缓存等实际生产场景中测试。
  - 合成文档QA负载使用Zipf分布模拟热点，但真实场景可能更复杂。
- **性能提升幅度**：在真实对话负载上提升较小（2%左右），可能不足以驱动大规模部署改进。
- **偏差风险**：仅与LRU和固定双队列对比，未与其他先进方法（如学习型缓存、FLOP感知驱逐）比较。
- **应用限制**：ARC需要维护幽灵缓存元数据，可能引入额外内存开销；且对于严格实时性场景，额外的元数据操作可能影响延迟。
- **未讨论**：如何与请求路由调度（如基于缓存的调度）协同优化；在分布式环境下cache一致性等问题未涉及。

（完）
