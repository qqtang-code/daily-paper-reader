---
title: "MiniPIC: Flexible Position-Independent Caching in <100LOC"
title_zh: MiniPIC：在不到100行代码中实现灵活的位置无关缓存
authors: "Nathan Ordonez, Thomas Parnell"
date: 2026-06-11
pdf: "https://arxiv.org/pdf/2606.13126v1"
tags: ["query:sparse-attn"]
score: 8.0
evidence: 面向LLM推理的位置无关KV缓存
tldr: "检索增强和代理工作负载中，大量重复可预测结构化输入（如文档、代码）需要频繁预填充，但现有前缀缓存（如vLLM）无法重用非相同前缀的KV条目，而位置无关缓存（PIC）实现常需大量代码修改或引入设备间传输开销。MiniPIC以不到100行核心引擎代码加自定义注意力后端，通过无旋转位置编码的KV缓存和用户控制原语（块对齐填充、跨度分隔符、提示依赖），支持多种PIC方法（如Block-Attention、EPIC、Prompt Cache）在同一vLLM实例中共存。在2WikiMultihopQA上，交错调度使预填充吞吐量提升49%，缓存跨度首令牌延迟降低两个数量级，未缓存跨度保持线性缩放，最坏开销仅5.7%。该设计轻量灵活，原生集成KV缓存CPU卸载，显著提升缓存效率。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.13126v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1352, \"height\": 442, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.13126v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1435, \"height\": 398, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.13126v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1049, \"height\": 387, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.13126v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1156, \"height\": 604, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.13126v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 873, \"height\": 313, \"label\": \"Table\"}]"
motivation: vLLM等引擎的前缀缓存无法重用非完全相同前缀的KV条目，而现有PIC实现需大量服务器代码修改或保留KV状态在设备外导致传输开销。
method: MiniPIC通过无旋转位置编码的KV缓存和用户控制原语（块对齐填充、SSep、PDep）实现灵活PIC，以不到100行核心代码加自定义注意力后端支持多种PIC方法。
result: "在2WikiMultihopQA上，预填充吞吐量提升49%，缓存跨度首令牌时延降低两个数量级，未缓存跨度保持线性扩展，最坏开销仅5.7%。"
conclusion: MiniPIC以极简设计实现高效位置无关缓存，提升预填充吞吐并降低时延，开销可控，易于集成到生产级推理引擎。
---

## 摘要
检索增强和智能体工作负载会重复预填充可预测的结构化输入（我们称之为“跨度”），例如文档和代码文件。然而，vLLM等引擎中的前缀缓存无法重用这些结构的KV条目，除非它们与另一个请求共享相同的前缀；而生产级推理服务器中的位置无关缓存（PIC）实现通常要么需要大量的服务器代码修改，要么将KV状态保留在服务器外部，从而引入主机到设备的传输开销。我们提出了极简PIC（MiniPIC）：一种基于两个要素构建的最小化、灵活且快速的vLLM设计——无位置编码的KV缓存和用户控制的缓存复用原语。MiniPIC在KV缓存中存储未旋转的K向量，在注意力内部使用每个请求的逻辑位置对K块应用RoPE，并暴露三个面向用户的令牌级原语：块对齐填充、跨度分隔符（SSep）和提示依赖（PDep），它们修改哈希行为和有效的块级因果注意力结构。通过不到100行核心引擎更改加上一个自定义注意力后端，这些原语足以在同一个运行中的vLLM实例内实现多种PIC方法，包括块注意力、EPIC和Prompt Cache，同时原生集成KV缓存CPU卸载实现。在2WikiMultihopQA上，采用交错调度的MiniPIC相比基线vLLM将预填充吞吐量提高了49%，将缓存跨度的首个令牌生成时间降低了最多两个数量级，保持了未缓存跨度的线性预填充扩展，且最坏情况开销仅为5.7%。

## Abstract
Retrieval-augmented and agentic workloads repeatedly prefill recurring predictable structured inputs (which we call "spans") such as documents and code files. Yet, prefix caching in engines such as vLLM cannot reuse their KV entries unless they share identical prefixes with another request, while Position-Independent Caching (PIC) implementations within production-grade inference servers typically either require substantial server code changes or keep KV state outside the server, incurring host-to-device transfer overhead. We present Minimalistic PIC (MiniPIC): a minimal, flexible and fast vLLM design built from two ingredients: positional-encoding-free KV cache and user-controlled cache-reuse primitives. MiniPIC stores unrotated K vectors in the KV cache, applies RoPE to K tiles inside attention using per-request logical positions, and exposes three user-facing and token-level primitives: block-aligned padding, span separator (SSep), and prompt depend (PDep), that modify hashing behavior and effective block-level causal attention structure. With fewer than 100 lines of core-engine changes plus a custom attention backend, these primitives are sufficient to realize multiple PIC methods, including Block-Attention, EPIC, and Prompt Cache, within the same running vLLM instance, while natively integrating with KV cache CPU offload implementations. On 2WikiMultihopQA, MiniPIC with interleaved scheduling improves prefill throughput by 49% over baseline vLLM, reduces cached-span time-to-first-token by up to two orders of magnitude, preserves the linear prefill scaling of uncached spans, and incurs only 5.7% worst-case overhead.