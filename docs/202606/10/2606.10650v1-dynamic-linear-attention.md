---
title: Dynamic Linear Attention
title_zh: 动态线性注意力
authors: "Xin Wang, Hui Shen, Boyuan Zheng, Xueshen Liu, Minkyoung Cho, Zhongwei Wan, Zesen Zhao, Zhuoqing Mao, Shen Yan, Mi Zhang"
date: 2026-06-09
pdf: "https://arxiv.org/pdf/2606.10650v1"
tags: ["query:sparse-attn"]
score: 9.0
evidence: 动态线性注意力实现高效长上下文建模
tldr: 线性注意力虽降低计算复杂度，但现有方法采用固定状态合并策略，无法适应动态变化的token重要性，导致关键信息丢失。本文提出DLA动态记忆建模框架，通过信息感知动态状态合并和容量受限记忆建模，自适应确定状态边界并选择性合并低信息状态。在两种线性注意力模型上预训练，在16个数据集上超越现有方法。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.10650v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1716, \"height\": 310, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.10650v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1701, \"height\": 509, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.10650v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1124, \"height\": 425, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.10650v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1721, \"height\": 355, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.10650v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1384, \"height\": 290, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.10650v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1042, \"height\": 263, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.10650v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1041, \"height\": 192, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.10650v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 840, \"height\": 274, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.10650v1/table-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 840, \"height\": 328, \"label\": \"Table\"}]"
motivation: 现有线性注意力在多状态记忆中使用固定合并策略，无法建模动态token重要性，导致长序列中关键token信息被掩盖，误差累积严重。
method: DLA包含两项创新：信息感知动态状态合并按token信息变化自适应决定状态边界；容量受限记忆建模通过选择性合并相邻低信息状态维持固定大小缓存。
result: 在两种线性注意力模型上预训练，在16个数据集（涵盖三类任务）上均达到最优性能。
conclusion: DLA通过动态状态合并有效提升多状态线性注意力的长序列处理能力，优于现有方法。
---

## 摘要
大型语言模型（LLMs）在处理长上下文时的可扩展性根本上受限于标准注意力的二次复杂度，这促使人们采用具有次二次成本的线性注意力机制。为了提升长上下文下的表示能力，近期方法以多状态方式组织记忆。然而，现有的多状态线性注意力方法依赖于固定的状态合并策略，无法适应动态变化的令牌重要性，会不可逆地模糊关键令牌，并导致长序列上的严重错误累积。为解决这一局限性，我们提出了DLA，一种用于多状态线性注意力的动态记忆建模框架。DLA引入了（i）信息感知动态状态合并，该机制基于令牌级信息变化自适应地确定状态边界，在语义转换附近保留高分辨率表示，同时积极总结稳定区域，以及（ii）容量受限记忆建模，通过选择性地合并相邻的低信息状态来维持固定大小、按时间顺序排列的状态缓存，从而以最小信息损失控制记忆增长。我们在两种不同的线性注意力模型上预训练DLA，并在三个类别的16个数据集上进行评估。实验结果表明DLA优于现有最先进方法。

## Abstract
The scalability of Large Language Models (LLMs) to long contexts is fundamentally constrained by the quadratic complexity of standard attention, motivating the adoption of linear attention mechanisms with sub-quadratic cost. To improve representation capacity under long contexts, recent approaches organize memory in a multi-state manner. However, existing multi-state linear attention methods rely on fixed state merging policies that cannot adapt to dynamically varying token importance, irreversibly obscuring critical tokens and causing severe error accumulation over long sequences. To address this limitation, we propose DLA, a dynamic memory modeling framework for multi-state linear attention. DLA introduces (i) Information-Aware Dynamic State Merging, which adaptively determines state boundaries based on token-level information variation, preserving high-resolution representations around semantic transitions while aggressively summarizing stable regions, and (ii) Capacity-Bounded Memory Modeling, which maintains a fixed-size, chronologically ordered state cache by selectively merging adjacent low-information states to control memory growth with minimal information loss. We pre-train DLA on two different linear attention models and evaluate on 16 datasets across three categories. Experimental results demonstrate the superiority of DLA over state-of-the-art.

---

## 论文详细总结（自动生成）

## 1. 核心问题与整体含义
- **研究动机**：大语言模型（LLMs）处理长上下文时面临标准注意力二次复杂度的瓶颈，线性注意力通过次二次复杂度缓解这一问题。但现有**多状态线性注意力方法**（如Log-Linear Attention）采用**固定状态合并策略**（如固定块大小或规则调度），无法适应 token 重要性的动态变化，导致关键 token 被过早合并到粗粒度摘要中，且错误不可逆地累积，严重损害长序列表示质量。
- **整体含义**：本文旨在突破固定合并策略的局限，提出一种**信息感知、自适应记忆建模**的线性注意力框架，在保持线性复杂度的同时，提升长上下文建模能力。

## 2. 方法论：核心思想、关键技术细节
- **核心思想**：DLA 包含两个关键模块：
  1. **信息感知动态状态合并**：根据 token 级信息变化（State Information Score）动态决定新 token 是合并到当前状态还是创建新状态，在语义过渡区保留高分辨率，在稳定区域积极压缩。
  2. **容量受限记忆建模**：维护一个固定大小的状态缓存（默认容量 K=30），当缓存满时，选择**信息密度最低**的相邻状态对进行合并，以最小信息损失控制记忆增长，同时保持时间顺序。
- **关键技术细节**：
  - **State Information Score**：  
    \[
    I_t = \frac{\|S_t - S_{t-1}\|_F}{\|S_{t-1}\|_F + \epsilon}
    \]  
    其中 \( S_t \) 是当前 token 的状态（\( \phi(k_t)v_t^\top \)），\( S_{t-1} \) 是最近记忆状态。预训练时用软门控微分，推理时硬阈值（\( \tau = 0.6 \)）决定边界。
  - **状态合并与缓存管理**（算法1、2）：  
    - 新 token 计算 \( I_t \)，若 \( I_t < \tau \) 则合并到当前状态，否则追加为新状态。  
    - 当缓存满（m=K）时，选择相邻状态对 \( (i, i+1) \) 中 \( (\bar{I}_i + \bar{I}_{i+1})/(n_i + n_{i+1}) \) 最小的进行合并（\( S_{i} \leftarrow S_i + S_{i+1} \)，更新 token 计数和信息分数）。
  - **输出计算**：  
    \[
    o_t = \sum_{i=1}^m \lambda_{t,i} q_t^\top S_i
    \]  
    其中 \( \lambda_{t,i} \) 由预训练学到，依赖查询表示。
- **理论支撑**：通过定理3.1和推论3.2证明固定合并策略在非平稳序列上产生更大的偏差上界，而DLA自适应合并可最小化该上界。

## 3. 实验设计
- **数据集与场景**：16个数据集，涵盖三类任务：
  - **常识推理**（8个）：LAMBADA、PIQA、HellaSwag、WinoGrande、ARC-e、ARC-c、OpenBookQA、CommonsenseQA。
  - **上下文检索**（6个）：SQuAD、TriviaQA、SWDE、FDA、NQ、Drop。
  - **长上下文建模**（2个）：RULER（4k/8k/16k长度）、LongBench（含多类型任务）。
- **Benchmark**：与现有最先进方法对比，包括：
  - 基线：Mamba-2-780M、Gated DeltaNet-1.3B（原始线性注意力）。
  - 多状态基线：上述模型 + Log-Linear Attention。
  - 全注意力 Transformer（与Mamba-2-780M参数相当的24层778M Transformer）。
- **评估指标**：准确率（常识推理、检索）、RULER上的单/多针检索准确率、LongBench上各子任务得分。

## 4. 资源与算力
- **后文说明**：所有预训练在 **4块NVIDIA A100 GPU** 上进行，使用 **50B tokens** 在 Long-Data-Collections 数据集上训练，序列长度 **16K**。训练时长未明确给出（仅提及“academic-scale language modeling pretraining from scratch”），但可推断为中等规模预训练。
- 推理效率实验在同一单A100 GPU上进行。

## 5. 实验数量与充分性
- **实验数量**：总计在 **16个数据集**上进行全面评估，包括三大类任务。此外还包含：
  - 推理效率实验（不同batch size和context length下的吞吐量、内存）。
  - 消融研究（分离两个组件的影响，表5）。
  - 超参数敏感性分析（记忆容量k和边界阈值τ，表6）。
- **充分性**：实验覆盖了多个主流评测基准，对比了强基线（Log-Linear Attention、原始线性注意力和全注意力），且进行了详尽的消融和参数分析。结果一致表明DLA优于所有基线，统计显著且鲁棒。实验设计客观公平（使用相同训练配置、同样参数规模、同一评测框架LM-Evaluation-Harness）。

## 6. 主要结论与发现
- **性能领先**：DLA在**所有16个数据集**上均优于 Log-Linear Attention，在Mamba-2上平均提升8%（常识推理）、44%（检索）；在Gated DeltaNet上平均提升6%（推理）、42%（检索）。在长上下文任务（RULER、LongBench）上提升更为显著（如RULER多针任务提升可达250%）。
- **逼近甚至超越全注意力**：Mamba-2 + DLA在多个常识推理任务上超越同参数全注意力Transformer（32.9% → 34.2%平均准确率）。
- **高效性**：DLA相比 Log-Linear Attention，在相同或更低内存消耗下获得更高吞吐量（图3）。
- **鲁棒性**：记忆容量k和边界τ对最终性能影响轻微，表明方法稳健。

## 7. 优点
- **方法创新**：首次将**token级信息变化**引入状态合并决策，动态分配记忆分辨率，理论上有界保证。
- **实用性强**：同时保证**自适应表示**和**固定推理成本**（容量受限缓存），适合实际部署。
- **实验充分**：覆盖多种线性注意力架构（Mamba-2、DeltaNet）、多种任务类型，并进行效率、消融、参数敏感性分析。
- **公平对比**：使用相同训练配置、相同参数规模、相同评测框架，结论可靠。

## 8. 不足与局限
- **实验规模**：预训练仅用 **50B tokens**，与目前主流大模型（>1T tokens）差距较大，可能低估DLA在大规模预训练下的潜力或局限性。
- **推理效率比较**：仅与Log-Linear Attention对比，未与其他高效线性注意力变体（如基于稀疏注意力或似然方法的模型）比较。
- **超参数选择**：阈值τ和容量K基于经验设定（τ=0.6, K=30），未说明是否对每个模型单独调优，可能存在未探索的适应空间。
- **长序列极限测试**：最长上下文为16K，未测试更极端长度（如100K+）下的行为，内存增长是否仍可控需验证。
- **理论假设**：信息分数通过F范数衡量，可能无法完全捕获语义变化，尤其在高度抽象的任务中。
- **可解释性**：学习到的\(\lambda_{t,i}\)权重未分析其含义，动态合并过程的可视化或解释缺失。

（完）
