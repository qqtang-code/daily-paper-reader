---
title: Training-free sparse attention based on cumulative energy filtering
title_zh: 基于累积能量过滤的无训练稀疏注意力
authors: "Chunlu Li, Yixuan Pan, Bai Du, Zhenyuan Chen, Yanzhao Li, Hui Dong, Hui Wang, Zhiqiang Zou"
date: 2026-06-15
pdf: "https://arxiv.org/pdf/2606.16317v1"
tags: ["query:sparse-attn"]
score: 9.0
evidence: 基于累积能量滤波的训练无关稀疏注意力用于DiTs
tldr: "视频生成中的Diffusion Transformer加速需稀疏注意力，但现有方法在稀疏性和精度间难以平衡。本文通过固定召回率保证精度，采用动态阈值提升稀疏性，并与Flash Attention深度融合消除额外开销。在Wan 2.2上，稀疏度从61.42%提升至82%，VBench指标下降小于5%，计算效率提升1.61倍。首次同时优化稀疏性和精度，实现高效无需额外开销的稀疏注意力。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.16317v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1435, \"height\": 805, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.16317v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1459, \"height\": 499, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.16317v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1450, \"height\": 604, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.16317v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1430, \"height\": 558, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.16317v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1447, \"height\": 2065, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.16317v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1470, \"height\": 686, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.16317v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1195, \"height\": 360, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.16317v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1360, \"height\": 499, \"label\": \"Table\"}]"
motivation: 现有稀疏注意力算法无法同时最大化稀疏性和最小化精度损失，需动态阈值方案兼顾两者。
method: 固定召回率保证精度，动态阈值提升稀疏性，深度融合Flash Attention消除额外计算开销。
result: "Wan 2.2上稀疏度从61.42%提至82%，VBench降幅<5%，计算效率提升1.61倍。"
conclusion: 动态阈值方案在保持精度下显著提升稀疏性和效率，无需额外掩码计算。
---

## 摘要
稀疏注意力通过仅计算重要令牌而跳过其余部分来加速用于视频生成的扩散变换器（DiTs）。令牌选择策略是平衡稀疏性和准确性的关键。我们将令牌过滤过程表述为一个双目标优化问题：最大化稀疏性和最小化准确度下降。现有算法无法同时实现这两个目标。例如，Top-p只考虑准确性约束，而Top-k维持固定的计算预算但放松了准确性约束。本文证明，维持固定的召回率对于确保准确性是足够的，而固定阈值对于降低计算成本是次优的。因此，我们提出了一种动态阈值方案，在保持相同准确性水平的同时提高稀疏性。此外，我们的算法与Flash Attention（FA）深度集成，消除了任何额外的掩码计算开销。在Wan 2.2上的实验结果验证，与同样集成FA的BLASST算法相比，我们的动态阈值策略将稀疏性从61.42%提高到82%，同时VBench指标下降不到5%。这导致注意力计算减少约15%，计算效率提升1.61倍，比BLASST高1.18倍。

## Abstract
Sparse attention accelerates Diffusion Transformers (DiTs) for video generation by computing only the important tokens while skipping the rest. The token selection strategy is key to balancing sparsity and accuracy. We formulate the token filtering process as a dual-goal optimization problem: maximizing sparsity and minimizing accuracy degradation. Existing algorithms cannot fulfill both objectives simultaneously. For example, Top-p only considers the accuracy constraint, while Top-k maintains a fixed computational budget but loosens the accuracy constraint.   This paper demonstrates that maintaining a fixed recall rate is sufficient for ensuring accuracy, whereas a fixed threshold is suboptimal for reducing computational cost. Therefore, we propose a dynamic thresholding scheme to improve sparsity while maintaining the same level of accuracy. Furthermore, our algorithm is deeply integrated with Flash Attention (FA), eliminating the need for any additional masking computation overhead. Experimental results on Wan 2.2 validate that, compared to the BLASST algorithm which is also integrated with FA, our dynamic thresholding strategy enhances sparsity from 61.42\% to 82\% with a VBench metric drop of less than 5\%. This results in an approximate 15\% in attention computation and a $1.61\times$ increase in computational efficiency, which is 1.18x higher than that of BLASST.

---

## 论文详细总结（自动生成）

# 论文总结：Training-free sparse attention based on cumulative energy filtering

## 1. 论文的核心问题与整体含义（研究动机和背景）

- **研究动机**：基于扩散变换器（DiTs）的视频生成模型中，注意力机制的计算复杂度随序列长度呈二次增长，导致推理速度慢。稀疏注意力通过仅计算重要令牌来加速，但现有方法在“最大化稀疏性”和“最小化精度损失”这两个目标之间难以平衡。
- **核心问题**：常用方法如Top‑k（固定计算预算）放松了精度约束，Top‑p（固定召回率）则无法保证计算成本最优；固定阈值方法（如基于最大值的过滤）也因P矩阵分布变化而次优。需要一种能同时兼顾稀疏性和精度的动态策略。
- **整体含义**：证明维持固定召回率即足以确保精度，但固定阈值对降低计算成本并非最优。据此提出一种基于累积能量（LSE）的动态阈值方案（DynSparse），无需额外掩码计算，可深度集成到Flash Attention中。

## 2. 论文提出的方法论：核心思想、关键技术细节、公式或算法流程

### 核心思想
- **问题重述**：稀疏注意力可视为双目标优化问题：最小化计算成本（最大化稀疏性），同时确保输出误差小于阈值。论文通过理论分析指出，最优阈值应与P矩阵的分布动态相关，而非固定值。
- **关键洞察**：在Flash Attention内部，累积能量（Log-Sum-Exp，LSE）自然反映了已选中令牌的累积概率，且其对数形式可作为判断后续块是否重要的动态指标。高熵分布（均匀）时LSE增长慢，可激进剪枝；低熵分布（尖峰）时一旦遇到全局最大值，LSE迅速上升，可提前退出。

### 关键技术细节
- **决策指标**：第i个查询、第j个块的决策指标为  
  `t_ji = ˆm_i - ln ℓ(j-1)_i - m(j-1)_i = ˆm_i - LSE(j-1)_i`  
  其中ˆm_i是当前块局部最大值，m(j-1)_i是累积最大值，ℓ(j-1)_i是累积概率（LSE的指数部分）。该指标表示当前块的潜在贡献相对已累积能量的对数比值。
- **阈值λ**：全局超参数，物理意义为某块“潜在能量”与“累积质量”之比的对数。建议初始值设为 `λ = ln(P_real) + ln(Bc/L)`，其中Bc为块大小，L为序列长度，P_real为校准常数（峰值与平均比）。对Wan 2.2 14B模型（L=75600，Bc=128），推荐λ=-6。
- **算法流程**：在Flash Attention的在线softmax循环中，每处理一个块时计算t_ji，若t_ji < λ则跳过该块（不参与p与v的计算）。所有变量（ˆm, ℓ, m）均为FA内部已有状态，无需额外计算。

### 与现有方法的区别
- 不同于Top‑k/Top‑p需要预先估计掩码，DynSparse在FA循环内动态决策，且不引入额外开销。
- 相比BLASST（使用running max与global max的差值），DynSparse使用LSE，能更准确反映累积能量，从而在相同精度下达到更高稀疏性。

## 3. 实验设计：使用了哪些数据集 / 场景，它的 benchmark 是什么，对比了哪些方法

- **数据集/场景**：视频生成模型Wan 2.2的两个版本：5B参数（序列长度27280，块大小32）和14B参数（序列长度75600，块大小128）。使用VBench基准评估生成视频质量。
- **Benchmark指标**：VBench包含Aesthetic Quality (AQ)、Background Consistency (BC)、Dynamic Degree (DD)、Imaging Quality (IQ)、Subject Consistency (SC)、Temporal Flickering (TF)、Temporal Style (TS)等。
- **对比方法**：
  - FlashAttention3 (FA3) —— 全注意力基线
  - SparseVideoGen (SVG)、RadialAttention (Radial) —— 静态/半静态稀疏
  - LiteAttention —— 掩码复用
  - BLASST —— 与FA深度融合的动态阈值方法（running max阈值）
  - Top‑k (k=0.8)、Top‑p (p=0.9) —— 固定预算/固定召回率方法
- **实验设置**：在同一prompt下生成视频，报告平均稀疏度（Sps）和计算加速比（SpeedUp）。定量结果见Table 1，消融结果见Table 2。

## 4. 资源与算力

- **未明确说明**：论文未提及具体GPU型号、数量、训练时长或推理耗时。仅在Table 1中报告了加速比（SpeedUp），但未给出绝对时间（可能由于商业模型未公开）。资源算力信息缺失。

## 5. 实验数量与充分性：做了多少组实验，是否充分、客观、公平

- **实验组数**：
  - 主要对比实验：两个模型（5B和14B）× 5~7种方法，共约12组定量结果（Table 1）。
  - 消融实验：在5B模型上测试6个不同λ值（-6, -8, -9, -11, -13等），对应稀疏度从92%到50%，共6组（Table 2）。
  - 定性可视化：Fig.2/Fig.5展示了帧截图对比，Fig.3展示掩码差异。
  - 统计分析：Appendix A.1 分析了不同峰度下召回率与MAE的关系（Fig.4和Table 3）。
- **充分性与公平性**：
  - 优点：对比了多种主流动态/静态方法，且对BLASST进行了直接公平比较（均集成到FA）。
  - 不足：未与更多最新方法（如Sparse Attention with Hilbert reordering/聚类方法）对比；未报告标准差或置信区间；VBench指标差异较小，但未进行统计显著性检验；对14B模型，部分对比方法（SVG、Radial、Lite）的数据是从Lite论文摘录的，非作者复现，可能引入不一致。
  - 总体上实验数量适中，消融验证了阈值鲁棒性，但进一步在更多模型或更长序列上的泛化性未验证。

## 6. 论文的主要结论与发现

- **理论贡献**：揭示了Top‑p和Top‑k的局限性，证明了固定召回率是精度的充分条件但非必要条件，且最优阈值与P矩阵分布（峰度）相关。
- **方法贡献**：DynSparse通过利用FA内部的累积能量动态决策，无需额外掩码计算，在保持精度（VBench指标下降<5%）前提下将稀疏性从BLASST的61.42%提升至82.14%（14B模型），计算效率提升1.61倍（比BLASST高1.18倍）。
- **实验发现**：
  - 某些层（5-8、20-25）和早期去噪步骤对精度更敏感，稀疏性较低。
  - 高熵分布下可更激进剪枝，低熵分布下需保留更多令牌。
  - 在相同稀疏度下，DynSparse的视觉质量优于Top‑k和Top‑p（减少了抖动和伪影）。

## 7. 优点：方法或实验设计上的亮点

- **方法创新性**：
  - 首次将累积能量（LSE）作为稀疏决策指标，理论上比running max更精确。
  - 与Flash Attention深度集成，零额外计算开销，实用性强。
  - 超参数λ有清晰的物理意义和初始化公式，便于调参。
- **实验设计**：
  - 同时评估了定性和定量指标（VBench + 掩码可视化 + 帧截图）。
  - 分析了稀疏性随层和时间步的变化，揭示了模型内部注意力分布特性。
  - 消融实验验证了阈值λ的鲁棒性（从-9到-13均可接受，-8最优）。
- **理论分析**：提供了误差上界的数学推导（Eq.1-4），并利用实际数据验证了峰度与最优召回率的关系。

## 8. 不足与局限

- **实验覆盖**：
  - 仅在Wan 2.2一个模型族上测试，未在更多DiT模型（如Open-Sora、Stable Video Diffusion）上验证泛化性。
  - 对比方法中SVG、Radial等数据引自其他论文，未在同一环境下复现，可能引入不公平。
  - 未报告推理时间绝对值，仅给出加速比，且加速比可能受实现细节影响。
- **偏差风险**：
  - 超参数λ的推荐值（-6）仅基于Wan 2.2 14B模型，对其他模型需重新校准。
  - 块大小在5B和14B模型中不同（32 vs 128），可能引入不一致。
- **应用限制**：
  - 方法假设V值独立同分布，实际可能不严格成立。
  - 动态阈值依赖FA内部循环，对于不支持修改FA内核的框架可能难以直接集成。
- **理论部分**：
  - 充分性证明基于最坏情况假设（均匀分布），实际误差可能低于上界，但未给出更紧的界。
  - 对低熵分布下的误差控制仅通过提前退出实现，未分析极端情况（如所有注意力集中在一个令牌）的行为。

（完）
