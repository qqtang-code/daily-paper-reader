---
title: "Sparrow: Sparse Rollout for Stable and Efficient Long-context RL of Large Language Models"
title_zh: Sparrow：面向大型语言模型稳定高效长上下文强化学习的稀疏展开
authors: "Yang Zhou, Ranajoy Sadhukhan, Zhaofeng Sun, Zhuoming Chen, Souvik Kundu, Saket Dingliwal, Sai Muralidhar Jayanthi, Aram Galstyan, Haizhong Zheng, Beidi Chen"
date: 2026-06-07
pdf: "https://arxiv.org/pdf/2606.08446v1"
tags: ["query:sparse-attn"]
score: 9.0
evidence: 强化学习中基于稀疏注意力的长上下文展开
tldr: 针对长上下文强化学习中稀疏注意力加速但训练不稳定的问题，提出动态稀疏调度，通过保持每token的actor-policy mismatch尾部统计量恒定来稳定训练。在Qwen3-1.7B/4B/8B上分别实现2.2x/2.4x/2.0x的rollout加速，阈值可泛化至更大模型和编码领域。进一步推导DistillSparse蒸馏方法，以更激进稀疏度达到相同mismatch阈值，获得更高加速。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.08446v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1746, \"height\": 527}, {\"url\": \"assets/figures/arxiv/2606.08446v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1737, \"height\": 501}, {\"url\": \"assets/figures/arxiv/2606.08446v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 612, \"height\": 454}, {\"url\": \"assets/figures/arxiv/2606.08446v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1733, \"height\": 1123}, {\"url\": \"assets/figures/arxiv/2606.08446v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1734, \"height\": 466}, {\"url\": \"assets/figures/arxiv/2606.08446v1/fig-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 1738, \"height\": 534}, {\"url\": \"assets/figures/arxiv/2606.08446v1/fig-007.webp\", \"caption\": \"\", \"page\": 0, \"index\": 7, \"width\": 860, \"height\": 689}, {\"url\": \"assets/figures/arxiv/2606.08446v1/fig-008.webp\", \"caption\": \"\", \"page\": 0, \"index\": 8, \"width\": 1387, \"height\": 1028}, {\"url\": \"assets/figures/arxiv/2606.08446v1/fig-009.webp\", \"caption\": \"\", \"page\": 0, \"index\": 9, \"width\": 1225, \"height\": 1223}, {\"url\": \"assets/figures/arxiv/2606.08446v1/fig-010.webp\", \"caption\": \"\", \"page\": 0, \"index\": 10, \"width\": 992, \"height\": 781}, {\"url\": \"assets/figures/arxiv/2606.08446v1/fig-011.webp\", \"caption\": \"\", \"page\": 0, \"index\": 11, \"width\": 1041, \"height\": 815}, {\"url\": \"assets/figures/arxiv/2606.08446v1/fig-012.webp\", \"caption\": \"\", \"page\": 0, \"index\": 12, \"width\": 1046, \"height\": 821}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.08446v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1751, \"height\": 730}, {\"url\": \"assets/tables/arxiv/2606.08446v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1005, \"height\": 307}, {\"url\": \"assets/tables/arxiv/2606.08446v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 941, \"height\": 157}, {\"url\": \"assets/tables/arxiv/2606.08446v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1681, \"height\": 512}, {\"url\": \"assets/tables/arxiv/2606.08446v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 635, \"height\": 269}]"
motivation: 稀疏rollout训练易因过度稀疏导致模型崩溃，需精细控制稀疏度以平衡稳定性和加速收益。
method: 提出动态稀疏调度，根据每token的actor-policy mismatch尾部统计量调整生成过程中的稀疏度，使其保持恒定阈值。
result: 在Qwen3-1.7B、4B、8B上分别获得2.2x、2.4x、2.0x的rollout加速，且阈值可泛化至Qwen3-14B和编码领域。
conclusion: 验证了长上下文RL中稳定稀疏rollout训练的关键因素，并引入DistillSparse蒸馏方法实现更激进稀疏下的高效加速。
---

## 摘要
尽管可验证奖励强化学习（RLVR）功能强大，但它会诱导极长的思维链，导致计算成本高昂。由于RLVR每步成本主要由长上下文展开生成主导，稀疏注意力为加速密集展开提供了一种有前景的方法。然而，稀疏展开需要在稳定性与效率之间进行精细权衡：过于激进的稀疏性会导致崩溃，而过于宽松的稀疏性则无法提供足够的加速。在这项工作中，我们通过稀疏到密集的演员-策略不匹配来研究这种权衡。我们首先观察到，稀疏展开崩溃并非由所有token的均匀退化驱动：即使在激进的稀疏性下，大多数稀疏token也与密集token完美对齐。受此启发，我们假设，如果每个token的演员-策略不匹配的下尾在整个轨迹中保持在临界阈值以上，则稀疏展开训练保持稳定。我们引入了一种动态稀疏调度，在生成过程中保持该尾部统计量恒定，并验证了我们的假设。在Qwen3思维家族模型中，将尾部不匹配统计量保持在一致阈值附近通常能够实现稳定训练。然后，我们使用一个成本模型来找到在不匹配阈值下实现最大加速的稀疏调度，在训练Qwen3-1.7B、Qwen3-4B和Qwen3-8B时分别实现了2.2倍、2.4倍和2.0倍的展开加速。实验证明，这些阈值可以推广到更大的模型（Qwen3-14B）和另一个RL领域（编码）。最后，我们的分析自然引出了DistillSparse：基于轻量级LoRA的稀疏展开蒸馏允许更激进的稀疏性达到相同的稀疏到密集不匹配阈值，从而获得更高的加速。

## Abstract
Despite being powerful, reinforcement learning with verifiable rewards (RLVR) induces extremely long COT, making it computationally expensive. Since RLVR per-step cost is dominated by long-context rollout generation, sparse attention offers a promising way to accelerate dense rollout. However, sparse rollouts require a delicate stability-efficiency tradeoff: overly aggressive sparsity causes collapse, while overly lenient sparsity gives insufficient speedup. In this work, we study this tradeoff through sparse-to-dense actor-policy mismatch. We first observe that sparse rollout collapse is not driven by uniform degradation across tokens: most sparse tokens align perfectly with dense even under aggressive sparsity. Motivated by this, we hypothesize that sparse rollout training remains stable if the lower tail of per-token actor-policy mismatch stays above a critical threshold throughout the trajectory. We introduce a dynamic sparsity schedule that keeps this tail statistic constant during generation and validate our hypothesis. Across Qwen3 thinking-family models, keeping the tail mismatch statistic near a consistent threshold generally enables stable training. We then use a cost model to find the sparsity schedule for maximum speedup under this mismatch threshold, achieving 2.2x, 2.4x, and 2.0x rollout speedups when training Qwen3-1.7B, Qwen3-4B, and Qwen3-8B. Empirically, we show the thresholds generalize to a larger model (Qwen3-14B) and another RL domain (coding). Finally, our analysis naturally motivates DistillSparse: lightweight LoRA-based distillation on sparse rollout lets more aggressive sparsity reach the same sparse-to-dense mismatch threshold, yielding higher speedup.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义（研究动机和背景）

- **问题**：可验证奖励强化学习（RLVR）虽能诱导长思维链（CoT）提升推理能力，但训练成本极高，其中长上下文展开（rollout）生成占据每步成本的70%以上。稀疏注意力可加速滚动生成，但直接使用会导致训练不稳定：过于激进的稀疏性引发训练崩溃，而过于保守的稀疏性则加速收益有限。
- **背景**：现有方法（如异步RL、量化、推测解码）要么不能减少滚动负载，要么引入额外开销；而稀疏注意力在推理精度上表现良好，但实际导致RL不稳定的根源在于滚动策略（sparse actor）与训练策略（dense policy）间的每token分布不匹配（actor-policy mismatch），而非简单的奖励不足。
- **核心目标**：如何使稀疏滚动在最大化加速的同时，保持密集滚动RL的训练稳定性，并且该方案应能跨模型规模泛化。

### 2. 论文提出的方法论：核心思想、关键技术细节、公式或算法流程（文字说明）

- **核心洞察**：稀疏滚动导致训练崩溃并非所有token的退化，而是由少数token的严重不匹配引起。大多数token即使在高稀疏度下也与密集策略完美对齐。因此，**平均不匹配度不是好的稳定性指标**，而应关注**尾部统计量**（如5%分位数的per-token接受率）。
- **关键技术**：
  - **动态稀疏调度（Dynamic Sparsity Scheduling）**：在生成过程中，随着序列长度增加，动态调整KV预算（即稀疏度），使得整个轨迹的5%分位数接受率保持恒定在某个阈值（如0.86）。这避免了固定稀疏度下尾部不匹配随长度增长而恶化的问题。
  - **成本模型（Cost Model）**：基于模型参数、硬件带宽、序列长度、KV预算等参数，构建稀疏注意力的计算和内存成本模型，用于在给定尾部不匹配阈值下，搜索使滚动成本最小的页面大小（page size）和KV预算。结论是page size=16在大多数情况下最优。
  - **DistillSparse蒸馏**：在RL训练中增加一个轻量级LoRA模块，在滚动生成时使用稀疏注意力+LoRA，利用训练时已计算的密集log-prob进行在线蒸馏（KL散度损失），从而在相同尾部不匹配阈值下允许更激进的稀疏度，进一步提升加速比。算法步骤：使用稀疏解码+LoRA生成轨迹 → 密集模型重新计算log-prob → 更新密集模型参数（PPO） → 冻结密集模型，更新LoRA模块（蒸馏损失+PPO损失）。

- **公式/算法（文字描述）**：
  - 使用5%分位数接受率 $\tau$ 作为稳定性指标。通过预实验为每个模型规模找出使训练稳定的临界 $\tau$（约0.86）。
  - 对于每个序列长度区间，预先测量不同KV预算下的 $\tau$，然后制定调度方案：每个长度区间使用恰好能达到 $\tau$ 的最小KV预算。
  - DistillSparse算法：第3步用稀疏+LoRA生成；第4步密集模型计算log-prob用于PPO；第5步稀疏模型计算log-prob用于蒸馏损失；第6步更新密集模型；第7步冻结密集模型更新LoRA；第8步蒸馏损失为PPO损失 + λKL散度。

### 3. 实验设计：使用了哪些数据集/场景，它的benchmark是什么，对比了哪些方法

- **模型**：Qwen3 thinking家族（1.7B、4B、8B、14B），均为可在训练中产生长CoT的模型。
- **主要数据集**：
  - **数学推理**：Polaris（53K示例，含AIME 2024/2025/2026和AMC 2022/2023等），最大生成长度截断至37K token。
  - **编码**：TACO（21K过滤后训练），评估用LiveCodeBench、HumanEval+、MBPP+，生成长度截断24K token。
- **基准方法**：密集滚动（dense rollout）训练的密集策略作为baseline。
- **对比方法**：
  - 不同稀疏度水平下的固定KV预算（如KV budget 2560 vs 4096导致崩溃与稳定）。
  - 不同尾部不匹配阈值（0.75,0.8,0.86,0.92）下的训练稳定性对比。
  - 现有分布对齐技术（如截断重要性采样TIS、拒绝采样Jackpot）的对比实验（见附录F，显示不能解决稀疏滚动的不稳定性）。
  - 不同页面大小（16,32,64,128）的成本-精度代价权衡。
  - DistillSparse与原始稀疏调度对比（相同阈值但加速更高）。
- **评估指标**：AIME（24/25/26）的Mean@16和Pass@16，AMC的Mean@4；编码领域的LiveCodeBench、HumanEval+、MBPP+；训练奖励曲线；滚动加速比（tokens/s）、端到端迭代加速比。

### 4. 资源与算力：如果文中有提到，请总结使用了多少算力（GPU型号、数量、训练时长等）。若未明确说明，也请指出这一点。

- **明确提及**：
  - 主要实验使用**4×H200 GPU**进行效率基准测试（表2）。
  - 训练Qwen3-14B（37K截断）使用**4个H200节点（共32×H200）**，耗时约**8天（190小时）** 完成一个epoch。
  - 稀疏推理引擎基于Vortex库（Infini-AI-Lab,2026），训练使用Verl（Sheng等,2025a）和SGLang（Zheng等,2024）。
  - 蒸馏实验（图10）在2×H200 GPU上运行。
- **未明确部分**：其他小模型（1.7B/4B/8B）的具体GPU数量和训练时长未详细报告，但根据论文描述可以推断使用了类似的硬件（H200节点）。

### 5. 实验数量与充分性：大概做了多少组实验（如不同数据集、消融实验等），这些实验是否充分、是否客观、公平。

- **实验组数**：
  - **控制研究1（阈值搜索）**：对每个模型规模（1.7B/4B/8B）测试了18种稀疏配置，覆盖不同KV预算和序列长度bin，生成尾部不匹配曲线。
  - **控制研究2（成本模型）**：对每个模型搜索页面大小16/32/64/128，以及多种KV预算。
  - **主实验（表1）**：对比密集滚动、两个稀疏水平（0.92,0.86）以及DistillSparse，在三个数学基准（AIME24/25/26）上报告Mean@16和Pass@16，以及在AMC上的Mean@4。
  - **泛化实验**：Qwen3-14B（阈值0.86）的训练奖励曲线和下游评估（表1）；编码RL（Qwen3-1.7B）在三个编码基准上的结果（表3）。
  - **蒸馏案例研究**：在Qwen3-1.7B上显示DistillSparse提升尾部对齐，并使用0.80级稀疏度加速至2.5x（图6c）。
- **充分性**：实验覆盖了多个模型规模、不同稀疏度、两种任务域（数学和编码），并包含了消融（不同尾部阈值、不同页面大小）、对比（与密集基线及现有对齐技术）、泛化验证。实验设计较为客观公平，采用标准基准和多次采样（16次/4次）以减少方差。

### 6. 论文的主要结论与发现

- **核心发现1**：稀疏滚动训练的不稳定性主要来源于**少数token的严重actor-policy不匹配**，而非平均行为。5%分位数接受率是更有效且与序列长度强相关的稳定性指标。
- **核心发现2**：通过**动态稀疏调度**使整个轨迹的尾部不匹配保持恒定的阈值（约0.86），可以在所有测试模型规模上稳定训练，并实现显著加速：Qwen3-1.7B 2.2x、4B 2.4x、8B 2.0x、14B 1.48x（滚动加速）。
- **核心发现3**：该阈值能够**泛化到更大模型（14B）和不同领域（编码）**，无需重新搜索。
- **核心发现4**：**模型越大，维持相同尾部阈值所需的KV预算也越大**，导致加速比随模型增大而下降。
- **核心发现5**：**DistillSparse** 通过轻量级LoRA在线蒸馏，在不改变训练稳定性的前提下，使更激进的稀疏度（如原0.80尾部对齐）达到0.86阈值，从而获得额外加速（如2.5x）。

### 7. 优点：方法或实验设计上有哪些亮点

- **方法创新**：
  - 首次从**尾部分布**角度理解稀疏滚动的不稳定性，并引入5%分位数接受率作为实用稳定性指标。
  - 提出**动态稀疏调度**，解决了固定稀疏度下尾部不匹配随长度增长而恶化的问题，使得控制研究成为可能。
  - **DistillSparse** 巧妙地复用训练中已有的密集log-prob计算进行蒸馏，仅通过LoRA引入极低额外开销，实现了更高效利用稀疏度。
- **实验设计**：
  - **控制研究**：首先通过大量测量建立尾部不匹配与稳定性之间的定量关系，再基于成本模型推导最优调度，逻辑清晰、系统性强。
  - **泛化验证**：不仅在多个模型规模内验证，还跨任务（数学→编码）、跨模型尺寸（8B→14B）验证阈值不变性，增强了结论的鲁棒性。
  - **效率评估**：同时报告了滚动加速比和端到端迭代加速比（表2），并给出了实际硬件上的测量结果（表4）。

### 8. 不足与局限：包括实验覆盖、偏差风险、应用限制等。

- **模型与任务局限性**：实验仅局限于Qwen3 thinking家族模型，且主要在数学推理和编码任务上验证。其他模型家族（如DeepSeek、Llama）或其他RL场景（如对话、工具使用）的泛化性未验证。
- **稀疏注意力类型局限**：主要研究块稀疏注意力（block-sparse），结论可能不完全适用于其他稀疏机制（如细粒度token-level稀疏）。作者也仅提到“相信类似结论可推广”。
- **硬件依赖**：成本模型基于特定GPU（H200）的算术强度和内存带宽，不同硬件（如A100、B200）可能会改变最优页面大小和加速比。
- **稳定性阈值是否通用**：文中指出的0.86阈值是针对Qwen3思维模型和数学任务得出的，其他模型或任务可能需要重新搜索。虽在14B和编码上成功泛化，但样本有限。
- **计算开销**：虽然DistillSparse额外成本低，但本文对多轮蒸馏后能否持续提升对齐程度、是否存在收敛极限未深入探讨。
- **消融实验覆盖**：未详细对比不同阈值选择对最终性能的敏感度，例如0.85或0.87是否同样良好？文中只列出了四个阈值进行对比，粒度偏粗。

（完）
