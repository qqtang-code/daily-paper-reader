---
title: "LVSA: Training-Free Sparse Attention for Long Video Diffusion"
title_zh: "LVSA: 无需训练的长视频扩散稀疏注意力"
authors: "Gael Glorian, Ioannis Lamprou, Zhen Zhang, Yujie Yuan, Hongsheng Liu"
date: 2026-05-29
pdf: "https://arxiv.org/pdf/2605.31057v1"
tags: ["query:sparse-attn"]
score: 9.0
evidence: 训练无关的块稀疏注意力用于长视频扩散
tldr: 长视频扩散模型中密集自注意力导致计算二次增长和生成静态重复视频。现有方案需重训练或可扩展性差。LVSA采用无需训练的块稀疏注意力，结合结构窗口和旋转全局锚点，消除固定网格偏差。在多个模型上实现2.98-3.33倍加速，支持更长时间生成，并引入VQeval公正评估。LVSA在训练长度内质量中性，扩展长度质量提升。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.31057v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1626, \"height\": 980, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2605.31057v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 726, \"height\": 320, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2605.31057v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1648, \"height\": 256, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2605.31057v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1626, \"height\": 452, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2605.31057v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1651, \"height\": 624, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2605.31057v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1496, \"height\": 427, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2605.31057v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1587, \"height\": 544, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2605.31057v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1381, \"height\": 594, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2605.31057v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1657, \"height\": 492, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2605.31057v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 593, \"height\": 388, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2605.31057v1/table-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 591, \"height\": 388, \"label\": \"Table\"}]"
motivation: 密集自注意力在长视频扩散中计算成本高且超出训练长度产生静态重复视频，现有方法要么昂贵要么难以扩展。
method: 提出无需训练、模型无关的块稀疏注意力LVSA，结合结构窗口和旋转全局锚点，消除长范围时间伪影。
result: 在Wan 2.1等模型上实现2.98-3.33倍加速，支持HunyuanVideo 2倍长度生成，比RIFLEx快2.41倍，在NPU上达3.24倍加速。
conclusion: LVSA在训练长度保持质量中性，扩展长度提升质量，提供高效可扩展的长视频生成方案。
---

## 摘要
密集自注意力是长视频扩散推理的计算和质量瓶颈：计算成本随序列长度呈二次增长，并且超出训练时域后模型会收敛到近乎静态的输出，即“冻结”的重复视频。现有方法要么成本过高（如需要重新训练），要么无法以可扩展的方式同时满足性能和质量目标。为此，我们提出了长视频稀疏注意力（LVSA），一种无需训练、与模型无关的视频扩散Transformer块稀疏注意力方法，它将结构化的窗口模式与旋转全局锚点相结合，从而消除了导致长距离时间伪影的固定网格偏差。LVSA结合FlashInfer内核，在Wan 2.1 1.3B上（6倍时域）相比密集注意力可减少高达3.17倍的计算量，在Wan 2.1 14B上（6倍时域）减少2.98倍，在HunyuanVideo 1.5上（1.5倍时域）减少3.33倍。除了减少计算量，LVSA还能在2倍时域下生成HunyuanVideo 1.5，而原先在单个GPU上会内存溢出。此外，在Wan 2.1 1.3B上，LVSA相比RIFLEx可提供高达2.41倍的加速，相比UltraViCo可提供3.27倍的加速。为了展示在不同平台上的适用性，我们在NPU上应用LVSA，在Wan 2.2 A14B上相比密集注意力获得高达2.71倍的加速，在Wan 2.1 1.3B上获得3.24倍的加速。为了公平地评估质量，我们引入了VQeval，一个能正确评分循环视频失败的工具，而这类失败在VBench-Long等先进评估工具中反而会被奖励。LVSA在训练时域长度下生成时保持质量中性，在扩展长度下质量更优。

## Abstract
Dense self-attention is the compute and quality bottleneck of long-video diffusion inference: cost grows quadratically with the sequence length, and beyond the training horizon the model converges to near-static output, that is, "frozen" repetitive video. State of the art approaches are either too costly, e.g., they require retraining, or fail to satisfy both performance and quality objectives in a scalable manner. To this end, we introduce Long Video Sparse Attention (LVSA), a training-free model-agnostic block-sparse attention for video diffusion transformers that combines a structured window pattern with rotating global anchors, thus removing the fixed-grid bias which causes long-range temporal artifacts. LVSA, combined with a FlashInfer kernel, reduces compute up to 3.17x on Wan 2.1 1.3B at a 6x horizon, 2.98x on Wan 2.1 14B at a 6x horizon, and 3.33x on HunyuanVideo 1.5 at a 1.5x horizon, compared to dense attention. Beyond reducing compute, LVSA enables HunyuanVideo 1.5 generation at a 2x horizon, which is otherwise out-of-memory on a single GPU. Moreover, LVSA provides speedups up to 2.41x compared to RIFLEx and 3.27x compared to UltraViCo on Wan 2.1 1.3B. To demonstrate applicability across diverse platforms, we apply LVSA on NPUs and achieve speedups up to 2.71x on Wan 2.2 A14B and 3.24x on Wan 2.1 1.3B compared to dense attention. To evaluate quality in a fair way, we introduce VQeval, a tool properly scoring loopy video failures, which instead are rewarded in state of the art evaluators like VBench-Long. LVSA is quality-neutral for generation at training horizon length and quality-positive at extended lengths.

---

## 论文详细总结（自动生成）

# LVSA: 无需训练的长视频扩散稀疏注意力 — 详细中文总结

## 1. 核心问题与整体含义（研究动机和背景）

- **问题**：视频扩散Transformer（如Wan、HunyuanVideo）中的密集自注意力计算量随帧数二次增长，导致长视频生成计算成本极高；同时，超出训练时域（如Wan的81帧、HunyuanVideo的129帧）后，模型会生成近乎静态或重复的“冻结”视频，质量严重下降。
- **现有方法不足**：一些方法尝试通过训练无关的块稀疏模式（如Sparse VideoGen、AdaSpa、Radial Attention）降低计算，但无法消除长时间重复伪影；另一些方法（如RIFLEx、UltraViCo）虽能保持质量，却未降低计算开销。
- **本文贡献**：提出LVSA（Long Video Sparse Attention），一种无需训练、模型无关的稀疏注意力机制，在降低计算的同时提升长视频生成质量，并引入专门惩罚循环视频的评估工具VQeval。

## 2. 提出的方法论：核心思想、关键技术细节

### 核心思想
- 将每个查询帧的注意力范围限制为 **局部窗口 + 全局锚点** 的组合，保持总注意力预算 \(C\) 近似等于模型训练时的帧数（如Wan为21帧），从而将注意力复杂度从 \(O(N^2d)\) 降至 \(O(TCP^2d)\)（线性于帧数 \(T\)）。

### 关键技术细节
1. **LVSA注意力模式定义**（Definition 2）：
   - 全局锚点集合 \(G = \{t \mid t \mod T_{\text{per}} = 0\}\)（周期性等间距，包含帧0）。
   - 局部窗口 \(W(t)\)：以帧 \(t\) 为中心，宽度 \(2W+1\)，并处理边界情况。
   - 最终注意力集 \(A(t) = G \cup W(t)\)。

2. **扩展窗口算法**（Algorithm 1）：
   - 为避免全局锚点与窗口帧重叠导致预算浪费，动态扩展窗口两侧，使每个查询帧的独立注意力帧数量保持一致（\(|A(t)| \approx C\)）。

3. **旋转周期性全局锚点**（Section 2）：
   - 在每个去噪步骤 \(s\)，全局锚点集合 \(G_s\) 按步骤偏移：\(G_s = \{(s \bmod T_{\text{per}} + i\cdot T_{\text{per}}) \bmod T \mid i = 0,1,\dots\}\)。
   - 目的：消除固定网格偏差，使每个帧在 \(T_{\text{per}}\) 步内至少成为一次全局锚点，防止中间帧信息贫乏。

4. **计算复杂度**：每个查询帧的注意力预算固定为 \(C\)，总复杂度 \(O(TC P^2 d)\)，线性于帧数 \(T\)。

## 3. 实验设计：数据集/场景、Benchmark、对比方法

### 模型与配置
- **模型**：
  - Wan 2.1 T2V-1.3B（单流、1D RoPE、T5编码器，40步）
  - Wan 2.1 T2V-14B（同架构，14B参数）
  - HunyuanVideo 1.5（双流、3D RoPE、Qwen2.5-VL编码器，50步）
- **分辨率**：480×832（主实验），NPU实验还包括720×1280。
- **视频长度**：Wan：81帧（1×）至481帧（6×）；HunyuanVideo：65帧（0.5×）至257帧（2×）。
- **提示词**：5条长描述提示（约500 tokens），固定种子16，CFG=5.0。

### 评估指标
- **VBench-Long**：含主题一致性、时间闪烁、运动平滑、背景一致性、图像质量（奖励静态视频）。
- **VQeval**（自定义）：动态质量、循环质量、文本对齐，专门惩罚冻结/循环失败模式。

### 对比方法
- 密集注意力（Dense）
- LVSA（SDPA后端 / FlashInfer后端）
- RIFLEx：修改单一RoPE频率
- UltraViCo：逐对logit衰减（使用SageAttention核）

## 4. 资源与算力

- **GPU实验**：单张80GB NVIDIA GPU（PyTorch 2.8 / CUDA 12.8）。
- **NPU实验**：vLLM-Omni框架下的NPU集群：
  - Wan 2.1 1.3B：单NPU，40步
  - Wan 2.2 A14B：8 NPU，Ulysses序列并行，40步
- **训练时长**：本文方法**无需训练**，仅推理阶段应用稀疏模式；旋转锚点等操作在CPU上耗时<200μs每步，可忽略。

## 5. 实验数量与充分性

- **主要实验组**：
  1. **跨模型计算效率**：3个模型 × 4~5个时长（总计约15组），每个单元格5条提示词的均值±标准差。
  2. **质量对比**：同上，报告VQeval和VBench-Long，并可视化冻结视频案例（图5）。
  3. **与SOTA对比**：Wan 1.3B上3个倍数（2×、3×、4×），对比Dense、RIFLEx、UltraViCo，含延迟和质量。
  4. **NPU实验**：2个模型 × 2个分辨率 × 3个时长。
  5. **消融**：旋转锚点效果、窗口大小影响（未展示但提及）。
- **充分性评价**：
  - 覆盖多种架构（单流/双流、1D/3D RoPE、不同编码器），不同硬件（GPU/NPU）。
  - 对比方法覆盖当前最相关训练无关方法，且补充了正交组合实验（LVSA+RIFLEx）。
  - 引入自定义VQeval解决VBench奖励静态的偏置，评估更公平。
  - **不足**：实验提示词仅5条（种子固定），可能不够多样；未在更多模型（如CogVideoX）上验证；消融实验（如窗口宽度W、全局周期Tper的选择）未系统展示。

## 6. 主要结论与发现

- **计算效率**：
  - 最长测试时域下，LVSA+FlashInfer加速比：Wan 1.3B 3.17×、Wan 14B 2.98×、HunyuanVideo 1.5 3.33×。
  - 使HunyuanVideo 1.5在2×时域（257帧）变得可行（密集注意力OOM），峰值内存仅~60 GB。
  - 相比RIFLEx加速2.41×，相比UltraViCo加速3.27×（Wan 1.3B）。
- **视频质量**：
  - 训练时域长度（1×）下，LVSA与密集注意力质量持平（VQeval偏差<±1分）。
  - 扩展时域下，LVSA显著优于密集（VQeval差值随时长增大，如Wan 1.3B 6×时+12.1分）。
  - VBench-Long因奖励静态而错误地提升密集得分，VQeval正确反映运动退化。
- **与SOTA对比**：
  - LVSA在质量上全面优于RIFLEx和UltraViCo（VQeval领先1.7~11.2分）。
  - LVSA是唯一同时降低计算和提升质量的方法。
- **NPU适用性**：在NPU上获得2.17×~3.24×加速，质量正向。

## 7. 优点

1. **无需训练**：即插即用，适用于任意视频DiT。
2. **同时提升计算和长视频质量**：解决了密集注意力的双重瓶颈。
3. **旋转锚点设计巧妙**：消除固定网格偏差，使所有帧公平参与全局信息交换。
4. **扩展窗口算法**：保持均匀注意力预算，计算可控且简单。
5. **自定义VQeval**：纠正了VBench奖励静态的偏置，更客观。
6. **跨平台验证**：GPU和NPU上均取得显著加速，工程实践性强。
7. **与现有方法正交**：可与RIFLEx等组合使用，无冲突。

## 8. 不足与局限

1. **实验覆盖有限**：仅测试5条提示词（种子固定），未见随机种子或更多样化场景验证。
2. **消融不完整**：窗口大小 \(W\) 和全局周期 \(T_{\text{per}}\) 的选择依据未系统展示（仅提“已微调”），缺乏敏感性分析。
3. **单场景假设**：论文明确指出“考虑单场景”，未涉及多场景（如镜头切换）情况。
4. **硬件限制**：HunyuanVideo 1.5双流架构导致2×时OOM，但该限制可能因架构优化而缓解，不完全是稀疏性的问题。
5. **评估偏置**：VQeval是自定义指标，其有效性需更多第三方验证；且本文未提供VQeval与人类评价的相关性分析。
6. **代码和可复现性**：提供了GitHub链接，但部分实验（如NPU上的LVS实现）依赖特定框架（vLLM-Omni），移植成本可能较高。

（完）
