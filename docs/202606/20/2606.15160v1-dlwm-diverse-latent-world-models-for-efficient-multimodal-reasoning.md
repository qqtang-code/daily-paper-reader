---
title: "DLWM: Diverse Latent World Models for Efficient Multimodal Reasoning"
title_zh: DLWM：面向高效多模态推理的多样化潜在世界模型
authors: "David Huang, Lianlei Shan"
date: 2026-06-13
pdf: "https://arxiv.org/pdf/2606.15160v1"
tags: ["query:mm-cot"]
score: 8.0
evidence: 多样潜在世界模型用于高效多模态推理
tldr: "多模态大语言模型中，现有推理方法通常假设输入只有一种潜在解释，并使用固定路径和统一计算预算，难以处理真实世界视觉输入的歧义性和多解释性。DLWM框架在连续潜在空间中构建一组多样化的潜在世界假设，通过正交性正则化保持多样性，并在每个假设上独立进行潜在推理。同时，它将推理过程建模为资源约束的序贯决策问题，利用强化学习动态分配计算资源，灵活决定扩展、终止或合并推理路径。在多个多模态推理基准上，DLWM准确率提升2-5%，内存占用减少24%，实现了更高效、更鲁棒的多模态推理。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.15160v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1443, \"height\": 706}, {\"url\": \"assets/figures/arxiv/2606.15160v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1445, \"height\": 711}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.15160v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 709, \"height\": 354}, {\"url\": \"assets/tables/arxiv/2606.15160v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 706, \"height\": 355}, {\"url\": \"assets/tables/arxiv/2606.15160v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1184, \"height\": 311}, {\"url\": \"assets/tables/arxiv/2606.15160v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 716, \"height\": 267}, {\"url\": \"assets/tables/arxiv/2606.15160v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 817, \"height\": 268}, {\"url\": \"assets/tables/arxiv/2606.15160v1/table-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 1217, \"height\": 277}, {\"url\": \"assets/tables/arxiv/2606.15160v1/table-007.webp\", \"caption\": \"\", \"page\": 0, \"index\": 7, \"width\": 1020, \"height\": 241}, {\"url\": \"assets/tables/arxiv/2606.15160v1/table-008.webp\", \"caption\": \"\", \"page\": 0, \"index\": 8, \"width\": 693, \"height\": 230}, {\"url\": \"assets/tables/arxiv/2606.15160v1/table-009.webp\", \"caption\": \"\", \"page\": 0, \"index\": 9, \"width\": 640, \"height\": 233}]"
motivation: 现有方法假设单一潜在解释，推理路径固定、预算统一，难以应对视觉输入的歧义性，且计算资源利用率低。
method: 在连续潜在空间中构建多样化的潜在世界假设，通过正交性正则化保持多样性，并采用资源感知强化学习策略动态分配计算资源。
result: "在多个多模态推理基准上，DLWM准确率提升2-5%，内存占用减少24%。"
conclusion: DLWM通过多样性潜在世界假设和资源感知自适应计算分配，实现了更高效、更鲁棒的多模态推理。
---

## 摘要
近年来，多模态大语言模型（MLLMs）的推理能力有了显著提升。现有方法通常依赖显式的思维链或连续的潜在空间轨迹来增强多步推理。然而，这些方法通常假设输入具有单一的潜在解释，并沿着固定路径或在统一的计算预算下展开推理。在现实世界的多模态场景中，视觉观察常常受到遮挡、模糊、视角变化或语义歧义的影响，从而产生多种可能的解释。统一的推理策略不仅限制了模型探索多个假设的能力，还会导致高内存使用和推理成本。我们提出了DLWM（多样化潜在世界模型），一种结合潜在空间推理与强化学习的多模态推理框架。首先，我们在连续的潜在空间中构建一组多样化的潜在世界假设，每个假设捕捉视觉输入的不同合理解释，并在每个假设上独立展开潜在推理。基于正交性的多样性正则化器明确防止假设坍塌。其次，我们将潜在推理过程形式化为一个资源受限的序列决策问题，并引入一种资源感知的强化学习策略，该策略自适应地在不同假设间分配计算资源，动态决定是扩展、终止还是合并推理路径，从而显著减少内存占用并提高推理效率。在多个多模态推理基准上的实验表明，DLWM在准确率上比现有方法高出2-5个百分点，同时内存使用减少24%。

## Abstract
Reasoning capabilities of multimodal large language models (MLLMs) have improved considerably in recent years. Existing approaches typically rely on explicit chain-of-thought or continuous latent-space trajectories to enhance multi-step reasoning. However, these methods generally assume that an input admits a single latent interpretation and unfold reasoning along a fixed path or under a uniform computation budget. In real-world multimodal settings, visual observations are often subject to occlusion, blur, viewpoint variation, or semantic ambiguity, giving rise to multiple plausible interpretations. A uniform reasoning strategy not only limits the model's ability to explore multiple hypotheses but also incurs high memory usage and rollout cost. We present DLWM (Diverse Latent World Models), a multimodal reasoning framework that combines latent-space reasoning with reinforcement learning. First, we construct a set of diverse latent world hypotheses in continuous latent space, each capturing a different plausible interpretation of the visual input, and unfold latent reasoning independently on each hypothesis. An orthogonality-based diversity regularizer explicitly prevents hypothesis collapse. Second, we formulate the latent reasoning process as a resource-constrained sequential decision problem and introduce a resource-aware reinforcement learning policy that adaptively allocates computation across hypotheses, dynamically deciding whether to expand, terminate, or merge reasoning paths, thereby substantially reducing memory footprint and improving rollout efficiency. Experiments on multiple multimodal reasoning benchmarks demonstrate that DLWM outperforms existing methods by 2-5 points in accuracy while reducing memory usage by 24%.

---

## 论文详细总结（自动生成）

### 论文核心问题与整体含义（研究动机和背景）

- **核心问题**：现有多模态推理方法（如显式思维链或连续潜在空间推理）通常假设输入只有一种潜在解释，并沿着单一固定路径或在统一计算预算下展开推理。然而，真实世界的视觉输入常存在遮挡、模糊、视角变化或语义歧义，导致多个合理解释。这种单一假设机制限制了模型探索多种假设的能力，且固定计算预算导致高内存和低效率。
- **整体含义**：论文提出 **DLWM（Diverse Latent World Models）**，旨在通过显式建模多个潜在解释并动态分配计算资源，提升多模态推理的鲁棒性和效率。

### 论文提出的方法论

- **核心思想**：在连续潜在空间中构建一组多样化的潜在世界假设，每个假设代表输入的一个合理解译，并在每个假设上独立进行潜在推理。同时将潜推理过程建模为资源受限的序贯决策问题，利用强化学习（RL）自适应分配计算资源。
- **关键技术细节**：
    1. **多样化潜在世界生成**：
        - 使用预训练视觉-语言编码器得到联合表示 \( z \)。
        - 通过生成器 \( G_\phi \) 引入可学习世界嵌入 \( e_i \)，生成 \( K \) 个世界表示 \( w_i = G_\phi(z, e_i) \)。
        - 每个世界初始化独立推理轨迹：\( h_0^{(i)} = w_i \)，共享推理模块 \( f_\theta \) 更新：\( h_{t+1}^{(i)} = f_\theta(h_t^{(i)}) \)。
        - 多样性正则化：使用正交损失 \( \mathcal{L}_{\text{orth}} = \sum_{i \neq j} \left( \frac{w_i^\top w_j}{\|w_i\|\|w_j\|} \right)^2 \) 防止表示坍塌。
    2. **资源感知RL计算分配**：
        - 状态 \( s_t \) 包含活跃世界表示和剩余预算 \( B_t \)，编码为平均池化活跃世界与归一化剩余预算的拼接。
        - 动作空间：`Expand(i)`（扩展一步，成本 \( c_{\text{exp}} \)）、`Stop(i)`（终止路径，成本 \( c_{\text{stop}} \)）、`Merge(i,j)`（合并两个世界，线性插值，成本 \( c_{\text{merge}} \)）、`Answer`（终止推理，成本0）。
        - 奖励函数：\( R = R_{\text{acc}} - \lambda_T T - \lambda_K \bar{K} \)，鼓励准确并惩罚步数和平均活跃世界数。
        - 策略优化：使用REINFORCE与移动平均基线，优化目标 \( J(\omega) = \mathbb{E}_{\pi_\omega}[R] \)。
    3. **训练流程**（Algorithm 1）：
        - 两阶段训练：先监督预训练（固定rollout长度），再RL微调。
        - 联合优化生成器、推理模块、评分头：\( \mathcal{L}_{\text{task}} + \lambda_{\text{orth}} \mathcal{L}_{\text{orth}} \)；策略网络单独通过策略梯度更新。
    4. **最终预测**：对活跃世界进行加权融合，权重由评分网络 \( s_\psi \) 给出：\( p(y|I,q) = \sum_{i \in A} \alpha_i p_i(y) \)，其中 \( \alpha_i = \text{softmax}(s_\psi(h_T^{(i)})) \)。

### 实验设计

- **数据集**：四个多模态推理基准：
    - **MMVP**：视觉细粒度感知与空间推理（强调视觉模糊）。
    - **GQA**：基于真实图像的组合与多跳关系推理。
    - **VQAv2**：多样化日常场景，测试视觉-语言融合。
    - **ScienceQA**：跨模态科学知识推理。
- **基准方法**：对比 CoT（显式思维链）、Coconut、CoLaR（文本领域潜在推理）、LVR、LaRe、Heima（多模态潜在推理）、Soft Thinking（推理时潜在控制）。所有方法使用相同骨干网络和最大计算预算。
- **评估指标**：准确率（主指标）、平均推理步数、平均活跃世界数、GPU内存、推理吞吐量。

### 资源与算力

- 论文明确使用 **8块 NVIDIA A100 GPU**，混合精度训练，AdamW优化器，学习率2×10⁻⁵，batch size 64。
- **未明确训练总时长**，但提到采用两阶段训练（监督预训练 → RL微调），未给出具体步数或时间。
- 后备注：使用相同训练步骤数与基线对比，但未量化。

### 实验数量与充分性

- **主实验**（表1a/1b）：在四个数据集上与七个基线对比准确率和效率。
- **消融实验**（表2-4及附录）：
    - 组件消融（多世界、正交正则、RL分配、合并、停止等）。
    - 世界数K从1到8（表3a）。
    - 计算预算B从4到12（表3b）。
    - 动作空间设计（表4）。
    - 训练策略对比（附录表5）：联合训练、仅监督、仅RL、两阶段。
    - 聚合方法对比（附录表6左）和多样性正则化对比（附录表6右）。
    - 重复多次随机种子并报告均值与标准差（附录B.1.3）。
- **充分性与公平性**：所有基线使用相同骨干、相同预算、相同评估脚本；设置统一随机种子、预处理、分词、答案归一化；效率指标在同一硬件环境测量。消融覆盖关键设计维度，实验数量充足，结论可信。

### 论文的主要结论与发现

1. **准确率显著提升**：DLWM在所有基准上达到最高准确率，比最强基线（LVR/LaRe）高出2.4–5.2个百分点；尤其在视觉模糊主导的MMVP上提升最大（+5.2），验证了多假设建模对处理歧义的有效性。
2. **效率保持竞争力**：尽管有多世界，但RL控制器使平均活跃世界仅2.2，平均推理步数5.0，GPU内存14.5GB，与高效方法Heima相当但准确率更高。相比固定rollout方法内存减少约24%。
3. **组件重要性**：多世界建模、正交正则化、RL动态分配、合并与停止动作均为关键贡献，缺一都会导致性能或效率下降。
4. **泛化能力**：在组合推理（GQA）、跨模态科学（ScienceQA）、开放域视觉问答（VQAv2）上均优于现有方法，说明框架对不同类型多模态推理任务有效。

### 优点

- **方法创新**：首次在连续潜在空间中显式构建多个语义不同的潜在世界假设，并通过正交正则保持多样性，而非隐式或简单叠加。
- **动态计算分配**：将潜推理建模为资源受限MDP，利用RL自适应执行扩展、终止、合并等结构化决策，统一多假设探索与效率优化。
- **实验充分且公平**：覆盖多种基准、详尽的消融分析、统一设置、统计报告，消融实验验证了每个设计点的贡献。
- **性能-效率平衡**：在提高准确率的同时，通过智能路径管理实现与轻量方法相近的计算开销，具有实用价值。

### 不足与局限

- **计算开销仍存在**：多世界生成和并行推理仍需额外资源，虽经RL缓解，但可能不适用于极端资源受限或实时性要求极高的场景。
- **场景限制**：仅在静态图像任务上验证，未评估视频理解、交互式推理等动态复杂任务。
- **RL训练稳定性**：强化学习部分仍存在训练不稳定、超参数敏感的问题（文中已提及），可能影响可复现性和部署难度。
- **规模扩展**：未在更大规模模型（如数十亿参数MLLM）上测试，多世界机制在超大规模下的扩展性未知。
- **可解释性风险**：RL策略可能引入不透明决策行为，在医疗、自动驾驶等高安全场景需额外谨慎和可解释性设计。

（完）
