---
title: "PersistentKV: Page-Aware Decode Scheduling for Long-Context LLM Serving on Commodity GPUs"
title_zh: "PersistentKV: 面向商品GPU长上下文LLM服务的页面感知解码调度"
authors: Muhammad Ahmed
date: 2026-06-25
pdf: "https://arxiv.org/pdf/2606.26666v1"
tags: ["query:sparse-attn"]
score: 9.0
evidence: 用于长上下文LLM服务的KV缓存移动减负与页感知调度
tldr: 长上下文LLM服务受限于KV缓存移动而非计算，现有paged-attention系统解码调度欠佳。提出PersistentKV，采用页感知调度和原生块表解码注意力引擎，通过KV-head分组、瓦片重用及紧凑工作队列优化。在RTX 3060上，自适应策略在多种负载下吞吐量提升1.063-1.399x，避免回归。揭示了工作分配是服务系统关键变量。
source: arxiv
selection_source: fresh_fetch
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.26666v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 854, \"height\": 300, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.26666v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1461, \"height\": 304, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.26666v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1645, \"height\": 265, \"label\": \"Table\"}]"
motivation: 长上下文LLM服务中，KV缓存移动成为瓶颈，现有paged-attention解码调度未能充分利用GPU资源。
method: 提出PersistentKV，包含原生块表解码注意力引擎和页感知调度，通过KV-head分组、瓦片重用及紧凑工作队列实现高效任务分配。
result: 在RTX 3060上，自适应策略在B1长上下文步骤中吞吐量提升1.399x，在B8混合负载中提升1.063-1.265x，并避免回归。
conclusion: 工作分配比注意力数学更重要，自适应页感知调度是长上下文服务的关键系统变量。
---

## 摘要
自回归大语言模型（LLM）服务日益受到键值（KV）缓存移动而非密集矩阵乘法的限制。现代分页注意力系统减少了KV缓存碎片，而FlashInfer等成熟内核提供了高度优化的原生分页解码注意力。然而，最佳的单核实现并不总是最佳的服务调度：低活跃度的长上下文解码可能未充分利用商品GPU，而混合序列长度则在多精确长度启动与粗糙填充批处理之间引入矛盾。我们提出PersistentKV，一个原生的块表解码注意力引擎以及针对分组查询注意力（GQA）的页面感知调度研究。PersistentKV按KV头组映射工作，设计用于在分组查询头间重用K、V块，支持原生页表，并添加紧凑的工作队列调度，仅执行非空行-KV头-序列分割任务。在RTX 3060上，使用FP16、页大小16、Hq=32、Hkv=8、d=128，且与FlashInfer具有相同的正确性容差下，一个校准的自适应策略为小活跃批量选择FlashInfer，为B1长上下文步骤选择PersistentKV序列分割，为B8长上下文步骤选择PersistentKV工作队列调度。在固定校准轨迹上的阈值和分割计数下，一个保留的轨迹种子在B8双峰、均匀和Zipf类工作负载上将同步墙吞吐量提升了1.063-1.265倍，在B1分桶轨迹上提升了1.399倍。在B4双峰边界情况下，该策略通过选择FlashInfer避免了PersistentKV的回归。这些结果识别了自适应页面感知解码调度的具体系统生态位，并表明工作分配（而不仅仅是注意力数学）是一个决定性的服务系统变量。

## Abstract
Autoregressive large language model (LLM) serving is increasingly limited by key-value (KV) cache movement rather than dense matrix multiplication. Modern paged-attention systems reduce KV-cache fragmentation and mature kernels such as FlashInfer provide highly optimized native-paged decode attention. However, the best single-kernel implementation is not always the best serving schedule: low-active long-context decode can under-utilize commodity GPUs, while mixed sequence lengths introduce a tension between many exact-length launches and coarse padded batches. We present PersistentKV, a native block-table decode attention engine and page-aware scheduling study for grouped-query attention (GQA). PersistentKV maps work by KV-head group, is designed to reuse K,V tiles across grouped query heads, supports native page tables, and adds a compact workqueue schedule that executes only non-empty row-KV-head-sequence-split tasks. On an RTX 3060 with FP16, page size 16, Hq=32, Hkv=8, d=128, and identical correctness tolerance against FlashInfer, a calibrated adaptive policy selects FlashInfer for small active batches, PersistentKV sequence splitting for B1 long-context steps, and PersistentKV workqueue scheduling for B8 long-context steps. With thresholds and split counts fixed on calibration traces, one held-out trace seed improves synchronized wall throughput by 1.063-1.265x on B8 bimodal, uniform, and Zipf-like workloads and by 1.399x on a B1 bucketed trace. On the B4 bimodal boundary case, the policy avoids the PersistentKV regression by selecting FlashInfer. These results identify a concrete systems niche for adaptive page-aware decode scheduling and show that work assignment, not only attention math, is a decisive serving-system variable.

---

## 论文详细总结（自动生成）

# PersistentKV: 面向商品GPU长上下文LLM服务的页面感知解码调度

## 1. 论文的核心问题与整体含义（研究动机和背景）

- **核心问题**：长上下文LLM服务中，KV缓存移动（而非密集矩阵乘法）已成为主要瓶颈。现有paged-attention系统（如FlashInfer）虽提供高度优化的单核实现，但**最佳单核不等于最佳服务调度**：低活跃度（小批量）长上下文解码导致GPU利用率不足，而混合序列长度迫使系统在“精确长度多次启动”与“粗糙填充大批次”之间权衡。
- **整体含义**：工作分配（work assignment）与调度策略是决定服务性能的关键变量，而非仅有注意力数学计算。通过页面感知的调度，可以在商品GPU上显著提升长上下文解码吞吐量。

## 2. 论文提出的方法论：核心思想、关键技术细节

### 核心思想
- **PersistentKV**：一个**原生块表解码注意力引擎**，直接操作paged缓存布局中的K/V页，无需重排为连续张量。通过**KV头分组（GQA）映射线程块**，实现瓦片重用；并引入**紧凑工作队列调度**，仅执行非空任务，避免填充浪费。

### 关键技术细节
1. **原生块表执行**：KV页存储为 `[physical_page, Hkv, page_size, d]`，请求持有块表（映射逻辑页→物理页）。解码循环每次处理32个token的瓦片，使用异步拷贝和精确在线softmax更新（m, ℓ, acc）。
2. **序列分割与行长局部边界**：对于低活跃度解码，将序列维度分割为S个不相交范围，每个范围计算部分softmax状态，再通过合并核精确组合。分割边界使用每行真实长度 `N_i^*`，而非桶长度，避免填充读取。
3. **紧凑工作队列调度**：针对长度桶调度在高度参差不齐批次中的启动膨胀问题，为每个非空 `(row, kvh, split)` 区间发射一个任务，CUDA网格为一维任务列表。合并核在行局部范围内执行分段缩减。
4. **校准自适应策略**：基于手动校准的阈值：
   - B≤1：使用PersistentKV长度桶（需序列分割）
   - B≥8 且 Nmax ≥16K：使用PersistentKV工作队列
   - 其他情况（如B4）：回退至FlashInfer，避免回归

## 3. 实验设计

### 所用场景/数据集
- **合成轨迹（Synthetic Traces）**：设计多种负载以隔离不同因素：
  - **分桶轨迹**：提示长度循环为8K、16K、32K、64K，模拟长上下文交互会话。
  - **同质轨迹**：固定32K提示长度，独立解码长度，隔离活跃批量大小影响。
  - **双峰轨迹**：短/长提示混合（每3个短请求配1个长请求），对长度桶调度最具挑战性。
  - **均匀与Zipf轨迹**：进一步测试长度分布的宽尾情况。

### Benchmark与对比方法
- **基线**：
  - FlashInfer 0.2.5（主要原生分页基线）
  - vLLM 0.6.4.post1
  - TensorRT-LLM 0.8 MMHA
  - Repack + PyTorch SDPA
- **对比条件**：
  - 相同页面布局、GQA形状（Hq=32, Hkv=8, G=4, d=128, FP16）、页大小16
  - 相同正确性容差（max|e|<2e-3, mean|e|<3e-4）
  - 使用校准/保留种子协议：阈值在种子20260622上选择，结果在种子20260623上报告。

## 4. 资源与算力

- **GPU型号**：NVIDIA RTX 3060（12 GB VRAM，28个SM，约302 GB/s流读取带宽）
- **软件环境**：CUDA 12.1，PyTorch 2.5.1，FlashInfer 0.2.5
- **未明确说明**：未提及训练时长、多机多卡设置或更广泛GPU型号（如A100、H100）的实验。论文明确限定为“商品GPU”场景，并指出不同代GPU需重新调参。

## 5. 实验数量与充分性

### 实验数量
- **孤立核基线测试**：5种方法在3种上下文长度（8K、32K、64K）下的性能对比（表1）。
- **主要服务结果**（表2）：5种轨迹（分桶B1、双峰B8、均匀B8、Zipf B8、双峰B4）下的自适应策略 vs. FlashInfer，报告GPU时间比、墙时间比及错误。
- **调度器导出计数器**（表3）：针对双峰B8轨迹的启动次数、合并流量等结构指标。
- **消融实验**：包括参差不齐性消融（长度桶→工作队列）、实现消融（索引Q/O、行长局部边界、零瓦片提前退出、紧凑工作队列）、CUDA图重放、融合合并等。

### 充分性与公平性
- **充分**：覆盖从单请求到B8的多场景，以及从精确桶到工作队列的调度演进，消融实验全面。
- **客观公平**：
  - 使用相同页面分配、相同序列长度、相同数值容差。
  - 校准/保留协议避免过拟合报告结果。
  - 合成轨迹虽然可控，但作者承认不替代生产流量。
- **局限**：仅一个保留种子；单GPU型号；缺少Nsight硬件计数器（权限限制）；GQA比率仅测试G=4。

## 6. 论文的主要结论与发现

- **自适应策略在B1长上下文解码中实现1.399×墙吞吐提升**；在B8双峰/均匀/Zipf负载中实现1.063–1.265×提升。
- **B4边界情况下，自适应策略选择FlashInfer，避免了PersistentKV的回归**（因两核启动和合并开销无法摊销）。
- **工作队列调度将每步启动次数从16.00降至2.00**，合并流量从4.06 MB/步降至2.54 MB/步。
- **结论**：工作分配是决定性系统变量，页面感知调度可在特定长上下文服务场景中超越强基线FlashInfer。

## 7. 优点

1. **方法原创性**：提出原生块表解码注意力引擎与紧凑工作队列，结合序列分割和行长局部边界，解决参差不齐批次调度难题。
2. **实验严谨性**：
   - 校准/保留协议避免数据泄露。
   - 多维度消融实验（路由、分割、CUDA图、融合合并）清晰展示了各组件贡献。
   - 正确性验证严格（相同误差容差）。
3. **清晰的问题定位**：指出“最佳单核 ≠ 最佳调度”这一系统级洞察。
4. **代码与可复现性**：提供合成轨迹生成、详细测量协议，便于复现。

## 8. 不足与局限

1. **实验覆盖有限**：
   - 仅使用RTX 3060单GPU，未在A100/H100等数据中心级GPU上验证。
   - 仅测试G=4的GQA比率，未扫描G∈{1,4,8}。
   - 仅使用一个保留种子，缺乏统计置信区间。
2. **生产环境差距**：
   - 使用合成轨迹而非真实生产请求迹线。
   - 测量仅限解码循环，未包含MLP层、采样、网络、预填充/解码干扰等完整服务栈。
   - 未集成到完整服务运行时（如vLLM、Triton）。
3. **硬件计数器缺失**：因权限限制无法获取SM占用率、DRAM字节、L2命中率等硬件级计数器，依赖调度器推导的计数器。
4. **策略手动调参**：阈值和分割计数为手动校准，非自学习或在线调优，可迁移性差。
5. **B4场景未解决**：PersistentKV在B4窗口仍为负收益，原子合并方案反而更慢，需要更低开销的合并设计。

（完）
