---
title: "WaveFilter: Enhancing the Long-Context Capability of Diffusion LLMs via Wavelet-Guided KV Cache Filtering"
title_zh: "WaveFilter: 通过小波引导的KV缓存过滤增强扩散LLM的长上下文能力"
authors: "Jinnan Yang, Yan Wang, Zhen Bi, Kehao Wu, Xiaojie Li, Jungang Lou, Zechao Li, Jing Liu"
date: 2026-05-30
pdf: "https://arxiv.org/pdf/2606.00724v1"
tags: ["query:sparse-attn"]
score: 9.0
evidence: 扩散LLM的小波引导KV缓存过滤
tldr: 扩散LLM在多步迭代推理中处理长上下文时计算开销大，现有KV缓存机制导致生成质量下降。受人类阅读启发，提出WaveFilter，即插即用无需训练，通过小波变换分解长序列并精确过滤关键token，构建稀疏KV缓存。实验表明，WaveFilter显著提升现有KV缓存方法在复杂长上下文任务中的性能，有效增强扩散LLM的长上下文能力。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.00724v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 756, \"height\": 807, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.00724v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1597, \"height\": 778, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.00724v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1447, \"height\": 855, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.00724v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1648, \"height\": 678, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.00724v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1650, \"height\": 512, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.00724v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1558, \"height\": 291, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.00724v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1650, \"height\": 926, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.00724v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1647, \"height\": 651, \"label\": \"Table\"}]"
motivation: 扩散LLM长上下文任务因多步迭代推理导致高计算延迟，现有KV缓存机制在长序列中生成质量剧降，亟需精准高效的关键token过滤方法。
method: 提出WaveFilter，创新引入小波变换分解长序列以识别关键token，构建稀疏KV缓存，实现即插即用、无需训练的通用框架。
result: 在复杂长上下文任务中，WaveFilter作为通用框架显著提升现有主流KV缓存方法的性能。
conclusion: WaveFilter是一种通用、无需训练的小波引导KV缓存过滤框架，有效增强扩散LLM的长上下文处理能力。
---

## 摘要
扩散大语言模型（DLMs）在各类任务中展现出显著优势。然而，受其多步迭代推理机制的限制，它们在长上下文任务中的计算开销和推理延迟已成为制约其大规模部署的核心瓶颈。在处理长序列时，现有的键值（KV）缓存机制常面临生成质量急剧下降的困境，其核心挑战在于如何在超长上下文中精确高效地筛选关键令牌。受人类阅读过程的启发，我们提出WaveFilter——一个通用且无需训练的缓存框架。该框架创新性地引入小波变换对长序列进行分解，以实现关键令牌的精确识别，并在此基础上构建稀疏KV缓存来计算最终的上下文表示。实验结果表明，WaveFilter作为一个即插即用的通用框架，显著提升了现有主流KV缓存方法在复杂长上下文任务中的性能。

## Abstract
Diffusion Large Language Models (DLMs) have demonstrated significant advantages across various tasks. However, constrained by their multi-step iterative inference mechanism, their computational overhead and inference latency in long-context tasks have become core bottlenecks restricting their large-scale deployment. When processing long sequences, existing Key-Value (KV) caching mechanisms often face a dilemma where generation quality degrades drastically, where the core challenge lies in precisely and efficiently filtering critical tokens within ultra-long contexts. Inspired by the human reading process, we propose \textbf{WaveFilter}, a universal and training-free caching framework. This framework innovatively introduces the wavelet transform for decomposition of long sequences to achieve precise identification of key tokens, based on which a sparse KV Cache is constructed to compute the final contextual representation. Experimental results demonstrate that WaveFilter, as a plug-and-play generic framework, significantly enhances the performance of existing mainstream KV Cache methods in complex long-context tasks.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义（研究动机和背景）

- **核心问题**：扩散大语言模型（Diffusion LLMs, DLMs）因多步迭代推理机制，在处理长上下文任务时面临巨大的计算开销和推理延迟。现有的Key-Value (KV) 缓存机制虽然能加速推理，但在超长上下文中生成质量急剧下降（如图1a所示），核心瓶颈在于**无法精准、高效地过滤出对去噪过程至关重要的关键token**。
- **整体含义**：为解决这一瓶颈，论文提出一种**通用、无需训练的缓存框架WaveFilter**。它模仿人类“先粗读后精读”的认知过程，利用小波变换分解长序列，快速定位关键语义区域，再通过多尺度递归过滤精确提取关键token，构建稀疏KV缓存。实验表明，WaveFilter可即插即用地增强现有KV缓存方法（如Fast-dLLM、Elastic-Cache）在长上下文任务中的性能，有效缓解精度下降。

### 2. 论文提出的方法论

- **核心思想**：模仿人类“先粗读（skim）后精读（scan）”的阅读习惯。粗读阶段快速构建宏观上下文语义结构；精读阶段针对具体问题精准定位关键信息。
- **关键技术**：
  - **离散小波变换 (DWT)**：用于将缓存键向量分解为低频频近似分量和高频细节分量。低频频分量能有效滤除噪声、保留全局语义轮廓。
  - **粗粒度全局感知**：在初始时间步，对缓存键的prompt部分应用DWT得到低频频分量（式6），然后通过注意力机制计算当前查询向量与压缩后低频频分量的相关性分数（式7），列求和得到重要性向量（式8），选取Top-K候选区域（式9）。
  - **细粒度局部定位（多尺度递归过滤）**：将粗粒度候选区域映射回原始索引空间（式10），提取对应的子序列键向量，再以更高分辨率进行小波变换并重复上述筛选过程（式11）。通过从尺度B递归到尺度1，逐层细化，最终得到最关键的token索引μ0。
  - **稀疏KV缓存构建**：提取μ0对应的稀疏键值矩阵，与当前时间步的生成位置键值拼接，用于后续注意力计算。
- **算法流程**（Algorithm 1）：
  1. 初始化：合并prompt和占位符，保存初始KV缓存。
  2. 对于后续每个时间步，对每个Transformer层：
     - 使用DWT对历史KV缓存进行多尺度分解；
     - 计算查询与低频分量的注意力，按重要性选择候选区域；
     - 多尺度递归过滤直至得到最精确的关键索引μ0；
     - 提取稀疏键值矩阵，计算最终上下文表示。
  3. 基于置信度阈值动态保留高置信度token，掩码其余token，继续下一时间步。

### 3. 实验设计

- **数据集/场景**：
  - **LongBench**：包含单文档QA（Qasper, MultiFieldQA）、多文档QA（HotpotQA, 2WikiMultiHopQA, Musique）、少样本学习（TREC, TriviaQA）、合成任务（PassageCount, PassageRetrieval）、代码生成（LCC）。
  - **Ruler**：涵盖4K和8K两种上下文长度，包括10个子任务：单/双/多值检索、变量追踪、常见词提取、单跳/多跳QA等。
- **Benchmark方法**：
  - 基础模型：LLaDA-8b-Instruct、Dream-v0-Base-7B。
  - 现有KV缓存方法：Fast-dLLM、Elastic-Cache。
  - 增强版本：Fast-dLLM & WaveFilter、Elastic-Cache & WaveFilter。
- **评估指标**：准确率（Accuracy）、吞吐量（Tokens/sec）、相对加速比。同时记录总生成token数和总运行时间。

### 4. 资源与算力

- 论文明确说明：所有实验均在一张**NVIDIA A800 80GB GPU**上完成。未提及GPU数量或具体训练时长（WaveFilter无需训练，仅进行推理评估）。推理时间已在表中给出（分钟级）。

### 5. 实验数量与充分性

- **实验数量**：
  - LongBench：10个子任务 × 2种模型 × 多种方法 = 约40组对比（部分子任务因OOM缺失）。
  - Ruler：10个子任务 × 2种上下文长度（4K/8K）× 多种方法 = 约40组对比。
  - 消融实验：对关键超参数Scale（B=1~4）和Proportion（mB=0.3~0.6）进行了联合热力图分析（图3），涵盖精度、吞吐量、总运行时间三个维度。
  - 额外提供案例定性分析。
- **充分性与公平性**：所有方法在同一硬件环境、同一解码策略（置信度感知解码）下重新运行，消除环境偏差。实验设计覆盖多种长上下文任务，且消融实验深入分析了超参影响。但作者也指出在某些任务（如s1）上吞吐量下降但总时间缩短，说明指标需综合评估。

### 6. 论文的主要结论与发现

- **WaveFilter作为即插即用框架**，能显著提升现有KV缓存方法（Fast-dLLM、Elastic-Cache）在长上下文任务中的准确率，同时保持相当或略低的吞吐量。
- **多尺度递归过滤机制**能精准定位关键token，有效缓解长上下文下的性能退化。
- **消融实验表明**：中等尺度（B=2）和中等比例（mB=0.3~0.4）能最好地平衡精度与效率；过深或过浅的尺度、过大或过小的比例均会损害性能。
- **在Ruler的8K上下文中**，WaveFilter帮助Fast-dLLM和Elastic-Cache恢复精度，如vt任务上从19.08%和25.72%分别提升至21.56%和26.04%。
- **局限性**：在文本摘要（少样本学习）任务上性能下降，因稀疏化可能丢失关键上下文。

### 7. 优点

- **创新性强**：首次将小波变换引入扩散LLM的KV缓存过滤，灵感来自人类阅读认知，方法新颖。
- **实用性强**：无需训练、即插即用，可无缝集成到现有缓存策略，通用性好。
- **实验详尽**：在两个长上下文基准、多个子任务上进行全面对比，消融实验深入分析了超参影响，并提供了案例定性展示。
- **分析客观**：作者主动指出单靠吞吐量评估的不足，并补充总运行时间和生成质量分析，体现科学严谨性。
- **开源友好**：方法与结构清晰，便于复现和扩展。

### 8. 不足与局限

- **评估指标局限**：仅用吞吐量（Tokens/sec）未能完全反映实际效率，作者也承认在部分任务中吞吐下降但总时间缩短、生成质量提升。需结合总运行时间和输出质量进行多维度评估。
- **计算开销**：虽然小波变换压缩了序列，但注意力权重计算仍引入额外成本，导致在部分数据集（如Ruler的s1）上吞吐量明显下降。
- **任务局限性**：在文本摘要等对全局上下文高度依赖的任务中，稀疏化可能误删关键提示信息，导致性能退化。论文仅在少样本学习任务中观察到该问题，未在其他任务中验证。
- **实验覆盖**：仅在两种基础模型（LLaDA-8b-Instruct、Dream-v0-Base-7B）和两个基准上验证，未扩展到更多模型（如更大规模或不同架构的DLLM）或更多长上下文场景（如对话、翻译）。
- **超参敏感性**：比例mB需根据数据集调整（[0.3,1]），虽然给出了默认范围，但未提供自动化选择策略。

（完）
