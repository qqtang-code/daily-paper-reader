---
title: "C$^2$KV: Compressed and Composable KV Cache Reuse for Efficient LLM Inference"
title_zh: C$^2$KV：高效LLM推理中的压缩与可组合KV缓存复用
authors: "Chuheng Du, Junyi Chen, Hanlin Tang, Kan Liu, Tao Lan, Lin Qu, Chaoyue Niu, Shengzhong Liu, Guihai Chen, Fan Wu"
date: 2026-07-20
pdf: "https://arxiv.org/pdf/2607.17715v1"
tags: ["query:sparse-attn"]
score: 9.0
evidence: KV缓存压缩与重用实现高效LLM推理
tldr: 长上下文LLM推理中，KV缓存存储和访问带宽成为系统级主要瓶颈，现有KV重用方法仅优化预填充计算而忽略存储成本。本文C$^2$KV提出统一框架，学习可组合且压缩的KV缓存流形：通过轻量级Extractor和可学习压缩token，在不修改基础模型前提下生成位置无关的模块化KV表示，支持非前缀灵活拼接，并采用压缩-拼接协同训练对齐提取与重用行为。在多个长上下文评测任务及不同模型族上，C$^2$KV实现高达17倍推理加速且生成质量无损，显著降低KV缓存存储与传输开销。该方法首次将KV压缩与重用统一设计，无需改动预训练模型，为长上下文高效推理提供了可复用的实用方案。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.17715v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 828, \"height\": 394, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.17715v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 862, \"height\": 506, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.17715v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 828, \"height\": 318, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.17715v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 847, \"height\": 554, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.17715v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1572, \"height\": 508, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.17715v1/fig-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 749, \"height\": 549, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.17715v1/fig-007.webp\", \"caption\": \"\", \"page\": 0, \"index\": 7, \"width\": 1734, \"height\": 865, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.17715v1/fig-008.webp\", \"caption\": \"\", \"page\": 0, \"index\": 8, \"width\": 868, \"height\": 452, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.17715v1/fig-009.webp\", \"caption\": \"\", \"page\": 0, \"index\": 9, \"width\": 1549, \"height\": 407, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.17715v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 881, \"height\": 502, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.17715v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 848, \"height\": 364, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.17715v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 802, \"height\": 448, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.17715v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1666, \"height\": 282, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.17715v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1752, \"height\": 1386, \"label\": \"Table\"}]"
motivation: 现有KV重用方法仅关注计算节省，忽略存储和传输瓶颈，且简单结合KV压缩会导致严重精度下降，需要统一优化压缩与重用。
method: C$^2$KV学习位置无关的可组合压缩KV流形，利用轻量级Extractor和可学习压缩token生成模块化表示，并通过压缩-拼接协同训练对齐提取与拼接行为。
result: 在多个长上下文基准和模型上，C$^2$KV实现最高17倍推理加速且生成质量无损，大幅降低KV缓存存储与传输开销。
conclusion: C$^2$KV首次联合优化KV缓存压缩与重用，无需修改冻结的基础模型，为高效长上下文推理提供可复用的实用方案。
---

## 摘要
长上下文推理是现代大型语言模型（LLM）应用（如检索增强生成和多文档推理）的核心。为缓解日益增长的推理成本，近期研究探索了键值（KV）缓存复用以减少冗余预填充计算。然而，现有复用方法主要关注计算节省，忽视了长上下文LLM服务中的一个关键瓶颈：存储和访问大型KV缓存的成本。虽然KV压缩似乎是自然的补充，但将压缩与非前缀KV复用简单结合往往会导致严重的精度下降。在本工作中，我们提出C$^2$KV，一个统一框架用于非前缀KV复用，联合优化KV提取和推理时拼接。C$^2$KV学习了一个可组合且压缩的KV缓存流形，其明确设计为位置无关。我们的方法引入了一个轻量级的侧边提取器，带有可学习的压缩标记和结构化注意力流，使得模块化KV表示能够灵活复用和拼接，而无需修改冻结的基础模型。我们进一步采用压缩-拼接联合训练策略，将提取时表示与其下游复用行为对齐。在多个长上下文基准和模型家族上的广泛实验表明，C$^2$KV显著减少了KV缓存存储和传输成本，在长上下文中实现了高达17倍的推理加速，同时保持了生成质量。

## Abstract
Long-context inference is central to modern large language model (LLM) applications such as retrieval-augmented generation and multi-document reasoning. To mitigate the growing inference cost, recent work has explored key-value (KV) cache reuse to reduce redundant prefill computation. However, existing reuse methods primarily focus on computation savings and overlook a critical bottleneck in long-context LLM serving: the cost of storing and accessing large KV caches. While KV compression appears to be a natural complement, naively combining compression with non-prefix KV reuse often leads to severe accuracy degradation. In this work, we propose C$^2$KV, a unified framework for non-prefix KV reuse that jointly optimizes KV extraction and inference-time concatenation. C$^2$KV learns a composable and compressed KV cache manifold that is explicitly designed to be position-agnostic. Our approach introduces a lightweight sidecar Extractor with learnable compression tokens and a structured attention flow, enabling modular KV representations that can be flexibly reused and concatenated without modifying the frozen base model. We further employ a compression-concatenation co-training strategy to align extraction-time representations with their downstream reuse behavior. Extensive experiments across multiple long-context benchmarks and model families demonstrate that C$^2$KV significantly reduces KV cache storage and transfer costs, achieving up to 17$\times$ inference speedup under long contexts, while preserving generation quality.

---

## 论文详细总结（自动生成）

# C²KV: Compressed and Composable KV Cache Reuse for Efficient LLM Inference 论文总结

## 1. 论文的核心问题与整体含义（研究动机和背景）

- **核心问题**：长上下文LLM推理中，KV缓存的**存储与内存带宽**逐渐成为系统级性能瓶颈，而现有KV缓存复用方法（如Prefix缓存、CacheBlend等）仅关注减少预填充计算，忽视了存储和传输成本。此外，简单将KV压缩（如SnapKV）与非前缀KV复用结合会导致严重精度下降，因为压缩后的KV表示通常与原始上下文耦合、不具备可组合性。
- **整体含义**：本文提出C²KV，一个**统一联合优化KV缓存压缩与复用**的框架，使KV表示既紧凑又位置无关，可在非前缀场景下灵活拼接、直接复用，显著降低存储和传输开销，同时保持生成质量。

## 2. 论文提出的方法论：核心思想、关键技术细节

### 核心思想
学习一个**可组合且压缩的KV缓存流形**，该流形是位置无关的（position‑agnostic），允许KV片段在任意位置“即插即用”。

### 关键技术细节

1. **C²Extractor（轻量级提取模块）**：
   - 附加在冻结的基础LLM上，不修改原模型参数。
   - 引入可学习**C²Tokens**作为压缩记忆槽，数量由压缩比k决定（m = ⌈n/k⌉），逻辑上每个C²Token对应一个长度为k的文档token块。
   - **新的QKV投影头**：为C²Tokens单独设计可训练的QKV投影矩阵，输出维度与基础模型匹配，与原始token的QKV拼接后送入标准注意力。
   - **残差均值池化变体**（C²KV-Residual）：在首层将块内文档token的隐藏状态均值加到C²Token状态上，增强局部语义。

2. **结构化信息流（Structured Information Flow, SIF）**：
   - 通过**注意力掩码**严格隔离原始token与C²Token的信息路径：
     - 原始token只能相互注意（标准因果掩码），不能注意任何C²Token，保证原始表示不变。
     - 每个C²Token只能注意其对应块内的原始token和sink块（初始若干token），实现块局部语义提取。
     - C²Tokens之间按因果方式注意前序C²Token，允许累积文档级上下文。
   - 与锚点token压缩方法对比：锚点token使用双向注意力，会修改原始token表示，导致KV表示与上下文耦合；而C²KV是单向、块局部、因果的，保证KV表示上下文独立、可组合。

3. **压缩‑拼接协同训练（Compression-Concatenation Co-Training）**：
   - 训练损失为标准自回归语言建模损失，监督信号来自**拼接后的多文档KV缓存**条件下的答案生成。
   - 每个文档独立提取C²KV，但梯度通过拼接后的联合上下文反向传播，迫使提取器学习对任意组合和顺序都有效的表示，平衡压缩保真度和拼接兼容性。

4. **推理时复用**：
   - 从存储中加载预提取的C²KV，根据目标位置分配新位置编码（RoPE重新旋转），直接拼接，无需任何混合或重计算，即“load‑only”操作。

## 3. 实验设计：数据集、Benchmark、对比方法

- **训练数据**：HotpotQA、2WikiMultiHopQA、LongMagpie三个多文档QA数据集的训练子集，共120k样本。
- **评测基准**：LongBench的7个子任务（GovReport、HotpotQA、MultiNews、MuSiQue、QMSum、SAMSum、2WikiMQA），以及RULER长上下文检索基准。
- **对比方法**：
  - **Full Recompute (FR)**：全量预填充，精度上界。
  - **Naïve Reuse**：直接复用缓存KV不修正。
  - **EPIC**：基于静态模式的择重计算复用。
  - **CacheBlend**：基于值差异的择重计算复用。
  - **Block-Attention**：基于训练的方法（仅Llama3.1-8B有可用检查点）。
  - **FR+SnapKV**：全量预填充+通用压缩（验证压缩与复用不兼容）。
- **模型**：Qwen3-4B-Instruct、Llama3.1-8B-Instruct、Qwen2.5-7B-Instruct，以及Qwen3-14B（扩展实验）。共4个模型族/规模。

## 4. 资源与算力

- 论文**未明确说明**训练使用的具体GPU型号、数量及总训练时长。
- 仅提及训练细节：AdamW优化器，学习率5e-5，预热比0.06，权重衰减0.1，全局batch size 32，梯度累积4步，精度BF16，训练1个epoch。C²Extractor添加的参数量约为基础模型的10%（Qwen3-4B和Llama3.1-8B）。

## 5. 实验数量与充分性

- **实验类型覆盖广泛**：
  - TTFT-精度权衡曲线（7个LongBench任务×3个模型，共21个子图）。
  - 解码阶段时延随上下文长度变化（3个模型，8个长度点）。
  - 压缩比缩放实验（4×/8×/16×固定比，动态比训练）。
  - RULER长上下文检索（2个模型，4k~64k，不同答案深度）。
  - 更大模型Qwen3-14B上的验证。
  - 通用压缩+复用不兼容性实验（SnapKV+各复用方法）。
  - 消融实验（5种设计变体对比）。
- **充分性与公平性**：
  - 对比了最主流的非前缀复用方法（训练免费和训练基础）。
  - 考虑了不同模型族、不同规模、不同压缩比、不同度量（F1/ROUGE-L、TTFT、TBT）。
  - 实验设置（贪婪解码、最大生成长度等）保持一致。
  - 消融实验逐一验证关键设计（SIF、位置重对齐、结构约束等），具有说服力。
- **潜在不足**：缺少与更多近期（2025‑2026）KV压缩方法的对比（如MiniKV、KeyFormer等）；解码时延仅与FR、CacheBlend、Epic对比，未包含其他训练基础方法（如KVLink）。

## 6. 论文的主要结论与发现

1. **存储和带宽是长上下文推理的新瓶颈**，现有方法仅优化计算而忽视该问题。
2. **简单将通用压缩（如SnapKV）与非前缀KV复用结合会导致严重精度下降**（表3），因为标准KV表示缺乏可组合性。
3. **C²KV在TTFT-精度权衡上达到Pareto最优**（图7）：最小化推理启动延迟，同时保持接近全重计算的精度。
4. **解码阶段效率显著提升**：因KV缓存缩小，每token生成时延（TBT）随上下文长度增长曲线趋于平坦（图8）。
5. **支持多种压缩比并保持鲁棒性**：4×、8×、16×压缩比下精度下降平缓；动态比训练可泛化到未见压缩比（10×）。
6. **结构化信息流和压缩-拼接协同训练均为必要组件**（消融实验表4），缺少任一会导致精度大幅下降。
7. **方法可扩展到更大模型**（Qwen3-14B）并保持性能。

## 7. 优点：方法或实验设计上的亮点

- **问题洞见深刻**：明确指出现有KV复用方法忽视了存储/带宽瓶颈，并通过动机实验（图2、图3、表3）定量论证。
- **方法设计精巧且实用**：
  - 保持基础模型冻结，仅增加轻量侧边模块（约10%参数量），避免“灾难性遗忘”和模型定制。
  - 结构化信息流巧妙利用注意力掩码实现压缩与解耦，无需修改基础模型架构。
  - 压缩-拼接协同训练通过端到端的生成损失自然对齐提取与复用行为，无需额外辅助损失。
- **实验全面且公平**：覆盖多模型、多任务、多指标、多压缩比，消融实验逻辑清晰。
- **部署友好**：推理时仅需加载和位置重分配，无混合/重计算，易于集成到现有LLM服务系统。

## 8. 不足与局限

- **实验覆盖方面**：
  - 训练数据仅来自三个多文档QA数据集，未在更多样化的长上下文场景（如代码、金融文档）上验证泛化性。
  - 缺少与更多最新KV压缩方法（如MiniKV、KVQuant、KIVI等）的对比，尤其是它们在复用场景下的表现。
  - 解码时延实验仅与三个方法对比，未包括Block-Attention或其他训练基础方法。
  - RULER实验仅测试了4×压缩比，未探索更高压缩比下的长上下文检索能力。

- **方法局限性**：
  - 假设可复用内容可离线识别和提取，不直接支持在线增量提取（论文在Limitations中承认）。
  - 采用统一压缩比，未实现自适应文档/层级别压缩，可能无法在不同重要性任务间最优分配预算。
  - 结构化信息流的注意力掩码限制了C²Token之间的信息交互（仅因果注意），可能影响需要深层跨段依赖的任务。
  - 需要为每个基础模型训练独立的C²Extractor，虽轻量但仍需一次SFT。

- **偏差风险**：
  - 训练数据来源于英文多文档QA，可能在其他语言或领域存在偏差。
  - 仅评测了QA和摘要任务，未涉及代码生成、数学推理等，对生成质量的影响评估不够全面。

- **资源与算力未说明**：未报告训练耗时和GPU型号，难以评估方法实际资源开销。

（完）
