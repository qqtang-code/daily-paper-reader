---
title: Structured Redundancy Modeling for Efficient Visual Token Pruning in High-Resolution MLLMs
title_zh: 结构化冗余建模：用于高分辨率多模态大语言模型高效视觉词元剪枝
authors: "Jouwon Song, Woohyeong Kim, Kyeongbo Kong"
date: 2026-07-25
pdf: "https://arxiv.org/pdf/2607.23046v1"
tags: ["query:multimodal"]
score: 8.0
evidence: 提出高分辨率多模态大语言模型的视觉标记剪枝方法，减少延迟瓶颈
tldr: 高分辨率多模态大模型产生数千视觉token，导致严重的延迟瓶颈。现有子集优化方法因迭代依赖产生显著选择开销，难以实现实际加速。本文提出SFPruner，通过语义引导岭杠杆和排序定向掩蔽，在单次前向传递中完成冗余感知重要性选择。在Qwen2.5-VL上，512 token的选择时间从112.4ms降至2.5ms，成功将理论FLOPs减少转化为实际推理加速。该方法在激进压缩下仍保持与SOTA相当的性能。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.23046v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1095, \"height\": 805}, {\"url\": \"assets/figures/arxiv/2607.23046v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1179, \"height\": 685}, {\"url\": \"assets/figures/arxiv/2607.23046v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1035, \"height\": 593}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.23046v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1195, \"height\": 666}, {\"url\": \"assets/tables/arxiv/2607.23046v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1198, \"height\": 644}, {\"url\": \"assets/tables/arxiv/2607.23046v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1191, \"height\": 592}, {\"url\": \"assets/tables/arxiv/2607.23046v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 787, \"height\": 236}, {\"url\": \"assets/tables/arxiv/2607.23046v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1213, \"height\": 212}]"
motivation: 现有视觉token剪枝方法依赖迭代子集构建，随着token数量增加，顺序选择开销极大，阻碍了理论加速转化为实际加速。
method: 提出SFPruner，采用语义引导岭杠杆抑制冗余协方差与排序定向掩蔽消除重叠冗余，在单次前向传递中实现冗余感知重要性选择。
result: 在Qwen2.5-VL上，512 token的选择时间从112.4ms降至2.5ms，加速约45倍，同时保持与SOTA相当的性能。
conclusion: 结构化冗余建模可稳定维持低选择成本，有效将理论剪枝收益转化为实际推理加速，为高分辨率MLLM提供高效token剪枝方案。
---

## 摘要
近期的高分辨率多模态大语言模型（MLLMs）每个输入生成数千个视觉词元，导致视觉词元爆炸，引发严重的延迟瓶颈。虽然词元剪枝缓解了这一问题，但最先进的子集优化方法通常依赖迭代子集构建来同时捕获视觉多样性和指令相关性。随着视觉词元数量增加，这种顺序依赖引入了显著的选取开销，严重限制了理论FLOPs减少转化为实际墙钟加速。为解决此限制，我们提出单次前向剪枝器（SFPruner），一种视觉词元剪枝的结构化重构，将冗余控制直接嵌入评分空间，避免了迭代组合优化的需求。我们的非迭代框架通过两种互补机制在单次前向传播中实现冗余感知的重要性选择。首先，为了在协方差层面减弱冗余，我们引入了一种语义引导的岭杠杆方案。通过整合指令相关性和视觉显著性，该机制抑制主导协方差方向并缓解表征偏差。其次，基于排名的方向性掩码通过非对称相似性竞争解决残余重叠，其中高分数词元通过并行张量操作明确抑制冗余的低分数替代项。大量评估表明，我们的方法保持稳定的选取成本，将词元选取过程最多减少110毫秒，在Qwen2.5-VL中从512词元时的112.4毫秒降至仅2.5毫秒。这种结构效率成功将理论词元减少转化为可观的推理加速，同时在激进压缩下保持与最先进技术高度竞争的性能。

## Abstract
Recent high-resolution Multimodal Large Language Models (MLLMs) generate thousands of visual tokens per input, leading to a visual token explosion that introduces severe latency bottlenecks. While token pruning mitigates this issue, state-of-the-art subset-optimization methods typically rely on iterative subset construction to jointly capture visual diversity and instruction relevance. As visual token counts scale, this sequential dependency introduces significant selection overhead, severely limiting the translation of theoretical FLOPs reductions into actual wall-clock speedups. To address this limitation, we propose Single-Forward Pruner (SFPruner), a structural reformulation of visual token pruning that embeds redundancy control directly into the scoring space, bypassing the need for iterative combinatorial optimization. Our non-iterative framework achieves redundancy-aware importance selection in a single forward pass through two complementary mechanisms. First, to attenuate redundancy at the covariance level, we introduce a semantics-guided ridge leverage scheme. By integrating instruction relevance and visual saliency, this mechanism suppresses dominant covariance directions and mitigates representation bias. Second, ranking-based directional masking resolves residual overlap through asymmetric similarity competition, where higher-scoring tokens explicitly suppress redundant lower-scoring alternatives via parallel tensor operations. Extensive evaluations demonstrate that our approach maintains stable selection costs, reducing the token selection process by up to 110 ms, from 112.4 ms to just 2.5 ms at 512 tokens in Qwen2.5-VL. This structural efficiency successfully translates theoretical token reductions into tangible inference speedups while preserving highly competitive performance against state-of-the-art techniques under aggressive compression.

---

## 论文详细总结（自动生成）

# 论文详细中文总结

## 1. 核心问题与整体含义（研究动机和背景）

- **问题背景**：高分辨率多模态大语言模型（MLLMs）如 LLaVA-NeXT、Qwen2.5-VL 对每个输入生成数千个视觉词元，导致自注意力计算量二次增长，造成严重的延迟和内存瓶颈。
- **现有方法的局限性**：
  - 启发式方法（如 Top-K 选择）速度快但无法充分消除空间冗余，在高压缩比下性能下降严重。
  - 子集优化方法（如基于 DPP、MMDP、MWIS 的方法）通过迭代贪婪搜索显式建模重要性和多样性，虽能更好地保留性能，但其顺序依赖的选择过程引入了显著的额外开销，随词元数量线性增长，阻碍了理论 FLOPs 减少转化为实际加速。
- **本文目标**：提出一种非迭代的结构化冗余建模方法，在单次前向传播中完成冗余感知的重要性选择，将选择开销降至接近启发式方法的水平，同时保持与子集优化方法相当的性能。

## 2. 方法论：核心思想、关键技术细节

### 核心思想
- 将冗余控制直接嵌入评分空间，避免迭代组合优化。分解为两个互补机制：**协方差级冗余衰减**（通过语义引导岭杠杆）和**排序基方向掩码**（通过非对称相似性竞争），全部通过并行张量操作实现。

### 关键技术细节
1. **语义引导（Semantic Guidance）**：
   - 计算每个视觉词元的基重要性分数，融合文本相关性和视觉显著性。
   - 文本相关性：视觉词元与文本嵌入的余弦相似度，经温度缩放归一化。
   - 视觉显著性：从视觉编码器自注意力中提取的注意力权重（如 CLS token 或全局代理）。
   - 线性融合后经 Min-Max 归一化得到 $\tilde{S}_{guide}$。

2. **协方差级冗余衰减：语义引导岭杠杆（SG-RLS）**：
   - 计算特征协方差矩阵 $C = V^\top V$，其中 $V$ 为 L2 归一化的视觉词元嵌入。
   - 岭杠杆分数 $\ell_i = v_i (C + \lambda I_D)^{-1} v_i^\top$，通过岭正则化抑制主导特征方向，使结构独特的词元获得高分。
   - 最终协方差级分数 $S_{SG-RLS,i} = \ell_i \cdot \tilde{S}_{guide,i}$（乘法“软与”门），同时要求结构唯一性和语义相关性。
   - 利用 Woodbury 矩阵恒等式，可在序列长度 $N$ 和特征维度 $D$ 之间灵活选择更小的矩阵求逆，避免 $O(N^3)$ 复杂度。

3. **排序基方向掩码（Ranking-Based Directional Masking）**：
   - 构建余弦相似度矩阵 $C_{sim} = V V^\top$。
   - 创建掩码矩阵 $M_{ij} = I(S_{SG-RLS,j} > S_{SG-RLS,i})$，实现非对称竞争：高分词元可抑制低分词元。
   - 方向抑制惩罚 $P_i = 1 - \max_j (M_{ij} \cdot C_{sim,ij})$。
   - 最终冗余感知分数 $S_{final,i} = S_{SG-RLS,i} \cdot P_i$。
   - 所有操作均为并行张量运算（矩阵乘法、逐元素比较、行归约），然后直接 Top-K 选取最终子集。

## 3. 实验设计

### 使用的模型与基准
- **模型**：三个代表性 MLLM 系列：
  - LLaVA-NeXT-7B（多 patch 高分辨率）
  - Qwen2.5-VL-7B（单序列密集编码）
  - LLaVA-Video-7B（多帧视频推理）
- **Benchmarks**：
  - 图像理解：VQAv2、GQA、TextVQA、MME、ScienceQA（SQA）
  - 视频推理：VideoMME、MLVU、LongVideoBench
- **评价指标**：绝对任务准确率、相对性能保留率（Rel.%）、选择延迟（wall-clock Selection Latency, ms）

### 对比方法
- 启发式方法：SparseVLM、PruMerge+、VisionZip、VisPruner、VisionSelector
- 子集优化方法：DivPrune、CDPruner、D²Pruner

### 实验设置（关键细节）
- 所有延迟测量在单个 NVIDIA RTX 4090 GPU 上，使用同步 CUDA 计时。
- 参数配置：$\alpha$（融合权重）和 $\lambda$（岭正则化常数）在补充材料中给出。
- 视频场景下：每帧保留 512 或 256 个词元。

## 4. 资源与算力

- **明确提及**：所有延迟测量在单个 NVIDIA RTX 4090 GPU 上进行。
- **训练资源**：本文方法为无需训练（training-free）的剪枝框架，不涉及模型训练，因此未报告训练算力及时长。对比的 VisionSelector 需要训练。
- **内存使用**：在极端测试（N=9216）中，SFPruner 峰值内存为 18,432 MB，与基线方法相近，低于未剪枝的完整序列（19,749 MB）。

## 5. 实验数量与充分性

### 实验组数（估计）
- **主要结果表格**：共 4 张（Table 1~4）。
  - Table 1：LLaVA-NeXT-7B 上三种保留预算（640/320/160 tokens），报告 9 个指标 + 延迟，对比 5 种方法。
  - Table 2：Qwen2.5-VL-7B 上两种保留比例（40%/20%），报告 8 个指标 + 延迟，对比 7 种方法。
  - Table 3：LLaVA-Video-7B 上两种保留预算，报告 7 个指标 + 延迟，对比 5 种方法。
  - Table 4：极端标度测试（9216 tokens），报告 FLOPs、峰值内存、剪枝时间、预填充时间、总推理时间，对比 2 种方法。
- **消融实验**：Table 5 在 LLaVA-NeXT-7B（320 tokens）上，对评分指标（SG vs SG-RLS）和选择策略（Top-K、顺序搜索、方向掩码）进行 2×3 组合消融，报告 3 个任务指标 + 延迟。
- **微架构分析**：图 3 展示了不同输入 token 数下的延迟和内存曲线，包含额外分析。

### 充分性与公平性评价
- **充分性**：覆盖了三种典型 MLLM 架构、多种分辨率和任务（图像+视频），包含从轻量到极端的压缩比例，以及消融实验验证各组件贡献，实验较为全面。
- **公平性**：对比方法均为近两年顶会论文的最新工作，延迟测量条件一致。但部分方法（如 VisionSelector）需要训练，而本文方法无需训练，存在天然不公平性，但论文通过比较性能保留率来减轻此问题。
- **客观性**：报告了多次实验（如极端测试下 10 次独立试验的平均值），有明确的同步计时边界。

## 6. 主要结论与发现

1. **结构效率**：SFPruner 在所有设置下保持几乎恒定的选择延迟（约 2.5~3.5 ms），不随保留词元数变化，而子集优化方法的延迟随保留词元数线性增加。
2. **性能保留**：在 Qwen2.5-VL 40% 保留时达到 99.0% 相对性能，与 CDPruner（98.8%）相当；20% 保留时 96.5%，优于大多数启发式方法。
3. **实际加速**：在极端标度测试（9216 tokens）中，剪枝时间仅 28 ms（vs CDPruner 576 ms），预填充延迟从 1813 ms 降至 1114 ms，总推理时间减少 41%，成功将 FLOPs 减少转化为实际加速。
4. **组件有效性**：消融实验证明 SG-RLS 和方向掩码互补，结合使用达到最优平衡（98.3% Rel.%、2.3 ms）。

## 7. 优点

- **方法创新性**：首次将冗余感知的视觉词元剪枝重构为单次前向评分过程，避免迭代组合优化，在效率和性能之间取得良好平衡。
- **理论优雅性**：利用岭杠杆分数从几何角度捕捉全局结构唯一性，结合指令引导实现任务感知；方向掩码通过非对称竞争解决局部冗余，全部使用并行张量操作。
- **通用性**：在多种 MLLM 架构（多 patch、单序列、视频）和多种基准上验证，无需额外训练或微调。
- **实用性**：在极端高分辨率场景下仍能保持低剪枝开销，使理论词元减少带来实际推理加速，对部署高分辨率 MLLM 有重要价值。
- **分析深入**：提供了微架构级别的延迟剖析和消融实验，解释为什么现有方法在 token 数量增加时效率下降。

## 8. 不足与局限

- **未提供训练式方法的直接对比**：虽然 VisionSelector 是训练方法且本文方法在性能上略优，但缺少与其他需要训练的剪枝方法（如 FastV、PruMerge 的前缀训练版本）的更广泛比较。
- **视频实验的帧数限制**：视频实验仅在 LLaVA-Video-7B 上进行，且保留 token 数为 512/256 每帧，未测试更长视频（如 64 帧以上）的场景。
- **消融实验的覆盖**：仅在一个保留预算（320 tokens）上进行了消融，未在不同压缩比下验证组件贡献的一致性。
- **参数敏感性**：论文未充分分析 $\alpha$ 和 $\lambda$ 对性能的影响，仅提及在补充材料中，但正文中缺乏直观展示。
- **语义引导的局限性**：在缺乏文本编码器的架构（如 Qwen2.5-VL）中，仅依赖视觉显著性，可能弱化了指令感知能力。
- **极端场景下与其他方法的相对性能差异不大**：在一些基准下，SFPruner 与 CDPruner 性能非常接近，优势主要体现在延迟上。
- **未讨论对生成任务（如视觉描述、开放问答）的影响**：评估主要基于分类/选择题类基准，未覆盖开放生成任务。

（完）
