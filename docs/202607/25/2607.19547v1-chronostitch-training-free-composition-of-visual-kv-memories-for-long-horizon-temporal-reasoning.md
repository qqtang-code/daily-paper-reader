---
title: "ChronoStitch: Training-Free Composition of Visual KV Memories for Long-Horizon Temporal Reasoning"
title_zh: "ChronoStitch: 无需训练的视觉KV记忆组合用于长时域时序推理"
authors: "Santiram Tiwari, Nishant Sinha, Kunal Kislay"
date: 2026-07-21
pdf: "https://arxiv.org/pdf/2607.19547v1"
tags: ["query:sparse-attn"]
score: 8.0
evidence: 训练自由视觉KV记忆组合用于长视界时序推理
tldr: 长视频问答中，独立缓存的KV记忆因局部旋转位置零导致时间顺序混乱。ChronoStitch通过全局三轴多模态RoPE坐标重新定位键，并选择性重计算高偏差视觉令牌，无需训练即可正确组合。在Qwen2.5-VL-3B和TempCompass上，事件顺序准确性提升，运行速度比完整联合预填充快3.3倍。该方法免训练、高效，解决了长视频时序推理中的KV记忆组合难题。
source: arxiv
selection_source: fresh_fetch
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.19547v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1082, \"height\": 366, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.19547v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1715, \"height\": 368, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.19547v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1213, \"height\": 324, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.19547v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1406, \"height\": 196, \"label\": \"Table\"}]"
motivation: 现有KV缓存组合因局部位置编码破坏时间顺序，无法正确回答时序问题。
method: 重新定位KV缓存到全局三轴RoPE坐标，并选择性重计算高偏差令牌以弥补内容差距。
result: 在Qwen2.5-VL-3B和TempCompass上，事件顺序准确性提高，速度比联合预填充快3.3倍。
conclusion: ChronoStitch免训练地实现了长视频时序推理的KV记忆正确组合。
---

## 摘要
长视频问答要求模型在不重复处理同一视频的情况下随时间保存视觉证据。一种实用的方法是将视觉语言模型每个视频块的内部键值(KV)缓存存储起来，并在查询时检索该状态。然而，独立缓存的视频块无法正确组合：每个块都是从局部旋转位置零开始预填充，因此简单的拼接会混淆时间阶段，并丢失回答诸如“发生了什么最先”、“事件发生的频率”或“整个视频中发生了什么变化”等问题所需的全局顺序。本文提出了ChronoStitch，一种无需训练的组合独立存储视觉KV记忆的方法。该方法首先将存储的旋转后键重新基于到保留时间、高度和宽度结构的全局三轴多模态RoPE坐标系上。我们证明了一维标量重索引在视觉token上存在几何不一致性，因为这样会将帧内的空间顺序变成虚假的时间位移。随后，我们解决了位置修复后残留的内容差距：后面的块最初在编码时没有关注前面的块。因此，ChronoStitch选择性地重新计算后面块中一小部分高偏差的视觉token，同时允许它们关注组合后的缓存。在Qwen2.5-VL-3B和TempCompass的时间分割上，ChronoStitch优于朴素组合和仅位置变体，在提升事件排序准确性的同时，运行速度比完全联合预填充快3.3倍。

## Abstract
Long-video question answering requires a model to preserve visual evidence over time without repeatedly reprocessing the same video. A practical approach is to store the vision-language model's internal key-value (KV) cache for each video chunk and retrieve that state at query time. However, independently cached video chunks do not compose correctly: every chunk is prefilled from local rotary position zero, so naive concatenation collides temporal phases and removes the global order required for questions about what happened first, how often events occurred, or what changed across the video. This paper presents ChronoStitch, a training-free method for composing independently stored visual KV memories. The method first re-bases stored post-rotary keys onto a global three-axis multimodal RoPE coordinate system that preserves time, height, and width structure. We show why a one-dimensional scalar re-indexing is geometrically inconsistent for visual tokens because it turns spatial order within a frame into false temporal displacement. We then address the residual content gap left by positional repair: later chunks were originally encoded without attending to earlier chunks. ChronoStitch therefore selectively recomputes a small fraction of high-deviation later-chunk visual tokens while allowing them to attend over the composed cache. On Qwen2.5-VL-3B and the temporal split of TempCompass, ChronoStitch outperforms naive composition and position-only variants, improving event-ordering accuracy while running 3.3x faster than full joint re-prefilling.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义（研究动机和背景）

- **核心问题**：长视频问答中，如何在不重复处理整个视频的情况下，利用已缓存的视觉KV记忆正确回答时序性问题（如事件顺序、频率、变化）？现有方法将视频分成独立块单独预填充并缓存KV，但简单拼接会导致时间相位冲突（每个块从局部位置零开始），破坏全局时序信息。
- **研究动机**：文本侧KV缓存重用（如KVLink, CacheBlend）已取得进展，但视频侧因为多模态旋转位置编码（mRoPE）的三维结构（时间、高度、宽度），导致位置再索引不能简单一维化。需要一种无需训练的方法，同时修复位置误差和跨块内容缺失。
- **整体含义**：本文提出的ChronoStitch是首个无需训练的视觉KV记忆组合方法，通过三轴坐标重定位键和选择性重计算高偏差视觉token，在保持速度优势的同时显著提升时序推理能力。

### 2. 论文提出的方法论：核心思想、关键技术细节、公式或算法流程（文字说明）

- **核心思想**：两阶段修复——① **位置修复**：将每个独立块的旋转后键从局部坐标通过delta旋转映射到全局三轴mRoPE坐标系（时间、高度、宽度），使层0键重建达到机器精度；② **内容修复**：位置正确后，后面块仍缺少对前面块的注意力历史，因此选择性地重计算后面块中键偏差最大的一部分视觉token（通过浅层重计算后层1键的移动量衡量），让它们对完整组合缓存进行注意力计算。
- **关键技术细节**：
  - 三轴delta旋转：根据目标全局坐标与局部坐标的差值，对每个频率轴施加相应的旋转角度。公式为：\(\Delta_i = (p'_{\sigma(i)} - p_{\sigma(i)}) \theta_i\)，旋转后键 \(R(\Delta) f(W_k x, p) = f(W_k x, p')\)。值不旋转。
  - 一维标量重索引为何错误：将视觉token展平为连续标量位置会导致同一帧内不同空间patch获得不同的时间索引，把空间顺序误认为时间位移，破坏帧内一致性。
  - 选择性重计算：对后面块的所有视觉token，计算层1重计算后键与存储键的欧氏距离 \(\delta_t\)，取分数 \(\rho\)（实验中为0.35，对应约25%的总体视觉缓存）进行完整多层重计算，新键值写回缓存。
- **算法流程**：（1）检索存储块；（2）将块的三轴坐标移位到全局时间线；（3）delta旋转存储的键；（4）拼接缓存；（5）按键偏差代理排名后面块视觉token；（6）选择\(\rho\)比例token重新计算（允许注意力覆盖完整缓存）；（7）返回修复后的KV内存用于回答查询。

### 3. 实验设计：数据集/场景、基准、对比方法

- **数据集与场景**：
  - 可控顺序敏感性探针（Order-swap probe）：将视频块以A→B和B→A顺序组合，询问哪个先出现，测试时序记忆是否随顺序翻转而翻转答案。
  - **TempCompass temporal split**：590道多项选择题，专门测试视频LLM的时间感知能力，包括事件排序（event ordering）和属性变化（attribute change）等子类。
- **基准**：全联合预填充（joint prefill）作为上界（非可部署的缓存方法）；朴素拼接（naive concatenation）；一维标量重索引（Scalar1D re-basing）；仅三轴重索引（three-axis re-basing only）；ChronoStitch（三轴重索引+选择性修复，修复比例\(\rho=0.35\)）。
- **对比方法**：五个策略在表1（键重建保真度）、表2（顺序敏感性）、表3（TempCompass）、表4（效率）中进行对比。

### 4. 资源与算力

- **模型与硬件**：使用 `mlx-community/Qwen2.5-VL-3B-Instruct-4bit`，在Apple M系列硬件上运行（未明确具体型号和数量）。MLX框架。
- **算力说明**：本文方法是训练自由（training-free）的，因此无训练时长。论文未详细报告GPU型号、数量或训练成本。推理时ChronoStitch比联合预填充快3.3倍（表4）。文中提到“vision tower is skipped”以及\(C_{\text{Chrono}} \approx L c_r T + 2(c_a|C|T + c_f|C|) + L(c_a \rho T^2 + c_f \rho T)\)，量化了计算复杂度。

### 5. 实验数量与充分性

- **实验数量**：
  - 表1：六个层（0, 1, 9, 18, 27, 35）的余弦相似度和相对MSE，对比三个方法（naive, Scalar1D, 三轴）相对联合预填充的键重建。
  - 表2：单一控制实验（6个order-swap对），比较六个变体（包括不同修复比例\(\rho=0.15, 0.35\)）。
  - 表3：TempCompass temporal split上590个问题，报告总体准确率、事件排序准确率、属性变化准确率，四个非oracle方法及上界。
  - 表4：12个视频样本的查询时间，比较联合预填充和ChronoStitch的平均/中位时间及加速比。
- **充分性与公平性**：
  - 实验覆盖了位置修复效果诊断（表1）、时序记忆能力（表2）、真实基准（表3）和效率（表4），层次较完整。
  - 消融：对比了三轴 vs 一维、仅位置 vs 加入修复，明确了各自贡献。
  - 但作者承认局限：① 模型较小（3B），联合上界仅63.9%，压缩了可观察差距；② 修复比例\(\rho\)仅从一个控制实验选取，未必最优；③ 效率报告基于单一硬件配置，缺乏硬件无关的FLOP和内存分析；④ 一维重索引虽然理论上错误，但在当前3B模型下游QA上未表现明显劣势。

### 6. 论文的主要结论与发现

- **位置修复必要但不足**：三轴重索引能在层0重建联合预填充键至机器精度（相对误差3.6×10⁻⁸），但仅凭位置修复无法恢复时序推理能力（表2中三轴re-basing only准确率甚至低于朴素拼接）。
- **选择性重计算是关键**：修复约35%的后面块视觉token（占总缓存25%）即可在顺序探针上达到联合预填充效果，在TempCompass上整体提升4.8个百分点（从49.3%到54.1%），事件排序提升7个百分点（从47.0%到54.0%）。
- **速度优势显著**：ChronoStitch比联合预填充快3.3倍，因为视觉编码器跳过，且主要注意力成本被限制在重计算的部分（\(\rho\)因子）。
- **一维标量重索引几何错误**：在层0重建上表现甚至差于朴素拼接（相对MSE更高），但在下游QA上差距不大，可能因模型规模小或测试集限制。

### 7. 优点

- **无需训练**：方法完全基于推理时的键旋转和选择性重计算，不修改模型权重，实用性强。
- **理论严谨性**：从mRoPE的几何结构出发，指出一维重索引的不一致性，并给出三轴delta旋转的数学推导（可加性）。
- **诊断实验设计巧妙**：通过顺序对称性测试（order-swap probe）清晰隔离了跨块注意力缺失的影响，证明了位置修复和内容修复各自的贡献。
- **效率与性能平衡**：仅重计算少部分token（约25%），即可恢复大部分时序推理收益，且速度提升3倍以上。
- **可复现性好**：基于开源模型和框架（MLX），方法描述清晰。

### 8. 不足与局限

- **模型规模较小**：仅测试3B参数模型，在更大规模模型（如7B、72B）上的表现未知；联合上界仅63.9%，导致可提升空间有限，可能低估方法真实潜力。
- **修复比例选择经验性**：\(\rho=0.35\)仅从一次控制实验确定，缺乏对模型规模、块长度、视频时长等因素的系统性调参。
- **效率度量不完整**：仅报告了单一硬件上的墙钟时延，未进行FLOP和内存消耗的细粒度分析，且真实部署场景中检索步骤的开销未考虑。
- **一维重索引的下游差距未充分显现**：尽管理论上错误，但在3B模型上仅相差0.3个百分点，可能暗示测试集对位置精度敏感度不足，或模型容量掩盖了问题。
- **基准测试局限**：TempCompass temporal split虽然针对时序，但总问题数590个，且评测准确性缺乏统计显著性检验。
- **应用限制**：方法假设视频已分块且KV缓存预先存储，未讨论缓存管理（如压缩、淘汰）以及检索的准确性对上游的影响。仅处理视觉token，未涉及文本指令与视觉的交互。

（完）
