---
title: "Adaptive Depth Sparse Framework: Similarity-Driven Resource Allocation for Pre-Trained LLMs"
title_zh: 自适应深度稀疏框架：基于相似性的预训练大语言模型资源分配
authors: "Yidu Wu, Xiang Wang, Kejie Zhao, Zhangchi Wang, Qinghai Guo, Xiaoying Tang"
date: 2026-07-23
pdf: "https://arxiv.org/pdf/2607.21291v1"
tags: ["query:sparse-attn"]
score: 8.0
evidence: 自适应深度稀疏框架实现高效LLM推理
tldr: 大语言模型推理成本高，现有加速方法常需任务微调或从头训练，适应性差。AdaDSF利用层间表示余弦相似性统计，为每层动态分配token保留比例，结合轻量路由器选择重要token，并通过特征保持对齐目标训练。在GPT-NeoX和Qwen2.5的语言建模与常识推理任务上，大幅降低推理FLOPs，性能接近密集模型。相比MoD、D-LLM等基线，精度退化更小，且无需完全重训练，提升了跨任务可用性。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.21291v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1168, \"height\": 678, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.21291v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 950, \"height\": 565, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.21291v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1148, \"height\": 429, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.21291v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1236, \"height\": 509, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.21291v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 853, \"height\": 663, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.21291v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 721, \"height\": 223, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.21291v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 468, \"height\": 140, \"label\": \"Table\"}]"
motivation: 现有LLM加速方法依赖任务特定微调或重训练，跨任务适应性差，且未充分利用层间贡献差异。
method: 基于层输入输出余弦相似度统计分配token保留比例，用轻量路由器选择重要token，通过特征保持对齐目标匹配稀疏与密集表示。
result: 在GPT-NeoX和Qwen2.5上推理FLOPs大幅减少，性能接近密集模型，精度退化低于MoD、D-LLM和DLO。
conclusion: AdaDSF无需全重训练即可高效实现深度稀疏化，兼顾推理效率与模型性能，泛化性强。
---

## 摘要
大语言模型（LLMs）在生成和推理任务中表现出色，但Transformer架构带来了高昂的推理成本。现有的加速方法通常依赖于特定任务的微调或从头训练，增加了适配成本并限制了跨任务可用性。我们提出了一种自适应深度稀疏框架（AdaDSF），该框架无需完全重新训练即可将现成的预训练LLMs转换为深度稀疏模型。我们的关键洞察是，各层对表示变换的贡献不均，这通过层输入与输出隐藏状态之间的余弦相似度来表征。基于此，AdaDSF从相似性统计中分配逐层的令牌保留率，使用轻量级路由器在每层选择信息丰富的令牌，并引入特征保留对齐目标以匹配稀疏模型与密集模型之间的中间和最终表示。在GPT-NeoX和Qwen2.5上针对语言建模和常识推理的实验中，AdaDSF在显著降低推理浮点运算次数的同时，保持了与密集模型相近的性能。在可比稀疏度下，AdaDSF相比包括MoD、D-LLM和DLO在内的强基线，始终产生更小的精度下降。

## Abstract
Large language models (LLMs) achieve strong generation and reasoning performance, but the Transformer architecture incurs high inference cost. Existing acceleration methods often rely on task-specific fine-tuning or training from scratch, increasing adaptation cost and limiting cross-task usability. We present an Adaptive Depth Sparse Framework (AdaDSF) that converts off-the-shelf pre-trained LLMs into depth-sparse models without full retraining. Our key insight is that layers contribute unequally to representation transformation, characterized by the cosine similarity between layer input and output hidden states. Based on this, AdaDSF assigns layer-wise token retention ratios from similarity statistics, uses a lightweight router to select informative tokens at each layer, and introduces a feature-preserving alignment objective to match intermediate and final representations between sparse and dense models. On GPT-NeoX and Qwen2.5 over language modeling and commonsense reasoning, AdaDSF substantially reduces inference FLOPs while preserving performance close to dense counterparts. Under comparable sparsity, AdaDSF consistently yields smaller accuracy degradation than strong baselines including MoD, D-LLM, and DLO.

---

## 论文详细总结（自动生成）

## 1. 论文的核心问题与整体含义（研究动机和背景）

大语言模型尽管在生成和推理任务上表现优异，但其基于Transformer的架构在推理时计算量随深度线性增长、随序列长度二次增长，导致部署成本高昂。现有加速方法（如量化、蒸馏、轻量注意力）往往依赖**任务特定的微调**或**从头训练**，适应新任务时需要额外开销，且跨任务通用性差。另一类深度稀疏方法（如Mixture-of-Depths、D-LLM、DLO）虽能减少计算，但存在以下局限：依赖特定训练流程、采用固定或启发式令牌保留策略导致资源错配、需要大幅度修改模型结构。因此，论文的核心目标是**设计一种无需完全重训练、仅对现成预训练LLM做最小改动即可实现的深度稀疏推理框架**，在保持性能的前提下显著降低推理FLOPs。

## 2. 论文提出的方法论：核心思想、关键技术细节与算法流程

**核心思想**：基于“不同Transformer层对表示变换的贡献不均匀”这一观察，利用**层输入与输出隐藏状态之间的余弦相似度**来量化每层的变换强度，并据此动态分配令牌保留比例。同时引入轻量级路由器选择信息性令牌，并通过**特征保持对齐训练**来弥合稀疏模型与密集模型之间的表示差异。

**关键技术细节与算法流程**：

1. **相似度驱动的层间令牌保留率分配**  
   - 在预训练密集模型上，对校准数据计算每层输入的余弦相似度 \( s_i = \text{CosSim}(x_{\text{in}}^{(i)}, x_{\text{out}}^{(i)}) \)。  
   - 通过**温度归一化加权**（式(5)）、**偏差缩放**（式(6)）和**Sigmoid映射**（式(7)）将相似度转换为初始保留率 \( r_i' \in [0.05, 0.95] \)。  
   - 最后根据全局平均保留率 \( t \) 进行**预算修正**（式(8)），使所有层的保留率之和等于 \( tL \)。  
   - 低相似度层（变换小）获得更少的令牌预算，高相似度层获得更多预算。

2. **稀疏模块与轻量路由器**  
   - 每一层包含一个**可训练的MLP路由器**和原始的Transformer层。路由器对输入令牌预测重要性分数，并选择Top-K个令牌（\( K_i = \lfloor r_i \cdot s \rfloor \)）。  
   - 被选中的令牌经过完整的Transformer计算；未被选中的令牌通过残差路径直接跳过。训练时通过Gumbel-Softmax或直通估计保持路由的可微分性。

3. **特征保持对齐训练**  
   - **隐藏状态对齐**：对每一层的输出，计算稀疏模型与密集模型的**softmax后的表示**之间的MSE损失（式(9)）。  
   - **输出分布对齐**：针对最终logits，使用**KL散度**（式(10)）对齐稀疏与密集模型的预测分布。  
   - 总损失为 \( \mathcal{L} = \mathcal{L}_{\text{align}} + \frac{1}{L}\sum_{l}\mathcal{L}_{\text{hid}}^{(l)} \)（式(11)）。  
   - 训练时冻结原始密集层参数（作为教师），仅训练路由器参数。

## 3. 实验设计：数据集、场景、Benchmark与对比方法

| 项目 | 详情 |
|------|------|
| **语言模型** | GPT‑NeoX‑130M、Qwen2.5‑0.5B、Qwen2.5‑1.5B |
| **训练数据** | GPT‑NeoX：Wikitext‑103（预训练场景）；Qwen2.5：GenQA、InfinityInstruct、OpenHermes2.5（指令调优场景）|
| **评估任务** | 语言建模（Wikitext‑103 PPL）；常识推理：ARC‑Challenge、ARC‑Easy、HellaSwag、PIQA、WinoGrande、OpenBookQA（共6个）|
| **基准模型** | 密集原始模型（Dense）|
| **对比方法** | **MoD**（Mixture‑of‑Depths）、**D‑LLM**、**DLO**（Dynamic Layer Operation）；注意D‑LLM仅在GPT‑NeoX上可比较，Qwen2.5上不适用。|
| **评价指标** | 令牌保留率（70%~90%）、归一化FLOPs（稀疏/密集）、PPL、准确率及相对于密集基线的退化（Diff）|

## 4. 资源与算力

论文正文中**未明确给出**所使用的GPU型号、数量、训练时长等具体算力信息。仅在“Future Work”中提及计划将来扩展到7B以上模型验证可扩展性。因此，无法评估其训练开销的具体规模。

## 5. 实验数量与充分性

论文包含**四类关键实验**：

- **整体性能对比**（表1a）：在Qwen2.5-0.5B/1.5B上比较MoD、DLO和AdaDSF在6个常识推理任务上的准确率和FLOPs。
- **不同稀疏度下的性能**（表1b）：在Qwen2.5-0.5B上测试90%/80%/70%保留率的效果，覆盖三种压缩水平。
- **GPT‑NeoX上的语言建模**（表2）：对比MoD、D‑LLM、DLO在Wikitext‑103 PPL上的表现，保留率70%~90%。
- **消融实验**：
  - 相似度驱动分配策略的贡献（表3）：对比不采用（w/o τ）以及不同温度τ下PPL的变化。
  - 对齐损失的影响（表4）：对比仅使用因果损失（\( \mathcal{L}_{\text{causal}} \)）与完整损失。

**充分性评价**：实验覆盖了不同规模（130M~1.5B）、不同稀疏度、不同任务类型（语言建模+多任务推理），并进行了关键模块的消融。但**未报告多次运行的标准差或置信区间**，可能受随机种子影响；并且对比方法如MoD、DLO的实现细节是否严格保持一致（如训练数据、步数、超参数）未充分说明，公平性存在一定隐患。总体而言，实验设计较为完善，但规模上限为1.5B，通用性待验证。

## 6. 论文的主要结论与发现

- AdaDSF在**所有稀疏度水平**（70%~90%）下，在语言建模和常识推理任务上均取得**最低的PPL或最小的精度退化**，优于MoD、D‑LLM、DLO。
- 例如在GPT‑NeoX‑130M上，80%保留率时AdaDSF的PPL为18.9，而MoD为21.6、D‑LLM为2019、DLO为19.6，FLOPs仅0.787×；在Qwen2.5‑0.5B上，70%保留率时Avg退化仅-4.0%，而MoD为-9.5%、DLO为-5.4%。
- **相似度驱动的保留率分配**相比固定策略可带来约0.78 PPL的改善（表3）。
- **特征保持对齐损失**（隐藏层+输出分布对齐）相比仅用因果损失，PPL从20.14降至18.91，显著提升稀疏模型稳定性。
- 结论：AdaDSF能够在不修改模型内部结构、不进行完全重训练的情况下，将预训练密集LLM高效转化为深度稀疏模型，提供更优的效率-精度权衡。

## 7. 优点

- **无需架构修改**：直接套用标准Transformer，仅添加一个轻量MLP路由器，易于集成到现有推理管线。
- **数据驱动的资源分配**：利用层间余弦相似度统计作为计算预算分配依据，避免人工设定启发式规则，更符合模型内部特性。
- **联合对齐训练**：同时约束中间层和输出层的表示，有效抑制令牌跳过带来的表示偏移，鲁棒性强。
- **兼容性强**：在两种不同系列模型（GPT‑NeoX、Qwen2.5）和两种训练范式（预训练、指令调优）上均有效，泛化性好。
- **计算-精度平衡优秀**：FLOPs降低接近理论上限（如70%保留率时FLOPs降至0.68×），而精度退化仅约4%，优于同期基线。

## 8. 不足与局限

- **模型规模有限**：最大仅验证到1.5B参数，未在7B/13B等更大模型上实验，**可扩展性尚未证明**。未来需要将方法应用于更大模型以确认通用性。
- **未报告训练成本**：虽声称“无需完全重训练”，但路由器训练仍需要额外时间和数据，且文中未给出具体GPU机时或能耗，难以评估实际额外开销。
- **路由器本身的计算开销**：每个稀疏层都需要一次MLP前向传播来预测重要性分数，这部分开销在低FLOPs下可能不可忽略，但文中未分析其对实际延迟的影响。
- **校准数据集依赖性**：相似度统计依赖于一个校准子集，不同校准集可能导致不同分配策略，其敏感性未被探讨。
- **多次运行统计缺失**：实验结果仅展示单次数值，未提供多次重复实验的均值和方差，难以评估方法稳定性。
- **对比复现公平性存疑**：基线方法（MoD、DLO）的复现细节、超参数是否严格遵循原文，且是否进行相同的对齐训练，文中未充分说明。

（完）
