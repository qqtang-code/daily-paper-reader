---
title: "ChronoStitch: Training-Free Composition of Visual KV Memories for Long-Horizon Temporal Reasoning"
title_zh: ChronoStitch：无需训练的组合视觉KV记忆用于长时域时间推理
authors: "Santiram Tiwari, Nishant Sinha, Kunal Kislay"
date: 2026-07-21
pdf: "https://arxiv.org/pdf/2607.19547v1"
tags: ["query:mm-cot"]
score: 8.0
evidence: 提出训练无关的KV记忆组合方法用于视频问答中的长时推理
tldr: 长视频问答需长时间保留视觉证据，常用方法是对视频块独立缓存KV memory。但独立缓存块从局部位置零开始填充，全局时序丢失，无法回答时序问题。ChronoStitch无需训练，通过将后旋转键重定位到全局三维多轴RoPE坐标系，并选择性重计算高偏差视觉token以弥补内容差距，实现正确组合。在Qwen2.5-VL-3B和TempCompass上，事件顺序准确性提升，速度比完整联合预填充快3.3倍。贡献是训练无关的视觉KV记忆组成方法。
source: arxiv
selection_source: fresh_fetch
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.19547v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1082, \"height\": 366, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.19547v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1715, \"height\": 368, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.19547v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1213, \"height\": 324, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.19547v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1406, \"height\": 196, \"label\": \"Table\"}]"
motivation: 独立缓存的视频块因局部位置零填充导致全局时序丢失，无法回答时序问题。
method: 通过全局三维RoPE重定位后旋转键，并选择性重计算高偏差视觉token。
result: 事件顺序准确性提升，速度比完整联合预填充快3.3倍。
conclusion: 训练无关的视觉KV记忆组成方法有效解决时序问题。
---

## 摘要
长视频问答要求模型在不重复处理同一视频的情况下随时间保留视觉证据。一种实用的方法是存储视觉-语言模型对每个视频块的内部键值（KV）缓存，并在查询时检索该状态。然而，独立缓存的视频块无法正确组合：每个块从局部旋转位置零开始预填充，因此简单拼接会导致时间相位冲突，并丢失回答关于“首先发生了什么”、“事件发生频率”或“视频中发生了什么变化”等问题所需的全局顺序。本文提出了ChronoStitch，一种无需训练的方法，用于组合独立存储的视觉KV记忆。该方法首先将存储的旋转后键重新定位到全局三轴多模态RoPE坐标系统上，该坐标系统保留了时间、高度和宽度结构。我们证明了为什么一维标量重索引对于视觉标记在几何上是不一致的，因为它会将帧内的空间顺序转变为错误的时间位移。随后，我们解决了位置修复后残留的内容差距：后面的块最初编码时没有关注前面的块。因此，ChronoStitch选择性地重新计算后块中少量高偏差的视觉标记，同时允许它们关注组合后的缓存。在Qwen2.5-VL-3B和TempCompass的时间划分上，ChronoStitch优于简单组合和仅位置变体，将事件排序准确性提高了，同时运行速度比完全联合重新预填充快3.3倍。

## Abstract
Long-video question answering requires a model to preserve visual evidence over time without repeatedly reprocessing the same video. A practical approach is to store the vision-language model's internal key-value (KV) cache for each video chunk and retrieve that state at query time. However, independently cached video chunks do not compose correctly: every chunk is prefilled from local rotary position zero, so naive concatenation collides temporal phases and removes the global order required for questions about what happened first, how often events occurred, or what changed across the video. This paper presents ChronoStitch, a training-free method for composing independently stored visual KV memories. The method first re-bases stored post-rotary keys onto a global three-axis multimodal RoPE coordinate system that preserves time, height, and width structure. We show why a one-dimensional scalar re-indexing is geometrically inconsistent for visual tokens because it turns spatial order within a frame into false temporal displacement. We then address the residual content gap left by positional repair: later chunks were originally encoded without attending to earlier chunks. ChronoStitch therefore selectively recomputes a small fraction of high-deviation later-chunk visual tokens while allowing them to attend over the composed cache. On Qwen2.5-VL-3B and the temporal split of TempCompass, ChronoStitch outperforms naive composition and position-only variants, improving event-ordering accuracy while running 3.3x faster than full joint re-prefilling.

---

## 论文详细总结（自动生成）

## 1. 论文的核心问题与整体含义（研究动机和背景）

长视频问答要求模型在不重复处理整个视频的情况下保留时间有序的视觉证据。一种实用方法是独立存储每个视频块的内部键值（KV）缓存，并在查询时检索。然而，独立缓存的视频块在拼接时存在两个根本问题：  
- **位置冲突**：每个块从局部旋转位置零开始预填充，简单拼接会导致不同块的时序相位重叠，丢失全局顺序，无法回答“首先发生了什么”、“事件发生频率”或“视频中发生了什么变化”等问题。  
- **内容缺失**：后块在独立编码时无法关注前块，导致跨块注意力缺失。  

论文提出 ChronoStitch，一种无需训练的方法，通过两步修复上述问题，实现独立视觉 KV 记忆的正确组合，用于长时域时间推理。

## 2. 论文提出的方法论：核心思想、关键技术细节

### 核心思想  
将独立缓存的视觉 KV 记忆视为两部分错误：位置错误和内容错误。分别通过**三维 RoPE 键重定位**和**选择性跨块注意力修复**来矫正。

### 关键技术细节  

1. **三维多轴 RoPE 坐标重定位（Three-Axis Re-basing）**  
   - 视觉标记的位置由三维坐标 \( p = (p_t, p_h, p_w) \) 表示，分别对应时间、高度、宽度。  
   - 对于每个存储的键 \( k \)，计算从局部坐标 \( p \) 到全局坐标 \( p' \) 的旋转增量 \( \Delta \)，直接对后旋转键施加旋转（值不变）：  
     \[
     \Delta_i = (p'_{\sigma(i)} - p_{\sigma(i)}) \theta_i,
     \quad R(\Delta) f(W_k x, p) = f(W_k x, p')
     \]  
   - 该操作在 layer 0 恢复联合预填充的精确位置几何（表 1 显示相对误差 \(3.6 \times 10^{-8}\)）。  
   - 强调必须使用三维坐标，而非将视觉标记展平为一维标量（后者将帧内空间顺序误当作时间位移）。

2. **选择性跨块内容修复（Selective Cross-Attention Repair）**  
   - 位置修复无法弥补深层隐藏状态的内容差距（后块独立编码时未关注前块）。  
   - 选取后块中受缺失跨块注意力影响最大的视觉标记：对后块做一次浅层重计算（与组合缓存做注意力），计算 layer 1 键的变化量 \( \delta_t \) 作为代理分数。  
   - 选择分数最高的 \( \rho = 0.35 \) 比例的后块视觉标记（约占组合缓存总量的 25%），重新通过所有层计算，允许它们关注整个组合缓存。新计算出的键值替换缓存中对应位置；其余标记保持不变。

3. **查询时效率**  
   - 重新计算的 token 比例约占总视觉缓存的 25%，无需重新运行视觉塔。主要计算代价：键旋转（线性于总长度）、浅层排名（小规模）、深层重计算（二次于修复部分）。  
   - 实测比完全联合预填充快约 3.3 倍（表 4）。

## 3. 实验设计：数据集、Benchmark、对比方法

- **数据集**：使用 **TempCompass** 的时间划分（temporal split），包含 590 道多选题，专门测试时间感知能力（事件顺序、属性变化等）。  
- **受控顺序敏感性探针**：自行构建的 A-B / B-A 顺序交换任务，测试模型是否能在交换块顺序后正确翻转答案。  
- **模型**：`mlx-community/Qwen2.5-VL-3B-Instruct-4bit`（3B 参数，4bit 量化），在 Apple M 系列硬件上运行。  
- **对比方法**：  
  1. **完全联合预填充**（Joint prefill，作为上界）  
  2. **ChronoStitch**（提出的完整方法）  
  3. **三维重定位仅位置**（Three-axis re-basing only，无内容修复）  
  4. **一维标量重定位**（Scalar1D re-basing，错误几何）  
  5. **简单拼接**（Naive concatenation，无任何修复）  

## 4. 资源与算力

论文未明确说明使用的 GPU 型号、数量或训练时长，仅提到实验在 **Apple M 系列硬件**上使用 **MLX 框架**运行，模型为 4bit 量化版。未提及分布式训练或大规模算力需求。因为方法无需训练，主要开销在推理和少量重计算，算力要求较低。

## 5. 实验数量与充分性

- **数量**：主要包含三大块实验：  
  1. **键重构保真度**（表 1）：在不同层（0、1、9、18、27、35）比较不同方法相对于联合预填充的余弦相似度和相对 MSE。  
  2. **受控顺序敏感性探针**（表 2）：测试 6 个顺序交换对，报告准确率和翻转对数量，并探索不同修复比例（\( \rho = 0.15, 0.35 \)）。  
  3. **TempCompass 时间划分**（表 3）：整体准确率、事件顺序子类和属性变化子类。  
  4. **查询时间效率**（表 4）：12 个视频样本的平均时延和相对成本。  

- **充分性与公平性**：  
  - 消融实验完整（对比位置仅修复、标量重定位、简单拼接），且与上界（联合预填充）对齐。  
  - 受控探针直接验证了位置修复不足和内容修复的必要性。  
  - 修复合比例基于少量控制实验确定，但未在不同模型规模或块长度下系统调优。  
  - 基准测试 TempCompass 是公认的视频时序理解基准，但模型仅 3B 且量化，可能压缩了可观测改进窗口。  
  - 总体实验设计合理，结论支持论文核心主张，但规模较小，外部泛化性需进一步验证。

## 6. 论文的主要结论与发现

1. **位置修复是必要条件但非充分条件**：三维 RoPE 重定位在 layer 0 实现机器精度重建，但仅凭位置修复不足以恢复时间推理能力（受控探针准确率仅 41.7%，甚至低于简单拼接的 50%）。  
2. **选择性跨块注意力修复是关键**：修复约 35% 的后块视觉标记（占总量 25%），在受控探针上恢复 100% 正确率，在 TempCompass 上整体提升 4.8 个百分点，事件顺序子类提升 7.0 个百分点。  
3. **一维标量重定位在几何上错误**：对于视觉标记，标量压缩将空间顺序变为假时间位移，重构精度甚至不如简单拼接。  
4. **效率显著**：查询时速度快 3.3 倍，同时无需重新运行视觉塔。  
5. **属性变化类改进较小**：因为此类问题常可从局部证据回答，对跨块时间交互依赖较低。

## 7. 优点

- **无需训练**：完全通过推理时操作实现，兼容现有 VL 模型（如 Qwen2.5-VL），无需额外微调。  
- **理论严谨**：明确分析三维 RoPE 的几何意义，指出一维重定位的缺陷，并给出数学证明。  
- **两步修复思路清晰**：分离位置错误和内容错误，分别用轻量旋转和部分重计算解决。  
- **效率与效果平衡**：只重计算少量高影响 token，获得大部分上界收益，实际部署可行性高。  
- **实验设计针对性强**：通过受控探针直接验证方法有效性，避免仅依赖模糊的 QA 指标。

## 8. 不足与局限

- **模型尺度小**：仅测试 3B 量化模型，上界只有 63.9%，可观测改进空间有限，更大模型或未量化模型上的表现未知。  
- **修复合比例不够鲁棒**：ρ 仅通过少量控制探针确定，未在多个 bench 或更长视频下系统搜索最优值，可能不够通用。  
- **效率评估局限**：仅报告了单一硬件上的时间，缺乏 FLOP 和内存占用的理论分析，也未与更多基线（如 KVLink、CacheBlend）直接对比。  
- **基准覆盖窄**：只用了 TempCompass 时间划分，缺乏其他长视频推理基准（如 Ego4D、VideoMME 等）的验证。  
- **未讨论长视频缓存的管理**：ChronoStitch 假设已检索到相关块，未涉及检索精度对下游的影响。  
- **一维标量方法的下游差距不大**：在 3B 模型上，一维和三维重定位仅位置方法的 QA 结果相近，说明位置修复在当前小模型上贡献有限，主要收益来自内容修复。

（完）
