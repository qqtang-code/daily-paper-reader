---
title: "Full-Pipeline Inference Optimization for MiMo-V2.5 Series: Pushing Hybrid SWA Efficiency to the Limit"
title_zh: MiMo-V2.5系列全流水线推理优化：将混合滑动窗口注意力效率推向极限
authors: "Xiaomi MiMo Team, Anqi Liu, Aoxin Ma, Bo Chen, Bo Yang, Chen Wang, Chen Zhang, Chengda Tang, Chengwei Wang, Chiheng Lou, Depeng Yan, Fuli Luo, Gang Wang, Hailin Zhang, Jiale Sun, Kang Zhou, Rui Huang, Shaohui Liu, Shen Huang, Shijie Cao, Shuaishuai Fan, Tianling Zhou, Xiangwei Deng, Xueyang Xie, Xuli Wang, Yingchun Lai, Yu Yang, Yuan Zhang, Zhen Tang, Zhonghua Deng, Zihan Jiang"
date: 2026-07-14
pdf: "https://arxiv.org/pdf/2607.13095v1"
tags: ["query:sparse-attn"]
score: 9.0
evidence: 面向混合滑动窗口注意力的KVCache优化
tldr: MiMo-V2.5模型采用Hybrid SWA、稀疏MoE和多模态编码器，理论上可减少注意力和KVCache开销，但实际部署需大量工程优化。本文系统优化KVCache，包含层预取、SWA感知前缀缓存树和专用放置策略，实现严格O(W)存储和高缓存命中率；构建RDMA增强的GCache分布式缓存，并设计KVCache亲和路由器以平衡计算与负载；同时优化多模态输入的GPU图像预处理、并行视频解码和缓存共享。这些改进构成首个大规模生产系统，高效覆盖Hybrid SWA+MoE+多模态复合架构，将混合SWA效率推向极限。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.13095v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1475, \"height\": 563, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.13095v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1486, \"height\": 565, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.13095v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1420, \"height\": 816, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.13095v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1144, \"height\": 926, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.13095v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1355, \"height\": 755, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.13095v1/fig-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 1128, \"height\": 778, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.13095v1/fig-007.webp\", \"caption\": \"\", \"page\": 0, \"index\": 7, \"width\": 1121, \"height\": 736, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.13095v1/fig-008.webp\", \"caption\": \"\", \"page\": 0, \"index\": 8, \"width\": 1132, \"height\": 562, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.13095v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 983, \"height\": 215, \"label\": \"Table\"}]"
motivation: 混合SWA虽能降低注意力和KVCache开销，但生产系统中实现这些优势需要全流程工程优化，以推动极端效率。
method: 优化KVCache（层预取、SWA感知前缀缓存树、专用放置），构建RDMA优化的GCache分布式缓存，开发KVCache亲和路由器，并优化多模态输入预处理。
result: 实现严格O(W)存储和高缓存命中率，成为首个高效覆盖Hybrid SWA+MoE+多模态的大规模生产系统。
conclusion: 全流程优化成功将混合SWA效率推向极限，为复合架构推理提供了可扩展的工业级方案。
---

## 摘要
我们针对MiMo-V2.5模型家族提出了全流水线推理优化，该模型结合了混合滑动窗口注意力（Hybrid SWA）、稀疏混合专家（MoE）和多模态编码器。尽管与全注意力相比，Hybrid SWA理论上可以显著减少注意力计算和KVCache存储，但在生产环境中实现这些收益需要大量的工程努力。我们通过逐层预取、SWA感知的前缀缓存树以及专门的放置策略，系统地优化了KVCache系统，实现了严格的$O(W)$ SWA存储和高缓存命中率。我们进一步构建了GCache，一个具有RDMA优化网络的高性能分布式缓存基础设施，并开发了一个KVCache亲和性路由器，以减少计算量同时保持负载均衡。我们还针对多模态输入进行了优化，包括GPU图像预处理、并行视频解码和多模态缓存共享。这些优化共同构成了首个在生产中高效覆盖Hybrid SWA + MoE + 多模态复合架构的大规模LLM服务系统。

## Abstract
We present a full-pipeline inference optimization for the MiMo-V2.5 model family, which combines Hybrid Sliding Window Attention (Hybrid SWA), sparse Mixture-of-Experts (MoE), and multimodal encoders. While Hybrid SWA can ideally reduce both attention compute and KVCache storage significantly compared to Full Attention, realizing these gains in production requires substantial engineering effort. We systematically optimize the KVCache system with layerwise prefetch, SWA-aware prefix cache trees, and specialized placement strategies, achieving strict $O(W)$ SWA storage and high cache hit rates. We further build GCache, a high-performance distributed cache infrastructure with RDMA-optimized networking, and develop a KVCache-affinity router to reduce computation while preserving load balancing. We also optimize for multimodal inputs, including GPU image preprocessing, parallel video decoding, and multimodal cache sharing. Together, these optimizations constitute the first large-scale LLM serving system in production that efficiently covers the Hybrid SWA + MoE + multimodal composite architecture.

---

## 论文详细总结（自动生成）

好的，以下是对论文《Full-Pipeline Inference Optimization for MiMo-V2.5 Series: Pushing Hybrid SWA Efficiency to the Limit》的详细中文总结。

***

### 1. 论文的核心问题与整体含义（研究动机和背景）

- **核心问题**：MiMo-V2.5模型家族采用了**混合滑动窗口注意力（Hybrid SWA）**、**稀疏混合专家（MoE）** 和**多模态编码器**等先进架构。虽然这些架构在理论上能显著降低注意力计算的复杂度（约7倍）和KVCache存储开销，但在大规模生产环境中，这些理论优势难以直接转化为实际的效率提升。如何通过系统性的工程优化，跨越理论与生产实践之间的鸿沟，实现Hybrid SWA + MoE + 多模态复合架构的高效推理，是本研究的核心问题。
- **研究动机**：
    - **长上下文推理的矛盾**：强推理能力需要建模长程依赖，这通常伴随着巨大的注意力和KVCache开销。Hybrid SWA旨在解决此矛盾，但其本身带来的管理复杂性（如KVCache命中率、前缀匹配、层次间一致性）是新的挑战。
    - **工程化挑战**：Hybrid SWA需要特殊的KVCache管理（如滑动窗口和全局注意力的混合），主流框架（如早期SGLang）支持不完善。MoE架构对分布式调度和负载均衡提出了高要求。多模态编码器在长视频、大图像场景下成为吞吐瓶颈。调度策略（Prefill/Decode）也需要精细优化。
- **整体含义**：本文是小米MiMo团队首篇关于MiMo-V2.5系列推理优化的实践报告，展示了如何通过**系统性、端到端**的工程优化，将先进的模型架构的理论效率潜力在**大规模生产**中变为现实，最终降低了推理成本，并回馈给用户（通过API降价）。

### 2. 论文提出的方法论：核心思想、关键技术细节

论文的核心思想是**对推理全流水线进行系统性的协同优化**，包括以下几个关键方面：

#### **KVCache系统重构**
- **核心思想**：打破传统单一KVCache池的束缚，实现SWA层的严格$O(W)$存储（W为滑动窗口大小），并利用SWA特性提升缓存命中率。
- **关键技术细节**：
    1.  **KVCache双池设计**：物理上隔离Full Attention和SWA的KVCache。SWA池仅需存储窗口内的KV，容量提升约7倍。
    2.  **逐层KVCache预取**：利用SWA层只需加载少量KVCache的特性，通过逐层调度实现Host-to-Device传输与计算的重叠，将缓存读取开销降至接近零。
    3.  **SWA感知的前缀缓存树**：针对传统RadixAttention在SWA下会返回“伪命中”的问题，修改了匹配规则：
        - **窗口安全长度匹配**：除Token序列匹配外，还检查尾部W个Token在SWA池中是否有有效槽位，裁剪命中长度。
        - **淘汰与请求生命周期绑定**：在请求结束、每次解码生成后触发SWA无效KV的释放。
        - **节点双索引**：前缀树节点同时记录Full Attention索引（逻辑顺序）和SWA段映射（窗口安全），允许独立淘汰超出窗口的SWA段。
    4.  **缓存命中率优化**：提出多种场景下的规则来保持设备间（GPU Host/Device, L1/L2/L3）SWA状态的一致性，例如在设备SWA完整但主机不足时主动补足，防止高频率长序列的L3缓存被过早淘汰，以及为中短序列保留固定位置的SWA KV。

#### **高性能分布式缓存基础设施（GCache）**
- **核心思想**：构建一个高性能、低成本的分布式KVCache系统，作为L3缓存，以延长缓存TTL（Time-To-Live）并提高命中率。
- **关键技术细节**：
    - **架构设计**：采用去中心化元数据管理（一致性哈希）、支持内存/磁盘/远程三层缓存（内存热数据、磁盘冷数据）、共享内存持久化（重启不丢失）。
    - **网络优化**：优先使用GPU网卡通信，并进行NUMA绑定和同轨道亲和性优化，单进程RDMA读吞吐可达170 GB/s（非GDR）至350 GB/s（GDR）。
    - **存储成本优化**：采用**与GPU机器共部署**的策略，利用GPU机器空闲内存和NVMe SSD作为缓存，实现零额外存储成本。
    - **可靠性保障**：通过一致性哈希、请求预分组、硬件故障探测和SDK超时重算等机制，面对GPU机器高故障率时保证在线推理基本不受影响。

#### **调度优化**
- **KVCache与负载亲和性调度**：开发LLM-Router，在多个Prefill实例中选择已缓存当前请求前缀的节点，同时考虑负载均衡。核心公式：`score(worker) = matchWeight * prefixMatchPercentage - normalizedLoad`。此举将L2缓存命中率提高约25%，节点输入吞吐提高约30%。
- **TTFT（首Token延迟）优化**：在调度队列中，优先处理**未缓存Token量更少**的请求，避免缓存友好的请求被长请求阻塞。并加入等待时间惩罚机制防止请求饥饿。此策略可在不影响短请求的前提下，将长请求的TTFT P90降低30%。

#### **Prefill优化**
- **并行配置优化**：利用优化的SWA KVCache存储更少，将专家并行度（EP）减半，提升了约40%的端到端性能。
- **长度分桶策略**：针对不同缓存前缀长度的请求（0-64K, 64K-256K, 256K-1M）划分到不同桶中，避免短请求被长请求拖慢（如DP Attention同步、Chunked Prefill干扰）。
- **MoE负载均衡**：得益于预训练的均衡设计，推理时无需额外策略，平均专家负载因子已达0.85。
- **NUMA冲突解决**：通过禁用系统`numa_balancing`内核参数，解决了SGLang配置与系统调度冲突导致的性能下降问题，提升约10%性能。

#### **Decode优化**
- **GPU内存优化**：包括支持解码阶段的SWA KVCache（容量增至约5倍），采用PD分离架构并预分配KVCache到CPU内存，以及优化CUDA Graph参数以减少内存浪费。
- **MTP（多Token预测）优化**：在预填充阶段也支持MTP，使解码早期（0-128 Token）加速比达到2.3倍，128-256 Token加速比达到1.5倍，显著降低了Agent场景的解码成本。

#### **多模态推理优化**
- **编码器性能提升**：通过异步传输、数据并行、跨请求批处理，将编码器QPS从15提升至30，P90延迟降低。
- **预处理优化**：将图像预处理（resize等）移植到GPU，采用多进程并行下载/解码，并将下载与推理流水线化。对于1小时视频，通过并行解码将端到端延迟从156秒降至23秒。
- **缓存优化**：通过一致性哈希路由请求到相同编码器，将多模态缓存命中率提升30%。使用共享内存实现单节点内多GPU间的嵌入缓存共享。

### 3. 实验设计：数据集/场景、Benchmark、对比方法

- **数据集/场景**：论文未明确使用特定公开数据集，而是在**小米的MiMo API生产环境**中进行验证。实验场景涵盖了**长上下文推理**、**Agentic交互**（多轮对话，上下文持续增长）、**多用户共享系统提示**以及**多模态输入（图像、长视频）** 等。
- **Benchmark**：
    - **端到端性能**：使用QPS（每秒查询数）、平均延迟、P90/P99延迟作为主要指标。
    - **缓存系统**：KVCache命中率（L2和整体）、缓存TTL。
    - **调度**：TTFT（首Token延迟）、节点吞吐量。
    - **SWA效果**：KVCache容量效率（与最优Full Attention模型对比）。
- **对比方法**：
    - **KVCache系统**：对比了早期未优化的SGLang v0.5.5实现。
    - **模型KVCache效率**：与DeepSeek-V4、MiniMax-M2、Kimi-K2、Qwen3.5等模型进行了KVCache内存的理论估算对比（图2）。
    - **调度策略**：对比了传统FCFS（先来先服务）调度策略。
    - **编码器**：对比了优化前与优化后的性能（表1）。
    - **MTP**：对比了未启用MTP与启用MTP优化的加速效果。

### 4. 资源与算力

- **明确提到的信息**：
    - GPU机器配备**8× 400G高性能网卡**。
    - 通过GCache与GPU机器共部署，利用了机器的内置NVMe SSD。
    - 使用了多节点多GPU部署，具体GPU型号和数量**未指明**。
    - 训练时长或总GPU小时数**未提及**。
    - 模型规模：参数量超过500B（图2b）。

### 5. 实验数量与充分性

- **实验数量**：论文围绕各个优化点提供了生产环境下的关键消融实验结果，包括：
    - KVCache命中率优化（设备状态一致性规则）。
    - 调度策略的TTFT对比（图6）。
    - KVCache亲和性调度对命中率和吞吐量的影响（数字指标）。
    - NUMA优化对性能的影响（约10%）。
    - Prefill并行配置优化（约40%）。
    - 长度分桶策略效果（图7）。
    - 多模态编码器前后性能对比（表1）。
    - MTP优化加速比（数字指标）。
    - 视频解码优化时间对比。
    - MoE负载均衡因子数据（图8）。
- **实验充分性与公正性**：
    - **充分性**：实验覆盖了论文所提出的所有主要优化方向，并提供了有说服力的数字或图表，证明了优化的有效性。尤其是在KVCache、调度和编码器优化上，提供了定量的性能提升（如命中率25%↑，吞吐30%↑，延迟P90-30%↓）。
    - **客观性**：对比实验设置明确（如“之前”和“之后”），且提到了一个大约70层的模型具体配置，具有可复现性基础。结果在生产环境中验证，增加了可信度。
    - **公正性**：论文诚实地指出了在某些优化（如MoE均衡性）上当前不采取特定策略，并比较了MiMo模型与业界其他模型的KVCache成本，承认DeepSeek-V4在某些方面更优。

### 6. 论文的主要结论与发现

1.  **Hybrid SWA的理论优势可以通过系统性的工程优化在生产中实现**：通过重构KVCache系统（双池、层预取、SWA感知前缀树）、构建GCache分布式缓存和优化调度，成功实现了KVCache容量约7倍的提升，并在生产环境达到了**93%以上的平均KV Cache命中率**，重度用户可达95%以上。
2.  **缓存受益于SWA和长TTL的协同效应**：SWA极小存储成本使得在同样的预算下能容纳更多并发的缓存，GCache提供了极低成本的巨大L3容量，极大延长了缓存TTL，从而实现了高命中率。
3.  **精细化的调度是提升系统效率的关键**：通过KVCache亲和性调度和“未缓存Token最少优先”的TTFT优化策略，不仅提高了缓存利用率和吞吐量，还有效优化了长尾延迟。
4.  **多模态推理中的流水线化和并行化很重要**：将GPU图像预处理、并行视频解码、以及下载与推理的流水线化，能将编码器性能翻倍，并将长视频处理时间缩短近7倍。
5.  **MoE架构在训练有素的模型上可以省去推理时的负载均衡策略**，但NUMA冲突、并行配置、长度分桶等系统级问题对性能有显著影响。
6.  **首次在**大规模生产系统中**高效覆盖了 `Hybrid SWA + MoE + 多模态` 这一复杂架构**。

### 7. 优点：方法或实验设计上的亮点

- **系统性与端到端视角**：本文不是孤立的优化一个点，而是对整个推理流水线（从KVCache、调度、Prefill、Decode、多模态处理）进行了全面的分析和协同优化，这种全局视角是论文最显著的优点。
- **工程实践深度**：论文深入阐述了大量工程细节，如NUMA冲突解决、GCache共部署的可靠性设计、SWA感知前缀树的双索引设计、设备间SWA状态一致性维护等，体现了深厚的工程实践功底。
- **生产环境验证**：所有的优化成果都在真实的API服务生产环境中得到了验证，提供了极具说服力的数据，而非仅仅停留在模拟或论文基准上。
- **开源贡献**：明确表明将部分优化贡献回SGLang社区，体现了开放共赢的态度，增强了论文的实用价值。

### 8. 不足与局限

- **实验对比不全面**：论文主要展示了自身优化前后的提升，虽然对比了KVCache的理论成本，但**缺乏与其它大模型推理系统（如vLLM、TensorRT-LLM）在端到端性能（如QPS、每Token成本）上的直接、公平的横向比较**。
- **算力资源细节模糊**：未披露训练和推理的具体GPU型号、数量、总计算量等关键资源信息，外界难以准确评估其工程优化的通用性和开销。
- **对模型训练的依赖**：MoE负载均衡良好很大程度上归功于预训练阶段引入了负载均衡目标，这并非通用经验。对于其他未做此优化的MoE模型，推理时可能需要额外的负载均衡策略。
- **缓存命中率计算的细粒度不足**：论文报告了平均93%的命中率，但未详细说明命中率的计算方法（是按Token计数、请求数还是其他），也未讨论长尾场景下的命中率情况。
- **应用场景限制**：论文的优化高度依赖于SWA和MoE架构。对于纯Full Attention、密集MoE或其他模型架构的推广性有限。

***

（完）
