---
title: "Eviction as Estimation: A Fixed-Lag Smoothing View of Test-Time Memory, and When Measuring Beats Accumulating"
title_zh: 驱逐即估计：测试时记忆的固定滞后平滑视角，以及何时测量优于累积
authors: "Maruthi Vemula, Neeraj Praneeth Gajula"
date: 2026-07-27
pdf: "https://arxiv.org/pdf/2607.24667v1"
tags: ["query:sparse-attn"]
score: 9.0
evidence: LLM工作记忆的驱逐策略，KV缓存压缩
tldr: LLM的测试时内存驱逐策略可视为对隐藏信号（token是否重用）的估计问题。现有方法要么在到达时立即提交（H=0），要么需知晓整个未来（Belady最优）。本文提出固定滞后平滑视角，等待有限步数后通过模型自身注意力测量展现效用，再作决策，并实例化为无训练策略RMM。在控制实验中，小内存表现如大内存；但在标准基准上，RMM与H2O相当，略逊于SnapKV，因自然文本中大多数token预测正确，测量退化为累计注意力。主要贡献是提出框架并诚实地对比了测量何时优于累计。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.24667v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1359, \"height\": 345, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.24667v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 792, \"height\": 631, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.24667v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1207, \"height\": 713, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.24667v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1445, \"height\": 247, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.24667v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1123, \"height\": 243, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.24667v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1225, \"height\": 135, \"label\": \"Table\"}]"
motivation: 现有内存驱逐策略要么在线提交（H=0）要么依赖未来知识，缺少等待有限步数后再决定的中间方案。
method: 提出RMM，通过等待固定滞后步骤，观测模型近未来预测的注意力来测量展现效用，再决定驱逐，无需训练。
result: 在控制设置中RMM使小内存表现如大内存；但在NVIDIA KVPress基准上，RMM与H2O相当，输给SnapKV，因自然文本预测正确率高。
conclusion: 贡献是提出固定滞后平滑框架并诚实评估测量何时优于累计，而非达到新SOTA。
---

## 摘要
具有有限工作记忆的语言模型必须反复决定保留哪些存储项。每种部署的方法都在项目到达时做出决定，无论是基于过去（StreamingLLM, H2O）还是基于对未来的猜测（SnapKV）。我们将这个选择重新表述为对隐藏信号（即一个项目是否会被重用）的估计问题，将现有方法置于一个轴上，即提交滞后$H$：在线过滤器和学习预测器在$H=0$时提交，而Belady的离线最优则在完全了解未来的情况下实现。介于两者之间的缺失机制——固定滞后平滑——等待有限步数，观察正确的近期预测所关注的项目，然后才提交。这种测量（demonstrated utility）将Belady的不可观测的未来请求转化为我们可以从模型自身读取的内容。我们将其实例化为一种无需训练的策略RMM，它是H2O的严格泛化，当测量均匀时正好退化为H2O。在重用是内生的且时间上分离的受控设置中，demonstrated utility识别已用记忆的效果远优于累积注意力，且小的有界记忆表现得像大得多的记忆。但在独立的第三方基准测试中，在NVIDIA的KVPress框架内与其自己的SnapKV、H2O和StreamingLLM实现运行时，优势大多消失：RMM在单轮问答中与H2O相当，而在流式多轮设置中则输给H2O和SnapKV。原因很简单：在自然文本上，模型对大多数标记是正确的，因此按正确性加权注意力几乎不改变结果，除非重用是尖锐且内生的（标准基准测试未涉及），否则demonstrated utility会退化为累积注意力。我们的贡献是框架以及一张诚实的“何时测量优于累积”地图，而非新的最先进技术。

## Abstract
A language model with a bounded working memory must repeatedly decide which stored items to keep. Every deployed method decides the moment an item arrives, from the past (StreamingLLM, H2O) or from a guess about the future (SnapKV). We recast the choice as an estimation problem on a hidden signal, whether an item will be reused, placing existing methods on one axis, the commit lag $H$: online filters and learned predictors commit at $H=0$, while Belady's offline optimum sits where the whole future is known. The missing regime in between, fixed-lag smoothing, waits a bounded number of steps, observes which items a correct near-future prediction attended to, and only then commits. This measurement, demonstrated utility, turns Belady's unobservable future request into something we read off the model itself. We instantiate it as a training-free policy, RMM, a strict generalization of H2O that reduces to it exactly when the measurement is uniform. In controlled settings where reuse is endogenous and separated in time, demonstrated utility identifies used memory far better than accumulated attention, and a small bounded memory behaves like a much larger one. But on independent third-party benchmarks, run inside NVIDIA's KVPress harness against its own SnapKV, H2O, and StreamingLLM implementations, the advantage mostly disappears: RMM is on par with H2O for single-turn question answering and loses to both H2O and SnapKV in a streaming multi-turn setting. The cause is simple: on natural text the model is correct about most tokens, so weighting attention by correctness barely changes it, and demonstrated utility collapses onto accumulated attention unless reuse is sharp and endogenous, which standard benchmarks do not exercise. Our contribution is the framework and an honest map of when measuring beats accumulating, not a new state of the art.

---

## 论文详细总结（自动生成）

# 论文详细中文总结

## 1. 论文的核心问题与整体含义（研究动机和背景）

- **核心问题**：在有限工作记忆的语言模型（如 KV cache 或循环状态）中，每个时间步都需要决定保留或驱逐哪些记忆项。现有方法均在项到达时立即决定（H=0），但一项记忆的效用（是否会被重用）本质上是一个未来属性，在到达时刻尚不可知。
- **研究动机**：现有方法要么从过去信息（StreamingLLM 的最近性、H2O 的累积注意力）打分，要么通过学习器预测未来需求（SnapKV，PyramidKV），但都缺少“等待有限步数、收集更多观测后再决定”的中间模式。这种模式在估计理论中称为**固定滞后平滑（fixed-lag smoothing）**，在神经记忆领域尚未被使用。
- **整体含义**：将驱逐问题重新解释为对隐藏信号（项是否会被重用）的估计，用**提交滞后 H** 统一现有方法（H=0 为在线过滤/预测，H→∞ 为 Belady 最优），并引入中间机制——固定滞后平滑，通过测量模型自身的近未来正确预测所使用的注意力（demonstrated utility）来决策，无需额外训练。

## 2. 论文提出的方法论：核心思想、关键技术细节、公式或算法流程

- **核心思想**：将驱逐决策延迟一个固定步数 H，让候选项目先进入临时缓冲区，等待它们被近未来正确预测实际使用后再决定是否提交到永久缓存。这避免了纯在线决策的盲目性，也无需像 Belady 那样知道完整未来。
- **关键技术细节**：
  - **Demonstrated Utility (展现效用)**：对缓存中第 k 个项，在滞后窗口 H 内，计算所有正确预测位置 t'（模型 top-1 预测等于真实下一个 token）对该项的注意力权重之和：
    \[
    DU_H(k) = \sum_{t' \in (t_k, t_k+H]} \mathbb{1}[\text{prediction at } t' \text{ correct}] \cdot a(t' \to k)
    \]
    这个信号是自监督的（不需要额外标签），通过教师强制检查预测正确性。它与 H2O 的唯一区别在于每个注意力项乘以了正确性指示函数。
  - **RMM 策略**：
    1. 每个到达项放入大小为 H 的临时缓冲区（可被读取）。
    2. 当项在缓冲区中经历 H 步后，计算其 demonstrated utility。
    3. 如果它的效用超过当前已提交缓存中最差项的效用，则提交它并驱逐最差项；否则永久驱逐。
    4. 整个策略无需训练，直接插入 KV cache 中。
    5. 当正确性权重均匀（即所有预测都正确）时，demonstrated utility 等于 H2O 的累积注意力，故 RMM 是 H2O 的严格泛化。
- **算法流程（文字说明）**：
  1. 初始化永久缓存容量 K 和临时缓冲区容量 H。
  2. 对于每一步新 token x_t：将其插入临时缓冲区；同时，对缓冲区中每个已存在项，更新其 demonstrated utility（利用当前步的正确性及注意力）。
  3. 当缓冲区中某个项年龄达到 H 步：计算其 demonstrated utility，与永久缓存中最低项比较，决定提交或丢弃。
  4. 查询时，永久缓存和临时缓冲区均参与注意力计算（总容量 K+H，诚实地计入开销）。

## 3. 实验设计：使用了哪些数据集/场景，benchmark 是什么，对比了哪些方法

- **受控实验（机制验证）**：
  - 推理链驱动自身使用：在多跳问答任务中（两个事实+14个干扰项），衡量 demonstrated utility 能否正确识别关键事实。
  - 有界记忆超出预算：流式事实序列，其中部分事实短期内重用，其他永不重用；记忆预算 K=40，测试长轨迹下各个策略保留重用事实的比例。
- **独立第三方基准**：
  - 使用 NVIDIA KVPress 框架（官方实现 SnapKV、H2O、StreamingLLM）。
  - 数据集：LongBench（单轮预填充压缩）——HotpotQA（F1）、TREC（准确率）；LoCoMo（流式多轮，多轮对话，只取后几轮 in-regime 数据）。
  - 预算：保持 50% 和 25% token 两种压缩比例（单轮测试两种，多轮固定 1800 token 预算）。
  - 对比方法：SnapKV、StreamingLLM、H2O、RMM（本文）。
  - 模型：Qwen2.5-7B，eager attention。
- **额外提及但未测试的 benchmark**：SCBench（太长不适用于 eager attention），RULER。

## 4. 资源与算力：文中有无提及？若未说明也指出

- **文中未明确提及 GPU 型号、数量、训练时长**。所有实验基于冻结的 Qwen2.5-7B 模型，无需训练，仅需推理阶段计算注意力和正确性。由于 RMM 无需训练，算力主要用于推理和比较基线运行。具体 GPU 资源未声明。

## 5. 实验数量与充分性：大概多少组实验，是否充分、客观、公平

- **受控实验**：2 种设置（推理链、流式重用）。每种设置结果以图/文展示，样本数未详细说明，但属于机制分析，非竞争性声明。
- **独立基准实验**：
  - 单轮预填压缩：LongBench 的两个任务（HotpotQA、TREC），各在 50% 和 25% 预算下测试，每组 n=40 个样本（论文中提到 "n=40 examples per cell"）。
  - 流式多轮：LoCoMo，8 个对话，预算 1800 token，只报告后几轮（in-regime turns）的 F1 分数。
  - 总计约 40×4 + 8 = 168 个结果点（每个条件一个数值）。
  - 论文强调使用第三方基准框架（KVPress）及其官方基线实现，提高了公平性。直接验证了 RMM 与 H2O 在均匀权重的等价性（作为控制）。
- **充分性评估**：实验数量相对有限，尤其是流式多轮只有 8 个对话，统计显著性可能不足。论文本身承认在标准基准上 RMM 最多与 H2O 相当，甚至输给 H2O 和 SnapKV。没有进行消融实验（如不同 H 值的影响），也没有在更大模型或更长上下文上测试。论文强调这些实验是“诚实的证据”，但不足以支持广泛泛化。

## 6. 论文的主要结论与发现

- **框架贡献**：将驱逐问题视为估计问题，用提交滞后 H 统一现有方法，并提出 fixed-lag smoothing 这一缺失机制。Demonstrated utility 作为 Belady 所需未来请求的可观测替代。
- **受控设置中的正面结果**：当重用是内生的、时间上分离的，demonstrated utility 明显优于累积注意力和最近性（例如，小内存 K=40 下 RMM 保留重用事实接近无界 oracle，而 H2O 和 StreamingLLM 衰减到零）。
- **独立基准上的负面结果**：在 LongBench 单轮设置中 RMM 与 H2O 无统计差异；在 LoCoMo 流式多轮中 RMM 输给 H2O 和 SnapKV。
- **原因分析**：自然文本中模型对大多数 token 预测正确（85-90%），所以正确性权重接近均匀，demonstrated utility 退化为 H2O 的累积注意力。仅当正确性选择性高且局部时（受控设置的情况）两者才分离。标准基准不包含这种尖锐的内生重用模式。
- **最终结论**：框架和桥梁是持久贡献；RMM 不是对部署方法的直接改进；论文提供了“何时测量优于累积”的清晰地图，而非新 SOTA。

## 7. 优点：方法或实验设计上有哪些亮点

- **理论框架统一性**：用单轴“提交滞后”将 filters、predictors、Belady 置于同一框架下，理清了问题结构。
- **可解释的 Belady 桥接**：提出 demonstrated utility，使 Belady 需要的未来请求在神经记忆中可观测且无标签，具有优雅的自监督性质。
- **RMM 的安全泛化**：严格泛化 H2O，在正确性信息无用时自动退化为 H2O，不会更差。
- **实验诚信度**：论文明确区分受控实验（机制展示）和独立基准（负载比较），并诚实地报告了负面结果（RMM 在标准基准上不优于 H2O），避免了“只报喜不报忧”的常见问题。
- **使用第三方框架**：在 NVIDIA KVPress 内评估，与官方基线实现直接对比，减少了实现偏差。
- **成功解释了结果差异**：清晰分析了为何受控实验中好、标准基准中不好（自然文本的正确性权重接近均匀），具有指导未来研究的作用。

## 8. 不足与局限：包括实验覆盖、偏差风险、应用限制等

- **实验规模有限**：
  - 独立基准仅两个任务，样本量小（n=40, 8 conversations），统计效力不足，尤其 LoCoMo 对话数少。
  - 未测试更长上下文（如 SCBench 所需），因为 H2O 和 RMM 都需 eager attention（受限于 8K 左右 token）。
  - 未做消融：未测试不同 H 值的影响，未比较不同正确性代理（如置信度代替 teacher forcing）。
- **依赖 teacher forcing**：计算 demonstrated utility 需要真实下一个 token 来确定预测正确性，这在推理部署中不可用（除非有额外的验证器或置信度阈值）。论文承认此限制。
- **流式多轮反直觉输给 H2O**：论文解释为“将缓存集中在早期正确答案关注的部分，却不符合后续独立问题的需求”，这一解释未通过额外实验验证（如分析缓存内容）。
- **未解决长上下文或大模型扩展**：仅在 Qwen2.5-7B 上测试，未验证在其他尺寸或架构上的行为。
- **应用限制**：RMM 要求 eager attention 来计算注意力分数，导致推理速度低于使用 FlashAttention 等方法，实际部署可能不具优势。论文提到可以用小窗口重计算来缓解，但未实现。
- **声称“从不更差”但实际输了**：RMM 在 LoCoMo 流式上输给 H2O，虽然理论上是泛化但表现更差，需要解释为何在具体条件下安全性质不保证。
- **基准选择偏差**：论文选出最适合平滑机制的场景（受控）和不利场景（标准），但未在混合场景或合成重用比例可调的 benchmark 上系统测试，无法精确量化“何时测量优于累积”的边界。

（完）
