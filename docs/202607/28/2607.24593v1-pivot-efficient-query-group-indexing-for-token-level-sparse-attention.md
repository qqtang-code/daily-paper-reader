---
title: "PIVOT: Efficient Query-Group Indexing for Token-Level Sparse Attention"
title_zh: PIVOT：面向令牌级稀疏注意力机制的高效查询组索引
authors: "Hong Liu, Yuan Cheng, Lin Niu, Yi Su, Yufei Xue, Anmin Liu, Guanghua Yu, Jianchen Zhu"
date: 2026-07-27
pdf: "https://arxiv.org/pdf/2607.24593v1"
tags: ["query:sparse-attn"]
score: 10.0
evidence: 令牌级稀疏注意力索引，提高注意力效率
tldr: Token级稀疏注意力索引器需要为每个查询扫描所有前驱token，导致O(L²)开销。PIVOT利用邻近查询top-k重叠和分数长尾分布，对一组查询共享一次全前缀扫描获得候选集，再从中挑选各查询的top-k。在DeepSeek-V3.2和GLM-5.1上匹配精度，索引器加速4倍，端到端延迟降低1.6倍。提出一种训练无关的替代方案，降低长上下文推理开销。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.24593v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 776, \"height\": 461, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.24593v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1855, \"height\": 500, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.24593v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1795, \"height\": 734, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.24593v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1851, \"height\": 458, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.24593v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1831, \"height\": 1031, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.24593v1/fig-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 1846, \"height\": 610, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.24593v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1845, \"height\": 792, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.24593v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 886, \"height\": 328, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.24593v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 887, \"height\": 399, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.24593v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 886, \"height\": 270, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.24593v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 886, \"height\": 255, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.24593v1/table-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 1117, \"height\": 566, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.24593v1/table-007.webp\", \"caption\": \"\", \"page\": 0, \"index\": 7, \"width\": 1116, \"height\": 454, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.24593v1/table-008.webp\", \"caption\": \"\", \"page\": 0, \"index\": 8, \"width\": 1117, \"height\": 687, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.24593v1/table-009.webp\", \"caption\": \"\", \"page\": 0, \"index\": 9, \"width\": 1117, \"height\": 631, \"label\": \"Table\"}]"
motivation: 现有token级稀疏注意力索引器需逐查询扫描所有前驱token，造成O(L²)冗余开销。
method: PIVOT通过代理查询共享前缀扫描，将一组查询聚合为候选集，再为每个查询从中挑选top-k，有两种速度与精度折衷变体。
result: 在LongBench和RULER上匹配原始DSA索引器精度，索引器加速达4倍，端到端延迟降低1.6倍。
conclusion: PIVOT是一种训练无关的插拔式替换方案，有效缓解长上下文下稀疏注意力索引瓶颈。
---

## 摘要
令牌级稀疏注意力机制（如生产系统中的DeepSeek稀疏注意力DSA）虽使下游注意力计算高效，却将瓶颈转移至为其提供输入的索引器。为每个查询选择top-k令牌时，索引器仍需对每个前置令牌进行评分，导致序列长度为L时每层的计算复杂度为O(L^2)。我们观察到这种逐查询扫描存在大量冗余：相邻查询选择的top-k令牌高度重叠，且索引器评分沿关键轴呈长尾分布。我们利用这些特性提出了PIVOT（通过一次完整前缀遍历的代理索引），这是一种无需训练、可即插即用替换DSA索引器的方案，通过在一组相邻查询间共享一次前缀扫描来降低计算量。PIVOT将一组查询聚合成单个代理查询，执行一次共享的完整前缀扫描以获取候选集，然后为每个查询从该候选集中选择top-k。两种变体在速度与保真度间权衡：PIVOT-Reuse在组内共享代理的top-k以追求最高速度；而PIVOT-Refine使用每个查询的索引器对候选集重新评分，然后选择各自的top-k，以较小的额外开销匹配密集索引器的精度。单一算法覆盖两个推理阶段，仅在组形成方式上有所区别：预填充阶段按连续查询的固定大小分组，解码阶段则在一次多令牌预测步骤中对同时解码的查询进行分组。在DeepSeek-V3.2和GLM-5.1上，基于LongBench和RULER的评估表明，PIVOT在匹配密集DSA索引器精度的同时，可将索引器加速高达4倍，并在长上下文场景下将端到端延迟降低高达1.6倍。

## Abstract
Token-level sparse attention, as implemented by DeepSeek Sparse Attention (DSA) in production systems, makes the downstream attention efficient but shifts the bottleneck to the indexer that feeds it. To select the top-k tokens for each query, the indexer must still score every preceding token, incurring a cost of O(L^2) per layer for a sequence of length L. We observe that this per-query scan is largely redundant: nearby queries select highly overlapping top-k tokens, and the indexer scores are long-tailed along the key axis. We exploit these properties in PIVOT, Proxy Indexing Via One full-prefix Traversal, a training-free, drop-in replacement for the DSA indexer that shares one prefix scan across a group of nearby queries. PIVOT aggregates a group into a single proxy query, performs one shared full-prefix scan to obtain a candidate set, and then selects a top-k for each query from that set. Two variants trade speed for fidelity: PIVOT-Reuse shares the proxy top-k across the group for maximum speed, whereas PIVOT-Refine re-scores the candidate set with the indexer of each query and then selects an individual top-k, matching the dense indexer at a small additional cost. A single algorithm covers both inference phases, differing only in how groups are formed: fixed-size groups of consecutive queries in prefill, and the queries decoded together in one multi-token prediction (MTP) step in decode. On DeepSeek-V3.2 and GLM-5.1 across LongBench and RULER, PIVOT matches the accuracy of the dense DSA indexer while accelerating it by up to 4x and reducing end-to-end latency by up to 1.6x at long context.

---

## 论文详细总结（自动生成）

# PIVOT：高效查询组索引实现令牌级稀疏注意力 —— 论文详细总结

## 1. 论文的核心问题与整体含义（研究动机与背景）

- **核心问题**：大型语言模型（LLM）在长上下文推理时，其使用的**令牌级稀疏注意力**（Token-Level Sparse Attention）机制（如DeepSeek的DSA）虽然降低了主注意力计算成本，但将算力瓶颈转移到了**索引器**（Indexer）上。索引器需要为每个查询（Query）对之前的所有前置令牌（Key）进行评分以选出top-k，这导致了**O(L²)的二次复杂度**。
- **背景与现状**：随着上下文窗口增长至百万级令牌，索引器成为推理过程中最昂贵的部分（在200K令牌长度下，索引器在预填充阶段占比高达81%，在解码阶段占比41%）。尽管已有加速方法（如降低精度、沿令牌/头/层轴方向优化），但**查询轴上的冗余仍未得到解决**——每个查询依然独立执行一次全前缀扫描。
- **研究动机**：作者观察到相邻查询的top-k选择存在**高度重叠**，索引器评分沿关键轴呈**长尾分布**。这为通过共享查询间的计算来削减成本提供了可能。

## 2. 论文提出的方法论：核心思想、关键技术细节

- **核心思想**：通过**跨查询共享一次前缀扫描**来替代逐查询扫描。PIVOT将一组相邻查询（组）聚合成一个**代理查询**（Proxy Query），执行一次全覆盖的扫描，得到一个**候选集**（Candidate Set），然后让组内每个查询从该候选集中挑选自己的top-k。
- **关键技术细节**：
    - **代理查询生成**：对组内所有查询的索引器查询向量和门控权重取**逐头均值**（Mean Pooling），形成代理查询。
    - **共享前缀扫描**：代理查询对所有前置令牌执行一次评分（使用DSA相同的索引器函数），获得代理分数。
    - **两种变体**：
        - **PIVOT-Reuse（极速版）**：直接将代理查询的top-k结果作为组内所有查询的top-k，无额外计算，复杂度为O(L)。
        - **PIVOT-Refine（精确版）**：先从代理分数中保留top-c候选集（c = 2k，k < c << L），然后再让每个查询**利用自己的真实索引器对候选集重新评分**，从中选出各自的top-k。复杂度介于O(L)和O(gL)之间。
    - **分组策略**：
        - **预填充阶段**：将同时可用的查询位置划分为**固定大小的组**（g=4）。
        - **解码阶段**：利用已有的**多令牌预测技术**，将同一推理步内解码的一个目标令牌和多个草稿令牌自然组成一个组（g=4，无额外开销）。
- **算法流程**（文字描述）：
    1. 形成查询组。
    2. 若是短序列，则退化为DSA的密集扫描；否则执行代理查询生成和共享前缀扫描（粗粒度步）。
    3. 根据变体类型（Reuse/Refine）执行细粒度选择：Reuse直接使用代理top-k；Refine获取候选集，每个查询对候选集进行重新评分，选出各自的top-k。
    4. 将结果（索引集）通过与原DSA相同的接口传递给下游Sparse MLA。
- **公式与关键设定**：代理索引分数计算公式与DSA一致（ReLU + 求和）。组大小g=4，候选预算c=2k=4096，使用均值池化。

## 3. 实验设计：数据集、基准与对比方法

- **模型**：
    - **DeepSeek-V3.2**
    - **GLM-5.1**
- **数据集与基准**：
    - **LongBench**：覆盖单文档QA、多文档QA、摘要、少样本学习、合成任务、代码补全等多个维度的真实长文档任务。
    - **RULER**：可控长度的长上下文基准测试，评估检索和推理能力（从4K到128K）。
- **对比方法**：
    - **密集DSA索引器**（基线）
    - **HISA**（沿令牌轴加速）
    - **MISA**（沿头轴加速）
    - **IndexCache**（沿层轴加速）
    - 也报告了PIVOT与IndexCache的组合结果（+IC）。

## 4. 资源与算力

- **文中明确说明**：所有模型均在**NVIDIA H20 GPU**上使用 **vLLM** 提供服务，并采用一致的配置进行对比。
- **未明确说明的细节**：论文**未提及**具体的GPU数量、训练时长或推理所用的具体算力消耗量。这属于资源报告的不足。

## 5. 实验数量与充分性

- **实验组数**：论文包含多组系统实验：
    - **主实验**（表1）：在LongBench（6个子任务）和RULER（6个长度级别）上对比了约10种方法/变体。
    - **效率实验**（图4）：在7个上下文长度（4K至256K）下对比了Kernel加速和端到端加速。
    - **消融实验**（表2-5）：针对代理聚合策略、组大小g、候选预算c、部署阶段（预填充/解码）等4个关键因素进行了系统分析。
    - **附录**（表6-9）：补充了跨模型、跨层的细粒度观察和完整消融数据。
- **实验充分性**：
    - **优点**：实验非常充分，覆盖了不同模型、不同基准、不同上下文长度、多个超参数和设计选择，对比方法具有代表性。
    - **客观性**：结果报告了均值/分数，并标出了最佳值。消融实验设计合理，验证了各设计选择的必要性。
    - **潜在偏差**：在LongBench上所有方法分数接近（由于任务长度较短），导致无法有效区分优劣；但RULER在长上下文时拉开了差距，验证了PIVOT在关键场景的优势。

## 6. 论文的主要结论与发现

- **准确性**：在LongBench和RULER上，**PIVOT-Refine在所有长度下均匹配了密集DSA的准确性**。PIVOT-Reuse在中等长度下保持良好，在超长上下文（128K）时略有下降。
- **效率**：PIVOT将索引器算力**加速高达4倍**，**端到端延迟降低高达1.6倍**。
- **冗余确认**：相邻查询的top-k高度重叠（共享率80%-90%），索引器评分呈长尾分布，这两个关键观察被实验证实。
- **正交性**：PIVOT可以与已有的加速方法（如IndexCache）叠加使用，几乎无损，表明其独立于其他轴线。
- **一次算法覆盖两阶段**：预填充和解码均可使用同一算法（仅分组方式不同），PIVOT与MTP自然结合，无额外开销。

## 7. 优点：方法或实验设计上的亮点

- **新颖性**：首次提出沿**查询轴**进行索引加速的思路，弥补了现有方法的空白。
- **简洁高效**：基于简单的观察（高度重叠和长尾分布）设计了直观的方案（组共享扫描），无需重新训练，可直接替换。
- **设计精妙**：两种变体（Reuse/Refine）提供了清晰的速度-精度折衷，给了用户选择权。与MTP结合利用了已有的解码架构，不增加额外开销。
- **实验全面**：在多个模型、多个基准和多个超参数设置下进行了系统消融，验证了关键观察和设计选择的有效性。
- **正交性验证**：展示了PIVOT可以与现有加速方法（IndexCache）叠加，证明其兼容性强。

## 8. 不足与局限

- **长上下文下的准确性损失**：PIVOT-Reuse在极长上下文（128K）下准确性下降，说明完全依赖代理查询可能丢失细节。PIVOT-Refine虽匹配但引入了额外计算。
- **实验资源报告不完整**：未明确给出GPU数量、训练时长或推理时的总算力消耗。这给复现和成本评估带来难度。
- **基线对比局限性**：LongBench任务由于长度相对较短，所有稀疏方法表现接近，未能充分区分优劣。实验需要更极端的长度才能体现差异。
- **候选预算超参数敏感**：候选预算c的默认值（2k）在消融中被证明是关键，但针对不同模型或任务，最优c可能需要人工调整，论文未提供自动选择机制。
- **仅覆盖特定模型**：实验集中在DeepSeek-V3.2和GLM-5.1上，虽然都是大型生产模型，但尚未证明PIVOT在更广泛的模型或注意力架构（如FlashAttention）下的通用性。
- **仅预填充/解码分组方式固定**：预填充阶段的分组大小g固定为4，未探讨动态分组或自适应分组的可能性。

（完）
