---
title: "FreqDepthKV: Frequency-Guided Depth Sharing for Robust KV Cache Compression in Long-Context LLM Inference"
title_zh: "FreqDepthKV: 频率引导的深度共享实现长上下文LLM推理中鲁棒的KV缓存压缩"
authors: "Anna Córdoba, Adam Puente Tercero, Nerea Angulo Hijo, Mar Linares Tercero, Julia Barrientos, Ainhoa Miranda, Jesús Olivera"
date: 2026-07-07
pdf: "https://arxiv.org/pdf/2607.06519v1"
tags: ["query:sparse-attn"]
score: 10.0
evidence: 通过频率引导的深度共享进行KV缓存压缩
tldr: 长上下文LLM推理受KV缓存内存和带宽成本限制，但激进压缩会丢失层特异性证据。FreqDepthKV将相邻层KV状态分解为共享低频深度分量和稀疏高频残差，利用轻量在线探头根据注意力头对重构敏感度分配缓存模式。在长上下文问答、检索、摘要等基准上，以32k预填窗口达到58.3 Exact Match，解码吞吐70.4 tokens/s，峰值内存6.2GB，有效压缩比3.9x。该方法无需重训练，自适应压缩策略显著提升长上下文推理效率。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.06519v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1213, \"height\": 671, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.06519v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1230, \"height\": 657, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.06519v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1646, \"height\": 462, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.06519v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1834, \"height\": 427, \"label\": \"Table\"}]"
motivation: 解决长上下文LLM推理中KV缓存内存和带宽瓶颈，避免激进压缩导致检索和推理证据丢失。
method: 将相邻层KV状态分解为共享低频深度分量与稀疏高频残差，在线探头按注意力头对重构logits贡献分配缓存模式。
result: "在32k预填窗口下，Exact Match 58.3，F1 63.0，ROUGE-L 32.5，pass@1 48.1；解码吞吐70.4 tokens/s，TTFT 2.06s，峰值内存6.2GB，有效压缩比3.9x。"
conclusion: FreqDepthKV通过频率引导的深度共享实现自适应压缩，无需重训练即在长上下文推理中保持精度并显著降低资源消耗。
---

## 摘要
长上下文LLM推理日益受到KV缓存的内存和带宽成本的限制，然而激进的压缩可能会移除检索和多步推理所需的逐层证据。我们提出FreqDepthKV，一种推理时缓存压缩方法，它将相邻层KV状态分解为共享的低频深度分量和稀疏的高频残差。一个轻量级的在线探针根据注意力头对重建敏感的注意力logits的贡献，将它们分配到共享深度、残差深度或精确缓存模式，使得压缩策略能够适应提示结构而无需重新训练。在长上下文问答、针检索、摘要和代码生成基准测试中，FreqDepthKV在显著更小的缓存预算下保持了任务准确性。对于32k令牌的预填充窗口，FreqDepthKV达到了58.3的精确匹配、63.0的F1、32.5的ROUGE-L和48.1的pass@1，与完整KV高度匹配，同时优于之前的压缩缓存方法。它还将解码吞吐量提高到70.4 tokens/s，将TTFT降低到2.06秒，并将峰值KV内存降低到6.2 GB，实现了3.9倍的有效压缩比。

## Abstract
Long-context LLM inference is increasingly limited by the memory and bandwidth cost of KV caches, yet aggressive compression can remove the layer-specific evidence needed for retrieval and multi-step reasoning. We introduce FreqDepthKV, an inference-time cache compression method that factorizes adjacent-layer KV states into shared low-frequency depth components and sparse high-frequency residuals. A lightweight online probe assigns attention heads to shared-depth, residual-depth, or exact cache modes according to their contribution to reconstruction-sensitive attention logits, allowing the compression policy to adapt to prompt structure without retraining. Across long-context question answering, needle retrieval, summarization, and code generation benchmarks, FreqDepthKV preserves task accuracy under substantially smaller cache budgets. With a 32k-token prefill window, FreqDepthKV reaches 58.3 Exact Match, 63.0 F1, 32.5 ROUGE-L, and 48.1 pass@1, closely matching full KV while outperforming prior compressed-cache methods. It also improves decoding throughput to 70.4 tokens/s, reduces TTFT to 2.06 seconds, and lowers peak KV memory to 6.2 GB, achieving a 3.9x effective compression ratio.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义（研究动机和背景）

- **核心问题**：长上下文大语言模型（LLM）推理中，KV缓存的大小和内存带宽成为主要瓶颈。自回归解码时，每个生成的令牌需要关注所有先前令牌、层和头的缓存，导致内存和计算开销随上下文长度线性增长。
- **现有方法的不足**：
  - 令牌驱逐、量化、结构化共享等方法主要关注序列维度或精度维度的冗余，忽略了相邻Transformer层之间在深度维度上存在的缓存结构冗余。
  - 均匀的深度压缩（如MiniCache）在检索敏感和推理密集的提示中会失效：一些层看似冗余，但实际包含高频残差信息（如关键证据令牌、代码依赖、推理链），一旦被擦除会导致注意力logits偏移，改变生成结果。
- **整体含义**：本文提出一种频率引导的深度共享方法（FreqDepthKV），旨在保留低频频深度分量进行共享的同时，针对性地保留稀疏的高频残差，从而在激进压缩下保持长上下文任务的准确性。

### 2. 论文提出的方法论：核心思想、关键技术细节、公式或算法流程

- **核心思想**：将相邻层KV缓存分解为低频深度分量（可共享）和稀疏高频残差（保留检索敏感证据）。通过在线探针估计每个注意力头在三种缓存模式（共享深度、残差深度、精确）下的重建损失，自适应选择最优模式。
- **关键技术细节**：
  1. **深度-频率分解**：对每\( B \)个相邻层，将KV状态沿深度轴堆叠，使用固定DCT基变换（\( \mathbf{F}_B \)）分离缓慢变化的层间分量（低频）与高频偏差：
     \[
     \mathbf{Z}_{b,h}^K = \mathbf{F}_B \mathbf{X}_{b,h}^K, \quad \mathbf{Z}_{b,h}^V = \mathbf{F}_B \mathbf{X}_{b,h}^V
     \]
     第一组系数作为低频共享分量，其余作为高频残差。
  2. **在线头部路由**：在预填充阶段，从最近令牌、文档边界和高熵注意力行中采样探针位置\( P \)，对每个头比较三种模式的重建注意力logits差异：
     \[
     \mathcal{L}_{b,h}(m) = \frac{1}{|P|} \sum_{t \in P} \left\| \frac{\mathbf{Q}_{\ell,h,t} \tilde{\mathbf{K}}_{\ell,h}^{(m)\top}}{\sqrt{d_h}} - \frac{\mathbf{Q}_{\ell,h,t} \mathbf{K}_{\ell,h}^\top}{\sqrt{d_h}} \right\|_2^2 + \lambda \Omega(m)
     \]
     选择最小损失的模态（SHARED/RESIDUAL/EXACT）。
  3. **稀疏残差存储**：对于残差深度头，通过每个令牌的最大logit扰动分数\( s_{b,h,t} \)保留Top-\( r_{b,h}T \)个令牌的高频系数，其余丢弃。
  4. **解码时融合**：共享系数广播到整个层块，残差系数仅对索引令牌添加，避免显式物化完整缓存。
- **算法流程**：
  - 预填充：对每个头执行DCT分解 → 在线路由（计算三种模式损失，选最优） → 按路由结果决定残留集。
  - 解码：使用压缩表示重建KV，融合注意力核。
  - 预算控制：通过调节\( \lambda \)使峰值KV内存满足预算。

### 3. 实验设计：数据集、场景、基准、对比方法

- **数据集与场景**：
  - **问答与检索**：LongBench、Needle-in-a-Haystack、L-Eval、NarrativeQA、Qasper。
  - **摘要**：GovReport、MultiNews。
  - **代码生成**：HumanEval、MBPP。
- **基准（评估指标）**：Exact Match（EM）、F1、ROUGE-L、pass@1，以及系统指标（解码吞吐量tokens/s、首令牌时间TTFT、峰值KV内存、有效压缩比）。
- **对比方法**：Full KV（完整缓存）、StreamingLLM、H2O、Scissorhands、SnapKV、PyramidKV、KVQuant、KIVI、MiniCache。所有方法在相同32k令牌预填充窗口下，先在一个保留子集上调优压缩预算，再固定预算跨数据集测试。

### 4. 资源与算力

- **文中未明确说明**：论文仅提到所有实验使用相同的长上下文解码器（未指定模型名、参数规模）、32k令牌预填充窗口，以及推理时压缩方法无需重训练。未给出GPU型号、数量、训练时长等信息。因此，无法量化算力消耗。

### 5. 实验数量与充分性

- **实验数量**：
  - 主结果（表1）：在多个任务上对比9种基线，报告5个任务指标和4个系统指标。
  - 消融实验（表2）：9个变体（去掉各组件、不同块大小），覆盖质量与效率指标。
- **充分性评价**：
  - ✅ 覆盖多种长上下文任务（问答、检索、摘要、代码生成），任务类型全面。
  - ✅ 对比基线覆盖了令牌驱逐、量化、深度共享等主流方法，具有代表性。
  - ✅ 消融实验设计合理，逐步验证每个组件（频率分解、稀疏残差、在线路由、重建损失、精确模式）的贡献。
  - ✅ 实验条件统一（相同预填充窗口、调优过程固定），比较公平。
  - ⚠️ 缺乏对更大模型（如70B+）或更极端上下文长度（128k+）的评估；未报告方差或多次运行结果；未与量化联合压缩的实验。

### 6. 论文的主要结论与发现

- FreqDepthKV在所有任务指标上均优于现有压缩缓存方法，且接近完整KV的性能：EM 58.3 vs Full KV 58.7，F1 63.0 vs 63.4，ROUGE-L 32.5 vs 32.8，pass@1 48.1 vs 48.6。
- 系统效率显著提升：解码吞吐70.4 tokens/s（Full KV为38.2），TTFT 2.06秒（Full KV为2.91秒），峰值内存6.2 GB（Full KV为24.0 GB），有效压缩比3.9×。
- 主要增益来自检索敏感场景（Needle-in-a-Haystack、代码生成），说明保留高频残差对保持层特定证据至关重要。
- 消融实验证实：频率分解、稀疏残差、在线路由、重建损失和精确模式各组件均贡献显著。

### 7. 优点：方法或实验设计上的亮点

- **无需重训练**：所有压缩决策在推理时完成，仅需固定DCT基和轻量在线探针，兼容现有模型。
- **自适应路由**：根据提示结构动态分配头到不同模式，避免均匀压缩导致的关键信息丢失。
- **频率分解视角创新**：将深度冗余视为频率结构，用低频共享+高频残差代替简单层间平均，更精细地保留证据。
- **实验设计合理**：对比基线全面，消融实验系统，系统指标（吞吐、TTFT、内存）与任务指标同时报告，体现实际应用价值。
- **兼容性**：与令牌选择、量化方法正交，可组合使用。

### 8. 不足与局限

- **路由固定**：仅在预填充时决定路由，解码过程中不更新，可能错过新生成令牌引发的新依赖关系。
- **未探索联合优化**：未与精度压缩（量化）或系统级缓存放置策略结合，潜力未充分挖掘。
- **模型规模限制**：实验基于单一大小的解码器（未明确参数规模），未验证在更大模型（如70B、175B）上的可扩展性。
- **缺乏统计显著性**：未报告多次运行的标准差或置信区间，无法判断差异是否显著。
- **探针开销**：虽然轻量，但探针点采样和DCT变换在极长上下文下仍可能增加预填充延迟，论文未充分分析。
- **应用限制**：方法依赖层间冗余假设，对于层结构特殊的模型（如MoE、混合架构）可能需要调整。

（完）
