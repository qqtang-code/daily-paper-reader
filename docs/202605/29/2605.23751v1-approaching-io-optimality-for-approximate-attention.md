---
title: Approaching I/O-optimality for Approximate Attention
title_zh: 接近I/O最优的近似注意力
authors: "Pál András Papp, Aleksandros Sobczyk, Anastasios Zouzias"
date: 2026-05-22
pdf: "https://arxiv.org/pdf/2605.23751v1"
tags: ["query:sparse-attn"]
score: 7.0
evidence: 近似注意力的I/O最优，近线性代价
tldr: 针对大语言模型注意力计算的I/O效率问题，现有方法如FlashAttention的I/O开销与序列长度n呈二次关系。本文受Alman和Song近似注意力框架启发，提出一种I/O高效算法，使I/O成本在多数参数下仅与n近似线性相关。通过理论分析给出各参数区间的下界，证明所提算法接近I/O最优。该工作显著降低了注意力计算的内存访问开销，有助于提升长序列场景下的计算效率。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.23751v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 551, \"height\": 653, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2605.23751v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 569, \"height\": 712, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2605.23751v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1428, \"height\": 762, \"label\": \"Table\"}]"
motivation: 现有注意力计算方法I/O复杂度随n二次增长，远高于理论下界Ω(nd)，需要更高效的算法。
method: 借鉴近似注意力框架，设计I/O高效算法，通过分块和近似计算减少快慢内存数据传输次数。
result: I/O成本从O(n^2)降至接近线性O(n d)（在多数参数范围），且逼近理论下界。
conclusion: 本文算法在I/O效率上达到或接近最优，为长序列注意力计算提供了更高效方案。
---

## 摘要
我们重新审视大型语言模型中注意力的I/O复杂度。给定查询-键-值矩阵$Q,K,V\in\mathbb{R}^{n\times d}$，以及一个快速内存大小为$M$的机器，目标是用尽可能少的数据在快速和慢速内存之间传输，计算“注意力矩阵”$A=\text{softmax}(Q K ^{\top}/\sqrt{d}) V$。现有文献中的方法，最著名的是FlashAttention及其变体，其I/O开销与$n$的平方相关，而平凡下界仅需$\Omega(nd)$的I/O来读取输入和写入输出。在这项工作中，我们提出了一种计算注意力的技术，在大多数参数范围内，I/O开销仅与$n$近似线性相关。这是通过开发受Alman和Song最近的近似注意力框架启发的I/O高效算法实现的。我们还证明了每个参数范围内的相应下界，以表明我们的算法确实接近I/O最优。

## Abstract
We revisit the I/O complexity of attention in large language models. Given query-key-value matrices $Q,K,V\in\mathbb{R}^{n\times d}$, and a machine with fast memory size $M$, the goal is to compute the "attention matrix" $A=\text{softmax}(Q K ^{\top}/\sqrt{d}) V$ with the minimal number of data transfers between fast and slow memory. Existing methods in the literature, most notably FlashAttention and its variants, incur an I/O cost that depends quadratically on $n$, while a trivial lower bound only requires $Ω(nd)$ I/O's to read the inputs and write the output. In this work, we present a technique for computing attention where the I/O cost only depends almost-linearly on $n$ in most parameter regimes. This is achieved by developing I/O-efficient algorithms inspired by the recent approximate attention framework of Alman and Song. We also prove corresponding lower bounds in each parameter regime to show that our algorithms are indeed close to I/O-optimal.