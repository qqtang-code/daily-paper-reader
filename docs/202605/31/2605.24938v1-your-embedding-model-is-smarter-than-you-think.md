---
title: Your Embedding Model is SMARTer Than You Think
title_zh: 你的嵌入模型比你以为的更SMART
authors: "Jianrui Zhang, Hyun Jung Lee, Sukanta Ganguly, Tae-Eui Kam, Donghyun Kim, Yong Jae Lee"
date: 2026-05-24
pdf: "https://arxiv.org/pdf/2605.24938v1"
tags: ["query:multimodal"]
score: 8.0
evidence: 解锁单向量模型的多向量能力用于多模态检索
tldr: 多模态检索中单向量模型丢弃细粒度局部证据，多向量模型需训练且忽略全局表示。SMART框架通过梯度流分析发现对比训练隐式塑造隐藏状态检索几何，在推理时对冻结隐藏状态应用延迟交互，即插即用提升性能。在MMEB-V2上超越SOTA，轻量后训练使单向量模型在视觉文档检索中胜过多向量方法。高效推理增强与微调技术开源。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.24938v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1417, \"height\": 708, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2605.24938v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1440, \"height\": 446, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2605.24938v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1450, \"height\": 590, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2605.24938v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1438, \"height\": 376, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2605.24938v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1448, \"height\": 292, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2605.24938v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1461, \"height\": 613, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2605.24938v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1460, \"height\": 629, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2605.24938v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1456, \"height\": 340, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2605.24938v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1438, \"height\": 423, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2605.24938v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1459, \"height\": 371, \"label\": \"Table\"}]"
motivation: 单向量检索器压缩序列为全局表示，丢失细粒度证据；多向量方法需训练且常缺全局摘要。
method: SMART利用对比训练梯度流，解锁单向量模型隐藏状态的隐式多向量能力，推理时直接延迟交互。
result: 提升多模态检索性能，超越SOTA，轻量后训练使单向量模型在视觉文档检索中胜过多向量模型。
conclusion: 提供即插即用推理增强与高效微调技术，开源代码和权重。
---

## 摘要
多模态检索在很大程度上依赖于单向量检索器，这些检索器将丰富的、连续的标记序列压缩成一个单一的全局表示。虽然高效，但它们丢弃了对密集检索任务至关重要的细粒度局部证据。多向量方法被引入作为解决方案，但它们严格需要训练，并且许多忽略了全局总结表示的必要性。为了解决这个问题，我们引入了SMART，一个解锁标准单向量模型潜在多向量能力的框架。我们首先证明，对池化嵌入的标准对比训练通过梯度流隐式塑造了先前隐藏状态的检索几何结构。通过在推理过程中对这些冻结的隐藏状态直接应用后期交互，SMART作为一个即插即用的升级，持续提升跨不同模态的性能，甚至进一步改进了MMEB-V2上最先进的模型。我们还揭示了SMART的优越性能，因为简单的轻量级后训练不仅节省时间和计算，而且在视觉文档检索上带来了进一步的改进，使得单向量模型能够超越最先进的多向量对应模型。最终，SMART为多模态检索提供了高效推理增强和强大的微调技术。我们在https://github.com/HanSolo9682/SMART开源我们的代码和权重。

## Abstract
Multimodal retrieval relies heavily on single-vector retrievers, which compress rich, sequential token sequences into one single global representation. While efficient, they discard fine-grained, local evidence critical for dense retrieval tasks. Multi-vector approaches were introduced as a solution, but they strictly require training and many ignore the necessity of a globally summarizing representation. To address this, we introduce SMART, a framework that unlocks the latent multi-vector capabilities of standard single-vector models. We first demonstrate that standard contrastive training on the pooled embedding implicitly shapes the retrieval geometry of preceding hidden states via gradient flow. By applying direct late-interaction over these frozen hidden states during inference, SMART acts as a plug-and-play upgrade that consistently improves performance across diverse modalities, improving even the state-of-the-art models further on MMEB-V2. We also reveal SMART's superior performance, as simple lightweight post-training not only saves time and compute, but also brings forth further improvement on Visual Document retrieval, allowing a single-vector model to outperform SoTA multi-vector counterparts. Ultimately, SMART offers both a highly efficient inference enhancement and a powerful finetuning technique for multimodal retrieval. We open source our code and weights at https://github.com/HanSolo9682/SMART.

---

## 论文详细总结（自动生成）

### 论文详细中文总结

#### 1. 论文的核心问题与整体含义（研究动机和背景）
- **核心问题**：多模态检索（如文本、图像、视频、视觉文档）中，主流单向量（single-vector）嵌入模型将整个输入压缩为一个全局表示（如<eot> token），虽然高效但丢弃了细粒度局部证据，导致在依赖局部细节（如文本与视觉标记的绑定、区域特征）的查询中表现不佳。多向量方法（如ColBERT、Colpali）通过保留所有token表示并计算局部匹配来解决此问题，但它们需要从头训练，计算和内存成本高，且常忽略全局摘要表示。
- **整体含义**：SMART框架揭示了单向量模型在对比训练下，其非池化隐藏状态已经被隐式组织为支持token级检索的几何结构，因此无需训练就可通过后期交互利用这些状态，实现从单向量到多向量的高效转化，兼顾效率与准确性。

#### 2. 论文提出的方法论：核心思想、关键技术细节、公式或算法流程
- **核心思想**：单向量模型虽然只对池化token（<eot>）计算对比损失，但梯度通过注意力机制传播到所有隐藏状态，使得这些隐藏状态隐式地学习到支持余弦检索的几何结构。因此，可以直接复用这些冻结的隐藏状态进行token级匹配，并只需与原始池化得分简单相加作为混合得分。
- **关键技术细节**：
    1. **训练无关推理（Inference-only）**：使用最后一层（Layer L）的非池化隐藏状态（排除padding和池化token），对每个查询token，计算其与候选文档所有token的最大余弦相似度（MaxSim），然后对所有查询token取平均得到 `s_late(q,c)`。与原始单向量得分 `s_single(q,c)` 直接相加得到混合得分 `s_hybrid(q,c)=s_single(q,c)+s_late(q,c)`。
    2. **轻量适配器训练**：保持骨干冻结，仅训练一个线性适配器（LayerNorm + 线性映射 + L2归一化）作用于最后层隐藏状态，优化`slate`得分，再与池化得分混合。
    3. **高效LoRA微调**：使用混合损失（包含`ssingle`和`slate`）对单向量模型进行LoRA微调，将单向量模型转化为多向量模型，训练时间比从头训练多向量模型节省约20%。
- **公式**：
    - 单向量得分：`s_single(q,c) = (h_{q,eot}^L)^T h_{c,eot}^L`
    - 后期交互得分：`s_late(q,c) = avg_{i∈Mq} max_{j∈Mc} (h_{q,i}^L)^T h_{c,j}^L`（均使用归一化向量）
    - 混合得分：`s_hybrid = s_single + s_late`

#### 3. 实验设计：使用的数据集/场景、benchmark、对比方法
- **数据集/场景**：
    - **MMEB-V2**：主要benchmark，涵盖图像检索（Image）、视觉文档检索（VDRv1, VDRv2, VR, OOD）、视频检索（Video）等12个密集检索任务。
    - **自建局部证据玩具基准（Controlled Toy Benchmark）**：40对报告，每对包含25个代码-标记绑定，共1000个查询，用于测试局部绑定区分能力。
- **对比方法**：
    - 单向量基线：VLM2Vec-V2.0, GME-2B/7B, Qwen3-VL-Embed-2B/8B
    - 多向量基线：Colpali-1.3, jina-embeddings-v4
    - 自训练模型：LamRA-Single, LamRA-Single-SMART, LamRA-Single-Convert, LamRA-Multi, LamRA-Hybrid（使用LoRA从Qwen3-VL-2B-Instruct微调）

#### 4. 资源与算力
- **推理阶段**：无需训练资源。
- **轻量适配器训练**：在Qwen3-VL-Embedding-2B上，使用一个节点8块48GB A6000，训练1小时50分钟。
- **LoRA微调（LamRA系列）**：使用一个节点8块80GB A100，全局batch size 512。LamRA-Single训练6.5小时（4 epoch）；LamRA-Single-Convert额外3小时（1 epoch，共9.5小时）；LamRA-Multi训练12小时（从头训练）。
- **开源**：代码和权重已公开在GitHub。

#### 5. 实验数量与充分性
- **实验数量**：
    - 在MMEB-V2上对5个不同骨干（VLM2Vec, GME-2B, GME-7B, Qwen3-VL-Embed-2B/8B）进行推理阶段评估，并报告12个子任务平均得分。
    - 在视觉文档子集上测试了适配器训练（2个size）和LoRA微调（5个变体）。
    - 层分析、定性可视化、消融混合得分等。
- **充分性与公平性**：
    - 涵盖多个模态和任务，对比了当时SOTA模型（包括单向量和多向量）。
    - 自训练的一致设置（相同骨干、相同LoRA配置、相同训练集）保证了公平比较。
    - 玩具基准验证了核心假设，但作者承认该设置下混合得分不如纯后期交互，但分析合理。
    - 实验充分，但未覆盖分类、标准VQA和视频时刻检索等任务（作者说明了原因）。

#### 6. 论文的主要结论与发现
1. 单向量模型的非池化隐藏状态已被隐式组织为支持token级检索，可直接用于后期交互，无需训练即可提升检索性能。
2. 推理时应用SMART（即插即用）可显著提升所有骨干模型在MMEB-V2上的平均性能，包括最先进的Qwen3-VL-Embedding系列。
3. 轻量级适配器训练可进一步改进性能，使2B单向量模型超越4B多向量模型jina-embeddings-v4。
4. 使用SMART混合目标进行LoRA微调，能以约20%的训练时间将单向量模型转化为竞争力接近的多向量模型。
5. 层分析表明最后层隐藏状态最有效，但前几层也包含有用信息；最终层池化token是鲁棒的全局锚点。

#### 7. 优点：方法或实验设计上的亮点
- **方法简洁高效**：无需额外训练即可增强现有模型，且不改变索引结构（仍可用单向量索引），推理时仅增加后期交互计算。
- **即插即用**：对任何单向量嵌入器有效，代码开源，易于集成。
- **轻量微调催化剂**：通过极小成本（<2小时）即可将骨干转化为多向量能力，超越专门训练的模型。
- **实验设计严谨**：包含控制实验（玩具基准）分离局部证据，并与多向量模型直接对比；层分析揭示了深度与性能的关系；定性可视化解释了token级匹配机制。

#### 8. 不足与局限
- **任务覆盖有限**：实验限于密集检索任务，作者指出对分类、标准VQA等全局任务无效，且未包含视频时刻检索（需时空推理）和图像视觉定位（存在歧义）。
- **玩具基准的局限性**：该基准故意去除全局语义，导致混合得分低于纯后期交互，仅用于诊断性验证，不能直接推广到自然检索场景。
- **训练资源依赖**：虽然节省了训练时间，但LoRA微调仍需8块80GB GPU的集群，对资源有限的研究组可能仍有一定门槛。
- **未探索多模型/联邦场景**：SMART在跨语言、跨模态等更复杂场景的适用性未验证。
- **消融实验有限**：虽然比较了单向量、多向量和混合，但未系统分析不同权重组合或更复杂的聚合策略。

（完）
