Title: SAKI: Score-Aware Low-Rank Key Indexing for Long-Context KV Retrieval

URL Source: https://arxiv.org/pdf/2608.03228v1

Published Time: Wed, 05 Aug 2026 00:45:19 GMT

Number of Pages: 11

Markdown Content:
# SAKI: Score-Aware Low-Rank Key Indexing for Long-Context KV Retrieval 

Lin Zhang Data Science and Platform, Cotality 

linzhang1126@gmail.com 

August 2026 

Abstract 

Existing low-rank KV-cache indexes preserve either model weights or key variance — neither of which is the quantity attention actually uses. We derive the expected attention-score distortion induced by key-side rank-r compression and show it defines a two-sided covariance-weighted low-rank approximation, L(P ) = ∥Σ1/2 

> q

A(I − P )Σ 1/2 

> k

∥2 

> F

, combining the model’s scoring operator 

A = W ⊤ 

> Q

WK with the realized query and key statistics; a margin condition converts L control into top-k recall. Its rank-r optimum has a closed-form asymmetric factorization obtained from the SVD of the whitened query–key operator — an optimal rank-r linear score map that no span-restricted (projector-based) method, PCA included, can express. The resulting training-free index, SAKI , beats key-PCA at every rank on every model tested — LLaMA-3.1-8B, Qwen2.5-7B, Mistral-7B-v0.1, and Llama-3.2-3B — removing 13–30% of PCA’s remaining top-64 recall error at r=32 (e.g. 0 .748 → 0.799 on LLaMA-3.1-8B, 0 .786 → 0.850 on Qwen2.5-7B), improving 68–89% of heads per model, with gains concentrated in deep layers where query and key geometry diverge. The theory predicts the experiment: per-head predicted score-MSE reduction matches measurement at Pearson r = 0 .997 (median gap 0 .0009), and our variant ordering — exact asymmetric optimum > weighted span > raw span ≈ PCA — shows the gain comes from the objective, not from inserting covariances somewhere. An operator-geometry diagnosis (extreme, cross-model non-normality of A; heavy-tailed weight spectra; hub-shaped cross-subspace coupling) explains why weight-only and invariant-subspace indexes fail, and why data-aware reconstruction was the right direction with the wrong objective. 

# 1 Introduction 

At a million tokens of context, an 8B-parameter model’s KV cache occupies ∼130 GB in bf16 — decoding becomes a retrieval problem. Practical systems separate selection from reading : a cheap index over cached keys nominates candidates, exact values are fetched for the winners [ 11 , 5], and sparse architectures build the split into training [ 1 ]. The index is a rank-r sketch of the keys; its quality gates everything downstream. What subspace should the index use? The field’s answers fall into two objective classes. Weight-side : truncate the projection matrices — optimal for the operator, blind to data. Data-aware reconstruction : preserve the cached tensors under the activation distribution — from key PCA through its increasingly sophisticated 2026 descendants [ 18 , 19 , 20 , 21 ]. On a common protocol (all 1,024 heads of LLaMA-3.1-8B, 4,096-token natural text, exact queries, top-64 recall) the class representatives achieve 0 .366 (weight-SVD) and 0 .748 (key-PCA) at r=32. Neither class optimizes what attention uses. Scores are s = q⊤k; retrieval needs their ranking .PCA preserves E k − P P ⊤k 2 — key variance: a high-variance direction that queries never read 1arXiv:2608.03228v1 [cs.LG] 4 Aug 2026 wastes an index dimension, while a modest-variance direction that WQ amplifies can dominate scores and be discarded. This paper derives the objective that key compression should optimize, solves it exactly, and measures the consequence. 

Contributions. 

1. Problem formulation (§5). The expected score distortion of key-side rank-r compression is a two-sided covariance-weighted low-rank approximation L(P ) = ∥Σ1/2 

> q

A(I − P )Σ 1/2 

> k

∥2 

> F

, derived from the attention-score loss itself with both query and key statistics; a margin condition (Prop. 2) links L to top-k recall. We claim no new linear algebra: the rank-constrained optimum follows from the classical full-rank-weights reduction [ 12 , 14 , 15 ]. The claim is the 

identification of this objective for attention retrieval, its exact specialization, and its empirical consequence. 2. Algorithm (§6). The optimum is an optimal rank-r linear score map Mr = Σ −1/2 

> q

CrΣ−1/2 

> k

—an asymmetric low-rank bilinear factorization, generally not a projector — computable from two d × d calibration covariances per head, foldable into WQ, W K , training-free. 3. Evidence that theory predicts practice (§7). SAKI beats PCA at all three ranks on all four models tested (13–30% of remaining recall error removed at r=32; 68–89% of heads improved per model), with the largest gains in deep layers. Predicted per-head score-MSE reduction matches measurement at Pearson r=0.997, directly validating the independence approximation the derivation uses. The variant ordering — exact asymmetric optimum >

weighted span > raw span ≈ PCA — isolates the objective, not covariance insertion, as the source of gain. 4. Diagnosis (§4, details in appendix). A cross-model operator-geometry analysis — extreme non-normality of Ah (median Henrici 0 .95–0 .98 across five models), heavy-tailed weight spectra, hub-shaped cross-subspace coupling — explains why weight-only and invariant-subspace indexes fail and motivates the objective-level fix. 

# 2 Related Work 

Token selection and sparse attention. H2O [ 9 ], SnapKV [ 10 ] (eviction); Quest [ 11 ] (query-aware page bounds); NSA [ 1 ] and DeepSeek-V3.2’s lightning indexer (learned selectors). SAKI supplies the subspace such systems sketch keys with and composes with their block structure. 

Channel-axis compression. MLA [ 2 ] trains a latent cache; Palu [ 3], Eigen Attention [ 4 ], LoRC [ 6] apply SVD-family projections; ShadowKV [ 5 ] sketches pre-RoPE keys with data SVD and fetches exact values. Quantization [7, 8] composes with SAKI codes. 

Contemporary data-aware KV compression. A rapidly moving 2025–2026 line makes activation-awareness standard: KV-CoRE [ 18 ] characterizes data-dependent low-rank compressibil-ity, CARE [ 19 ] uses activation covariance for low-rank attention conversion, OjaKV [ 20 ] adapts the projection online, STAR-KV [ 21 ] adds adaptive rank selection. “Data-aware beats weight-SVD” is therefore not a claim of this paper: that family — key-PCA being its objective-level representative — optimizes reconstruction of cached tensors, however adaptively. The distinction from CARE, the closest conceptual neighbor, must be explicit: CARE optimizes representation fidelity under 

2attention-module conversion; SAKI optimizes pairwise attention-score fidelity specifically for a retrieval index, with asymmetric query/key factors derived from both covariances. The axes compose: OjaKV-style online tracking applies verbatim to Σ q, Σk; STAR-KV-style adaptive ranks to r.

Classical multivariate analysis and weighted low-rank approximation. The machinery is classical and we position against it explicitly. Rank-constrained approximation under full-rank two-sided weights reduces by substitution to Eckart–Young [ 12 ]; this is the solution structure of reduced-rank regression [ 14 ], with the general weighted problem studied in [ 15 ]. Canonical correlation analysis [ 13 ] whitens the cross-covariance of two observed variable sets (Σ −1/2 

> q

Σqk Σ−1/2 

> k

); our object differs in kind — a fixed model operator weighted (not whitened) by marginal square roots, a known bilinear form rather than an estimated cross-covariance. In retrieval, asymmetric MIPS [16 ] and ScaNN’s anisotropic quantization [ 17 ] adopt inner-product-aware objectives for corpus quantization against generic queries; SAKI derives a per-head subspace through a fixed bilinear operator with both distributions estimated from the model’s own activations, foldable into its weights. The novelty claim is deliberately narrow: new problem formulation + exact specialization + empirical consequence , not new linear algebra. 

Non-normal operator theory. Transient growth and non-normal dynamics are studied in fluids, RNNs, and optimization; we are unaware of prior measurement of transformer score-operator non-normality ( §4). 

# 3 Setup and baselines 

Head h scores position j against query i via sij = q⊤ 

> i

kj = x⊤ 

> i

Ahxj , Ah = W (h)⊤ 

> Q

W (g) 

> K

∈ RD×D, rank 

≤ d (GQA; D=4096, d=128). The index compresses cached keys to r ≪ d dims; queries are exact at decode; codes are pre-RoPE with rotation applied after reconstruction [ 5]. Protocol: exact float32 streaming forward pass of LLaMA-3.1-8B on 4,096 tokens of natural text; recall of the true top-64 attended positions over the last 512 queries; medians across all 1,024 heads. Index r = 16 r = 32 r = 64 

SAKI-opt (exact Mr, ours) 0.731 0.799 0.876 

SAP-map (weighted span, ours) 0.696 0.766 0.845 SAP-svd (singular span, ours) 0.669 0.740 0.829 key PCA 0.679 0.748 0.833 weight SVD 0.267 0.366 0.555 Schur invariant subspace 0.235 0.319 0.411 Table 1: Median top-64 attention recall, 1,024 heads, context 4,096. Relative to PCA’s remaining error, SAKI-opt removes 16%/20%/26% at r = 16 /32 /64. 

# 4 Diagnosis: why weight geometry is not enough 

Three measured facts, established across five open models and detailed in Appendix B, motivate the objective-level fix. (a) Attention score operators are extremely non-normal : median Henrici departure 0 .975–0 .984 for every standard-attention model measured (LLaMA-3.1/3.2, Qwen2.5, 3Mistral), ∼88% complex eigenvalues, flat in depth; the MLA-based DeepSeek-V2-Lite is measurably closer to normal (0 .948) — latent bottlenecks regularize score-operator geometry. Singular and invariant subspaces are therefore nearly disjoint objects, and “which subspace” is a modeling decision. 

(b) Weight spectra are heavy-tailed (median stable rank 27 .7/128 against a 99%-energy rank of 106 /128): weight-only truncation at useful ranks discards live directions — hence weight-SVD’s 0.366. (c) Half the operator’s energy lives in hub-shaped cross-subspace couplings (ordered Schur form): key-side truncation to a leading invariant subspace severs exactly those couplings, predicting — correctly, see Table 1 — that eigen/Jordan-style compression fails hardest. PCA’s strength shows activation geometry dominates weight geometry; its residual weakness is the subject of the next section. 

# 5 The score-aware objective and its exact solution 

Let key-side compression be a rank-r linear map P applied to cached keys. The score error is 

e = x⊤ 

> q

A(I − P )xk. Under an independent query/key model with second moments Σ xq = E[xqx⊤ 

> q

], Σxk = E[xkx⊤ 

> k

], 

E[e2] = tr 

h

A(I − P ) Σ xk (I − P )⊤A⊤ Σxq

i

= (Σ xq )1/2A(I − P ) (Σ xk )1/2 2 

> F

=: L(P ). (1) PCA minimizes (I − P )(Σ xk )1/2 2 

> F

; the correct weight is Σ 1/2 

> q

A(·) Σ 1/2 

> k

. In head coordinates ( q =

WQx, k = WK x) the operator is absorbed into the query moment: with Σ q = E[qq ⊤] = WQΣxW ⊤

> Q

and Σ k = E[kk ⊤], 

L(P ) = Σ1/2 

> q

(I − P ) Σ 1/2 

> k
> 2
> F

, C := Σ 1/2 

> q

Σ1/2 

> k

∈ Rd×d, (2) computable from two d × d calibration covariances per head. Σ q is where the attention operator lives: key variance × query probability × scoring geometry — the quantity retrieval needs. 

Proposition 1 (Exact minimizer over rank-r maps; classical reduction) . Over all rank-r linear maps P : Rd → Rd, L in Eq. (2) is minimized by 

Mr = Σ −1/2 

> q

Cr Σ−1/2 

> k

, Cr = UrΛrV ⊤ 

> r

the truncated SVD of C, (3) 

with optimum L(Mr) = P 

> i>r

λi(C)2 (substitution ˜P = Σ 1/2 

> q

P Σ1/2 

> k

plus Eckart–Young; inverses read as regularized pseudoinverses when covariances are singular — Appendix A gives the full derivation including centering, rank-deficiency, and finite-sample regularization). 

Remark 1 (Terminology) . Mr is generally not idempotent ( M 2

> r

̸ = Mr): it is an optimal rank-r

linear score map — an asymmetric low-rank bilinear factorization — not an oblique projector. The distinction matters for the result: orthogonal projectors (all span-based methods, PCA included) form a strict subset of rank-r maps, so the optimum over the larger class can only be better, and Table 1 shows the strict-subset gap is real ( 0.799 vs. 0.766 /0.740 at r=32 ). 

Proposition 2 (Top-k preservation) . If |sij − ˆsij | ≤ ε for all j and the true top-k margin is 

γk = s(k) − s(k+1) , the top-k set is recovered exactly when 2ε < γ k; recall degrades continuously in 

ε/γ k. Control of L (mean-square ε) therefore transfers to recall. 

4Figure 1: Score-aware indexes vs. baselines. (A) Recall vs. rank. (B) By layer at r=32: SAKI’s gains concentrate in deep layers where PCA is weakest. (C) Per-head delta at r=32: median +0 .041, 76 .9% of heads improve. 

Remark 2 (The independence assumption, tested) . In self-attention qi and cached kj come from the same sequence, with positional and content correlations and RoPE’s relative rotation; Eq. (1) with a general cross-moment adds the term −2 tr [A(I − P ) Σ kq A(I − P )] -type corrections that have no closed form. Rather than assume the approximation harmless, we measure it: per head, the reduction in score-MSE predicted by Prop. 1 ( 1 − P 

> i>r

λ2 

> i

/ P 

> i

λ2 

> i

) against the actual reduction on real causal query/key pairs. Across all 1,024 heads at r=32 : Pearson r = 0 .997 , median |predicted −

actual | = 0 .0009 (Appendix C). For score-MSE on natural text, the independence model is near-exact; RoPE handling (pre-rotation codes) remains the approximation to refine. 

# 6 SAKI 

Per head, from one calibration pass: Σ q (uncentered second moment of pre-RoPE queries), Σ k and mean μ (centered keys); form C; store key code cj = B⊤ 

> k

(kj − μ) ∈ Rr, Bk = Σ −1/2 

> k

VrΛ1/2 

> r

; Bq = Σ −1/2 

> q

UrΛ1/2 

> r

, (4) so that ˆs = ( B⊤ 

> q

q)⊤cj + q⊤μ reproduces q⊤Mr(k − μ) + q⊤μ exactly (Appendix A verifies the factorization). Both maps fold into WK , W Q offline; when composing with RoPE, reconstruct ˆk = μ + Mr(k − μ) and rotate. Cost: two d × d covariances and one d × d SVD per head — seconds per model. Variants isolating what matters: SAKI-opt (exact Mr), SAP-map (Σ 1/2 

> k

-weighted span, orthonormalized), SAP-svd (raw right-singular span of C). 

# 7 Experiments 

Main result. SAKI-opt beats PCA at every rank (Table 1, Fig. 1); framed against what remains, it removes 16%, 20%, and 26% of PCA’s residual recall error at r = 16 , 32 , 64. The per-head delta at r=32 has median +0 .041 with 76 .9% of heads improving (79 .3% at r=64), and the gains concentrate in layers 16–31 (+0 .061 median) — precisely where query and key geometry diverge and a query-blind subspace is costliest. 5The objective, not the covariances, wins. The variant ordering is exact-optimum > weighted span > raw span ≈ PCA at every rank. Merely inserting covariance matrices into a span construction (SAP-svd) does not beat PCA; solving the derived objective over the full class of rank-r maps does. This is the empirical signature of the asymmetric factorization. 

Theory predicts practice. The predicted-vs-actual score-MSE ablation (Remark 2, Appendix C) shows per-head agreement at r = 0 .997 across 1,024 heads — the derivation’s independence model describes real attention statistics almost exactly, so the closed form can be trusted as the deployment-time optimum for its objective. 

Cross-model replication. Running the identical pipeline (exact streaming forward pass on the same calibration text, PCA vs. exact Mr) on three further models — including Qwen2.5-7B, whose attention carries QKV biases, and Llama-3.2-3B with factor-32 llama3 RoPE scaling — replicates the result at every rank on every model (Table 2). The gain is largest on Qwen2.5-7B (30% of remaining error removed at r=32; deep-layer deltas up to +0 .12; 89% of heads improve) and smallest but still uniform on Mistral-7B (13%; 71% of heads). SAKI never loses to PCA in any (model, rank) cell. Model PCA → SAKI (median recall@64) heads ↑ err. removed 

r = 16 r = 32 r = 64 @r=32 @r=32 LLaMA-3.1-8B .679 →.731 .748 →.799 .833 →.876 76.9% 20% Qwen2.5-7B .717 →.777 .786 →.850 .864 →.914 89.0% 30% Mistral-7B-v0.1 .742 →.774 .818 →.841 .900 →.914 71.1% 13% Llama-3.2-3B .679 →.721 .760 →.797 .850 →.874 68.4% 15% Table 2: Cross-model SAKI: identical protocol on four models. SAKI beats PCA in every cell. 

What is not yet shown. No configuration reaches 0 .95 recall at r ≤ 32 (Qwen2.5-7B comes closest at r=64: 0.914); evidence is one calibration domain at 4K context with no end-to-end generation quality. Calibration transfer, longer contexts, and LongBench/RULER are the remaining pre-submission experiments ( §9). 

# 8 Extensions 

Ranking-optimized refinement (primary). L is an MSE surrogate; the deployed metric is top-k recall, and Prop. 2 says errors matter only near the margin. Initializing at the closed form 

Mr and optimizing only the low-rank factors under a pairwise logistic loss on (top-k, non-top-k)pairs around the boundary — LLM frozen — gives the two-tier story SAKI-ClosedForm (score-MSE optimal) / SAKI-Rank (task-aligned refinement). Per-layer rank allocation : the depth trend is strong and measured; allocation should follow it. Query-conditioned mixtures of subspaces 

(router over precomputed P1 . . . P M ) are promising but introduce training, routing, and baseline burdens that would dilute this paper; we defer them. Coupling-preservation regularization : the Schur coupling geometry (Appendix B) specifies what any index must preserve and can regularize SAKI-Rank. 69 Limitations 

Evidence is one calibration domain at 4K context, recall-only (no end-to-end generation). Cross-model replication is done — four models, uniform wins (Table 2) — but the spread (Qwen 30% vs. Mistral 13% of remaining error removed at r=32) is not yet explained; relating it to per-model operator geometry is an open analysis. Calibration transfer (domain and length shift) must be measured; OjaKV [ 20 ] explicitly targets static-subspace failure under shift, and online updates of Σq, Σk are the natural response if SAKI proves sensitive. RoPE’s relative rotation is handled by pre-rotation codes, not modeled in the objective (rotation-averaged covariances are the refinement); the independence model itself is validated at r = 0 .997 (Remark 2). Head-to-head comparison with the 2026 data-aware family [ 18 , 19 , 20 , 21 ] — including whether any incorporates query statistics through the score, CARE being the closest to check — is required before submission. 

# 10 Conclusion 

Low-rank KV indexes have optimized weights or key variance; attention uses neither. Deriving the expected score distortion yields a two-sided covariance-weighted objective whose exact rank-r

optimum is an asymmetric, training-free, closed-form score map — and the theory holds up unusually well in practice: it predicts per-head score-MSE reduction at r = 0 .997, its variant ordering appears exactly in the measurements, and it removes a fifth to a quarter of PCA’s remaining recall error where retrieval is hardest. The operator-geometry diagnosis explains why the field’s earlier answers fail and what any future index must preserve. 

# References 

[1] Yuan et al. Native Sparse Attention: Hardware-Aligned and Natively Trainable Sparse Attention. arXiv:2502.11089, 2025. [2] DeepSeek-AI. DeepSeek-V2: A Strong, Economical, and Efficient Mixture-of-Experts Language Model. arXiv:2405.04434, 2024. [3] Chang et al. Palu: Compressing KV-Cache with Low-Rank Projection. arXiv:2407.21118, 2024. [4] Saxena et al. Eigen Attention: Attention in Low-Rank Space for KV Cache Compression. arXiv:2408.05646, 2024. [5] Sun et al. ShadowKV: KV Cache in Shadows for High-Throughput Long-Context LLM Inference. arXiv:2410.21465, 2024. [6] Zhang et al. LoRC: Low-Rank Compression for LLMs’ KV Cache. arXiv:2410.03111, 2024. [7] Liu et al. KIVI: A Tuning-Free Asymmetric 2bit Quantization for KV Cache. arXiv:2402.02750, 2024. [8] Hooper et al. KVQuant: Towards 10 Million Context Length LLM Inference. arXiv:2401.18079, 2024. [9] Zhang et al. H2O: Heavy-Hitter Oracle for Efficient Generative Inference. arXiv:2306.14048, 2023. 7[10] Li et al. SnapKV: LLM Knows What You are Looking for Before Generation. arXiv:2404.14469, 2024. [11] Tang et al. Quest: Query-Aware Sparsity for Efficient Long-Context LLM Inference. arXiv:2406.10774, 2024. [12] Eckart and Young. The approximation of one matrix by another of lower rank. Psychometrika, 1936. [13] Hotelling. Relations between two sets of variates. Biometrika, 1936. [14] Izenman. Reduced-rank regression for the multivariate linear model. Journal of Multivariate Analysis, 1975. [15] Srebro and Jaakkola. Weighted low-rank approximations. ICML, 2003. [16] Shrivastava and Li. Asymmetric LSH (ALSH) for sublinear time maximum inner product search. NeurIPS, 2014. [17] Guo et al. Accelerating large-scale inference with anisotropic vector quantization. ICML, 2020. [18] KV-CoRE: data-dependent low-rank compressibility of KV caches. arXiv, 2026. [19] CARE: covariance-aware low-rank attention conversion. arXiv, 2026. [20] OjaKV: online adaptive PCA for KV-cache compression. arXiv, 2026. [21] STAR-KV: adaptive rank selection and hybrid low-rank KV decomposition. arXiv, 2026. 

# A Full derivation of Proposition 1 

Conventions. Column vectors q, k ∈ Rd; P : Rd → Rd linear, rank (P ) ≤ r; scores s = q⊤k,compressed ˆ s = q⊤P k (centering handled below). 

Reduction. With Σ q ≻ 0, Σ k ≻ 0, substitute ˜P = Σ 1/2 

> q

P Σ1/2 

> k

(a bijection on rank-≤ r matrices, since congruence by invertible matrices preserves rank): L(P ) = ∥Σ1/2 

> q

Σ1/2 

> k

− ˜P ∥2 

> F

= ∥C − ˜P ∥2 

> F

.Eckart–Young [12] gives ˜P ⋆ = Cr, hence Eq. (3) and L(Mr) = P 

> i>r

λi(C)2.

Singular covariances. If Σ q or Σ k is rank-deficient, replace inverse square roots by pseudoinverse square roots; the bijection holds on the subspace range (Σ q) × range (Σ k), outside of which L is unaffected by P , so any completion of Mr by zero is optimal. In finite samples we regularize eigenvalues below 10 −6λmax (all experiments); sensitivity to this floor was not observed. 

Centering. Writing k = μ + kc with E[kc] = 0 and compressing only kc: s = q⊤kc + q⊤μ; the second term is exact at decode (store μ, one inner product), so Eq. (2) applies to centered key moments and uncentered query moments, as implemented. Compressing k uncentered is the special case μ = 0. 

Factorization identity. With Bq = Σ −1/2 

> q

UrΛ1/2 

> r

and Bk = Σ −1/2 

> k

VrΛ1/2 

> r

: BqB⊤ 

> k

· transposed appropriately gives ( B⊤ 

> q

q)⊤(B⊤ 

> k

kc) = q⊤Σ−1/2 

> q

UrΛrV ⊤ 

> r

Σ−1/2 

> k

kc = q⊤Mrkc, i.e., the stored r-dim codes reproduce the optimal bilinear score exactly. 8Non-idempotence. M 2 

> r

= Σ −1/2 

> q

CrΣ−1/2 

> k

Σ−1/2 

> q

CrΣ−1/2

> k

̸ = Mr in general (equality would require Σ−1/2 

> k

Σ−1/2 

> q

to act as identity on the factor range); Mr is therefore not a projector of any kind, and we do not call it one. 

# B Operator-geometry diagnosis: details 

Figure 2: Spectral atlas of Ah, all 1,024 heads of LLaMA-3.1-8B: singular vs. eigenvalue decay (the gap is non-normality), 99%-energy rank, Henrici index, and their relation by layer. All spectra are exact d × d computations via thin-QR reduction ( A = Q1M Q ⊤ 

> 2

, restriction 

B = M G ); the Schur coupling analysis reorders the real Schur form of B into modulus-ranked clusters (reconstruction residual 10 −14 ). Full statistics in the project’s atlas stats.md .

# C Predicted vs. actual score-MSE reduction 

At r=32, per head: predicted relative score-MSE reduction 1 − P 

> i> 32

λi(C)2/ P 

> i

λi(C)2 vs. the measured reduction on real causal (query, key) pairs (centered keys, pre-RoPE). Across 1,024 heads: 9Figure 3: Hub-shaped coupling geometry: modulus-ordered Schur clusters of Ah form a DAG whose couplings carry half the operator energy. Pearson r = 0 .9969; median predicted 0 .969, median actual 0 .969; median absolute gap 0 .0009. The independence approximation of Eq. (1) is near-exact for score-MSE on natural text. 

# D Reproducibility 

Single Apple-silicon workstation, float32/float64 numpy; weights range-read from public checkpoints; calibration text is public-domain natural text. Code: fetch qk weights.py , spectral atlas.py ,

coupling dag.py , cross model atlas.py , forward pass.py , recall experiment.py , sap experiment.py ,

mse ablation.py .10 Figure 4: Cross-model universality of score-operator non-normality (every head, exact spectra, five models). Model Henrici med. (p10–p90) > 0.5 stable rank /d complex λ

LLaMA-3.1-8B 0.982 (0.923–0.991) 100% 27.7/128 87% Llama-3.2-3B 0.975 (0.916–0.986) 100% 26.8/128 87% Qwen2.5-7B 0.984 (0.922–0.992) 99.9% 27.0/128 88% Mistral-7B-v0.1 0.982 (0.920–0.991) 100% 23.5/128 88% DeepSeek-V2-Lite (MLA) 0.948 (0.851–0.971) 100% 25.4/192 88% Table 3: Non-normality across model families. 11