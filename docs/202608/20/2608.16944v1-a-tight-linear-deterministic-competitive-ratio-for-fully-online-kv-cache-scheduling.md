---
title: A Tight Linear Deterministic Competitive Ratio for Fully Online KV-Cache Scheduling
title_zh: 全在线KV缓存调度的紧线性确定性竞争比
authors: "Ian D'Ambrosio"
date: 2026-08-14
pdf: "https://arxiv.org/pdf/2608.16944v1"
tags: ["query:sparse-attn"]
score: 7.0
evidence: 为内存约束下的在线KV缓存调度给出紧致线性确定性竞争比
tldr: 针对非抢占式LLM请求在增长KV-cache约束下的完全在线调度问题，已有下界为Omega(sqrt(n))而上界为n，存在明显间隙。本文构造一个长请求观察其开始时间，再释放n-1个单token请求的硬实例，证明最优确定性竞争比满足(n-1)/12 <= R_det(n) <= n，即恰好为Theta(n)。该结果填补了间隙，且全部证明在Lean 4中机器验证。
source: arxiv
selection_source: fresh_fetch
motivation: 现有完全在线KV-cache调度的确定性竞争比上下界之间有明显间隙，需精确刻画其最优比率。
method: 构造释放长请求并观察其确定性开始时间的硬实例，随后在中途释放n-1个单token请求，结合因果一致性与串行策略推导上下界。
result: "证明对任意n>=2，确定性最优竞争比R_det(n)满足(n-1)/12 <= R_det(n) <= n，从而R_det(n)=Theta(n)。"
conclusion: 填补了竞争比间隙，给出显式下界实例与上界策略，并通过Lean 4机器验证，为在线KV-cache调度提供了精确的理论保证。
---

## 摘要
Jaillet等人提出了一个在增长KV缓存内存约束下批量处理非抢占式LLM请求的全在线模型。对于端到端总延迟，他们证明了每个确定性算法都有Ω(√n)的竞争比，而基本顺序上界为n。我们填补了这一空白。设R_det(n,M)为在内存M下恰好n个请求的最优确定性比率，并设R_det(n)=sup_M R_det(n,M)。对于每个n≥2，我们证明(n-1)/12 ≤ R_det(n) ≤ n，因此R_det(n)=Θ(n)。下界释放一个填满内存的长请求，观察其确定性开始时间，然后在长请求运行中途释放n−1个宽的单令牌请求。短请求不能与长请求重叠，而事后调度在有用时会按相反顺序运行这两组。困难实例使用显式固定内存M=2(n−1)n。上界由一致因果串行策略实现。精确模型、因果性论证、两个比较器分支以及量词顺序均在Lean 4中进行了机器验证。精确有限控制和重放命令随证明一同提供。

## Abstract
Jaillet et al. introduced a fully online model for batching nonpreemptive LLM requests under a growing KV-cache memory constraint. For total end-to-end latency they proved that every deterministic algorithm has competitive ratio Omega(sqrt(n)), while the elementary sequential upper bound is n. We close this gap. Let R_det(n,M) be the optimal deterministic ratio for exactly n requests at memory M, and let R_det(n)=sup_M R_det(n,M). For every n >= 2 we prove   (n-1)/12 <= R_det(n) <= n,   so R_det(n)=Theta(n). The lower bound releases one memory-filling long request, observes its deterministic start time, and then releases n-1 wide one-token requests halfway through the long run. No short request can overlap the long one, whereas a hindsight schedule runs the two groups in the opposite order when useful. The hard instance uses the explicit fixed memory M=2(n-1)n. The upper bound is achieved by a uniform causal serial policy. The exact model, causality argument, both comparator branches, and quantifier order are machine-checked in Lean 4. Exact finite controls and replay commands accompany the proof.