---
title: Resolving Ambiguity in Composed Image Retrieval via Calibrated Interaction
title_zh: 通过校准交互解决组合图像检索中的歧义问题
authors: "Amsisan Tran, Baogh Le, Tuan Kiet Pham, Sui Yang Guang"
date: 2026-05-23
pdf: "https://arxiv.org/pdf/2605.24634v1"
tags: ["query:multimodal"]
score: 9.0
evidence: 解决组合图像检索中的歧义问题
tldr: 现有组合图像检索假设查询对应单一目标，但模糊查询（如“make it more formal”）导致假阴性问题。本文将其重构为不确定性下的校准意图解析，引入共形预测层返回具有覆盖保证的候选集，并根据预期信息增益策略自动提出澄清问题。在多个基准上单轮性能达到SOTA，且远低于朴素对话基线的交互预算，首次实现了有效覆盖和校准。该方法能精确处理模糊查询，推动了组合图像检索的实用化。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.24634v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1670, \"height\": 862, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2605.24634v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1758, \"height\": 888, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2605.24634v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1756, \"height\": 886, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2605.24634v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 669, \"height\": 398, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2605.24634v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 685, \"height\": 313, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2605.24634v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1161, \"height\": 373, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2605.24634v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 782, \"height\": 373, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2605.24634v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1198, \"height\": 301, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2605.24634v1/table-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 726, \"height\": 262, \"label\": \"Table\"}]"
motivation: 现有组合图像检索假设查询对应单一目标，但模糊查询导致假阴性，模型无法区分精确与模糊查询。
method: 将CIR重构为校准意图解析，采用共形预测层提供候选集覆盖保证，并基于预期信息增益策略选择最优澄清问题。
result: 在开放域和时尚基准上单轮匹配SOTA，交互预算远低于朴素对话基线，首次实现有效覆盖和校准。
conclusion: 提出校准交互框架，有效解决CIR中的模糊性问题，提供可靠的不确定性量化。
---

## 摘要
组合图像检索（CIR）使用参考图像和描述如何修改它的文本来搜索语料库。尽管从三元组训练的合成器到零样本和生成方法取得了快速进展，但基本上所有系统都共享一个假设：查询映射到单个目标，通过相对于一个标注的Recall@K进行评分。我们认为这与任务根本不符。像“让它更正式”这样的查询并非指定一张图像，而是指定语料库的一个区域，用户意图中的具体成员实际上是欠定的。这种欠指定性是众所周知的假阴性问题的根源，并使当前模型无法区分精确查询和模糊查询。我们将CIR重新定义为不确定性下的校准意图解析：一个检索器被包裹在共形预测层中，返回一个具有覆盖率保证的候选集，其大小是歧义性的原则性度量；当集合较大时，一个期望信息增益策略会提出一个最有用的问题（来自可解释的歧义轴），然后集合收缩。我们引入了AmbiCIR，这是一个基准和人体验证的用户模拟器，它恢复了CIRR中被休眠的辅助和对话标注，并扩展了CIRCO的多正例设置。在开放域和时尚基准测试中，我们的方法匹配了单轮最先进技术，证实了校准解析在精确查询上无额外代价，同时以比朴素对话基线所需的交互预算少得多的交互次数达到预期目标，并且这是第一个报告该任务有效覆盖率和校准的方法。

## Abstract
Composed image retrieval (CIR) searches a corpus with a reference image and a text describing how to modify it. Despite rapid progress from triplet-trained compositors to zero-shot and generative methods, essentially all systems share one assumption: that a query maps to a single target, scored by Recall@K against one annotation. We argue this is fundamentally at odds with the task. A query such as make it more formal does not name an image but a region of the corpus, and which member the user intends is genuinely underdetermined. This underspecification is the root of the well-known false-negative problem and leaves current models unable to tell a precise query from an ambiguous one. We reframe CIR as calibrated intent resolution under uncertainty: a retriever is wrapped in a conformal prediction layer that returns a candidate set with a coverage guarantee and whose size is a principled measure of ambiguity; when the set is large, an expected-information-gain policy asks the single most useful clarifying question, drawn from interpretable ambiguity axes, and the set contracts. We introduce AmbiCIR, a benchmark and human-validated user simulator that revive the dormant auxiliary and dialogue annotations of CIRR and extend the multiple-positive setting of CIRCO. Across open-domain and fashion benchmarks our method matches single-turn state of the art, confirming calibrated resolution is cost-free on precise queries, while reaching the intended target in a fraction of the interaction budget required by naive conversational baselines, and it is the first to report valid coverage and calibration for the task.

---

## 论文详细总结（自动生成）

# 论文总结：Resolving Ambiguity in Composed Image Retrieval via Calibrated Interaction

## 1. 核心问题与整体含义（研究动机和背景）
- **问题**：组合图像检索（CIR）中，查询（参考图像+修改文本）通常被假设为映射到单一目标图像，但实际中许多查询（如“make it more formal”）仅指定了语料库的一个区域，具体目标存在**内在歧义性**。这导致两个严重后果：一是在训练和评估中，有效但不被标注的目标被当作假负例（false-negative problem）；二是现有模型无法区分精确查询与模糊查询，总是输出过度自信的点排名。
- **动机**：作者认为CIR应被重新定义为**不确定性下的校准意图解析**，系统应能量化歧义、给出有覆盖率保证的候选集，并在歧义较大时主动向用户提问以消除不确定性。
- **意义**：该问题在现有SOTA方法中均被忽略，本文首次将其形式化并提供了完整的解决方案，推动了CIR从“确定性匹配”向“不确定交互”的范式转变。

## 2. 方法论：核心思想、关键技术细节
### 2.1 核心思想
- 将CIR视为校准意图解析过程：检索器输出在语料库上的信念分布，通过共形预测层转化为具有覆盖率保证的候选集，候选集大小作为歧义信号；若集合过大则触发基于期望信息增益的澄清问题，用户回答后信念更新，候选集收缩。
- 保证单轮性能不退化（对精确查询立即提交），同时通过交互高效处理模糊查询。

### 2.2 关键技术细节
1. **基础检索器与信念**：使用冻结的视觉语言骨干（如BLIP-2、CLIP、OSCAR）加轻量融合适配器，计算查询与每个图像的余弦相似度，经温度缩放Softmax得到信念分布 \( p(I | q, h_m) \)。历史信息通过语义更新（ϕ(q, h_m)）和逻辑重加权（式8）注入。
2. **共形预测层**：
   - 使用**分割共形预测**（APS），在预留校准集上计算累积非一致性得分，取经验分位数 \(\hat{\eta}_\alpha\)。
   - 测试时，按信念降序累积概率直到超过 \(\hat{\eta}_\alpha\)，形成候选集 \(C_\alpha(q, h_m)\)，理论上保证 \( \Pr(I_T \in C_\alpha) \ge 1-\alpha \)。
   - 采用**蒙德里安（组条件）共形预测**，针对四种歧义轴分别计算阈值，以平衡各组的覆盖率。
   - 候选集大小 |C_\alpha| 作为歧义量化指标。
3. **澄清策略**：
   - 触发条件：当 |C_\alpha| > τ 且轮次未达上限时执行澄清。
   - 候选问题来自四个可解释的**歧义轴**（保留什么、改变什么但不相关、视角、背景），由MLLM（BLIP-2）生成具体问题。
   - 使用**期望信息增益**（EIG）公式选择最优问题：IG(a) = H(p_current) - Σ_r p(r|a) H(p_updated(r))，期望最大化熵减。
4. **信念更新**：
   - **语义更新**：将问题-答案对追加到文本端重新编码。
   - **逻辑重加权**：基于答案模型 g(a,I) 的软似然 \( \ell(r|a,I) \in [\epsilon,1] \) 乘以原信念（式8），防噪声。
   - 重新计算共形集。
5. **训练流程**：
   - 阶段1：训练融合适配器（软三元组损失+温度缩放校准）。
   - 阶段2：在D_cal上后验计算共形阈值（无梯度）。
   - 阶段3：学习轻量问题排序器（策略梯度），最小化期望轮次（式9），但最终结果用人实验证。

## 3. 实验设计
### 3.1 数据集与基准
- **CIRR**：开放域，提供视觉相似子集和未使用的辅助标注（歧义轴、对话路径），用于训练/评估。
- **CIRCO**：开放域，提供每个查询多个有效正例（平均4.5个），适用于多正例评价和歧义真值。
- **FashionIQ**：时尚领域，测试域迁移能力。
- **AmbiCIR**：本文整合上述资源并增加交互协议的新基准。

### 3.2 对比方法
- **单轮方法**：监督型（TIRG、CIRPLANT）、零样本（Pic2Word、SEARLE、CompoDiff、OSrCIR）。
- **交互基线**：相同检索器上仅改变提问策略：RANDOM-Q、FIXED-Q、MLLM-Q（无EIG）、ORACLE-Q（理想上限）。
- **不确定性基线**：Top-K启发式、原始分割共形预测（无蒙德里安）。

### 3.3 评价指标
- 单轮：各数据集原生指标（CIRR Recall@K/Recall sub@K, CIRCO mAP@K, FashionIQ Recall@10/50）。
- 校准：经验覆盖率、最差组覆盖率、平均集大小|C|、期望校准误差（ECE）。
- 交互：Success@T（在T轮内成功）、轮次计数（TTS）、澄清率。

## 4. 资源与算力
- 论文中提及：**单GPU训练**，融合适配器使用AdamW优化器，训练分三个阶段。
- **未明确说明**：GPU型号（如A100还是V100）、具体训练时长、显存消耗等。仅提到“Single GPU”和“实现细节见补充材料”，但补充材料未提供。因此资源细节无法从正文完整获取。

## 5. 实验数量与充分性
- **单轮比较（Tab1）**：在CIRR、CIRCO、FashionIQ上与6种基线全面对比，所有指标持平或略优（在±1.0显著性阈值内），证明无单轮代价。
- **校准与覆盖（Tab2）**：在三种风险水平（α=0.05/0.10/0.20）下对比Top-K和基本共形预测，显示蒙德里安共形预测在覆盖、集大小、ECE上全面优势。
- **歧义测量验证（Tab3）**：在CIRCO上计算|Cα|与真值正例数之间的斯皮尔曼ρ=0.62，并展示不同歧义轴的集大小和澄清率，证明歧义信号可靠。
- **交互效率（Tab4）**：在三个数据集上与多种交互策略对比，显示EIG策略显著优于随机/固定/MLLM基线，接近oracle。
- **消融实验（Tab5）**：移除一个组件（共形触发、EIG、信念更新等），量化每项贡献，证明所有设计均有效，EIG贡献最大。
- **敏感性分析（Tab6）**：对提交阈值τ和风险水平α进行扫描，显示在宽范围内稳定。
- **跨域泛化（Sec4.10）**：从CIRR零样本迁移至FashionIQ，交互后恢复大部分性能，验证稳健性。
- **人类研究（Sec4.9）**：30名参与者600次查询，模拟器与真实用户一致性88.7%，说明实验室仿真较真实。
- **整体评价**：实验覆盖单轮、校准、歧义、交互、消融、灵敏度、域迁移、人机交互，非常全面且客观。所有数值为3次随机种子均值，标注统计显著性阈值，公平性良好。

## 6. 主要结论与发现
- **无单轮代价**：校准解析在精确查询上匹配SOTA单轮性能，不额外增加成本。
- **歧义可量化**：共形集大小真实反映歧义程度（与CIRCO多正例数强相关），且不同歧义轴对应不同集大小。
- **交互高效**：基于期望信息增益的单个澄清问题即可大幅提升检索成功率，轮次需求远低于朴素对话基线（如MLLM-Q）。
- **首次实现校准**：在CIR任务上报告了有效的覆盖率和校准误差，填补空白。
- **域迁移**：即使零样本迁移到时尚领域，校准解析仍能通过交互缓解性能下降。

## 7. 优点
- **问题定义的革新**：明确指出现有CIR范式缺陷，并提出更符合实际的使用场景（交互式模糊查询）。
- **技术整合巧妙**：将共形预测、贝叶斯实验设计、MLLM有机结合，设计出可插拔的解析层，不依赖强大骨干。
- **数据复用与构建**：复活CIRR闲置标注，扩展CIRCO多正例为歧义真值，并构建人体验证的用户模拟器，为后续研究提供平台。
- **严谨性**：多随机种子、统计显著性阈值、人类验证，且公开所有评价协议。
- **实用导向**：精确查询零开销，模糊查询低开销（平均0.75次MLLM调用），满足实际系统效率要求。

## 8. 不足与局限
- **理论假设**：共形预测的覆盖率保证依赖于校准集与测试集之间的可交换性，虽用蒙德里安部分缓解，但严重分布偏移下仍可能退化。
- **MLLM依赖**：澄清问题生成和内部答案模型依赖BLIP-2，其质量限制了上限，且每次澄清增加推理延迟（平均0.21秒）。
- **用户模拟器偏差**：尽管与人类有88.7%的答案一致性，但模拟器无法捕捉真实用户的策略性、不一致性及复杂背景，可能高估效果。
- **交互轮次上限**：固定为M=3，未探索更长的多轮对话或动态终止策略。
- **假阴性问题缓解有限**：仅在后验共形集层面处理，训练仍使用传统三元组损失，未完全消除假负例对训练的影响。
- **应用限制**：实验仅在开放域和时尚域进行，尚未在更广泛场景（如遥感、医疗图像）验证；且需要用户配合回答，不适用于完全自动化的检索。

（完）
